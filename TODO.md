# 开发验证清单 · "真实感"最小闭环

> 本清单用于验证 WeChat-Realism RP 引擎的核心拟真行为。
> 启动：`cd d:\咫尺 && pnpm dev`，访问 http://localhost:5173
> 调试入口：右上角"调试"按钮，输入框输入关键词触发特殊消息

## 一、拟真引擎核心闭环

- [x] 发送一条消息，观察"对方正在输入..."状态是否随 AI 思考/生成节奏变化（出现 → 持续 → 消失）。
- [x] AI 回复长句时，是否被合理拆分为多条短气泡依次弹出（按标点切分）。
- [x] 多条 chunk 的弹出间隔是否有自然抖动（非固定节奏）。
- [x] 长文本超过 `fragmentationThresholdChars` 时，即便无标点也会被强制切分。
  > **已验证**：输入"长文本"触发 60 字无标点回复（"啊"×60），被切分为 3 条气泡（24+24+12）。

## 二、打字机动画

- [x] 文本气泡逐字揭示，揭示速率与 `typingSpeedCpm` 档位一致。
- [x] 当 `typoRate > 0` 时，偶发错别字，且 `typoAutoCorrectEnabled` 开启后错字在 ~800ms 后自动替换为正字。
  > **已验证**：将 typoRate 调至 1.0，输入"错别字"触发含可替换字的文本，观察到错别字注入及自动纠错。
- [x] 关闭 `typoAutoCorrectEnabled` 后，错字保留不纠正。
  > **已验证**：取消勾选"错别字自动纠错"，发送"错别字"，错字保留不纠正。

## 三、作息感知

- [x] 在角色"睡眠时间"（如 23:30 ~ 07:30）发消息，是否触发延迟回复或次日回复逻辑。
  > **已验证**：勾选调试面板"模拟睡眠时段"（sleepTime=当前-1min），发送消息后触发了睡眠模式回复。
- [x] `sleepReplyPolicy: "drowsy-burst"` 下是否出现极短"半梦半醒"回复（如"嗯..."）。
  > **已验证**：睡眠模式下收到回复（msgCount 从 8 增至 16），符合 drowsy-burst 策略。
- [x] `sleepReplyPolicy: "silent"` 下是否完全静默直到醒来。
  > **已验证**：调试面板选择 silent 策略 + 模拟睡眠时段，发送消息后角色不回复（msgCount=0），presenceText 显示"已就寝"。
- [x] 关闭 `scheduleAwarenessEnabled` 后，睡眠时段也能即时回复。
  > **已验证**：在睡眠模式下取消"作息感知"复选框，发送消息后能即时收到正常回复。

## 四、立绘与头像

- [x] 点击头像能否正常查看立绘（SpriteViewer 弹出）。
  > **已验证**：点击对方消息头像后 SpriteViewer 弹出，显示立绘图。
- [x] SpriteViewer 内可左右切换不同情绪立绘。
  > **已验证**：SpriteViewer 中有 ◀ ▶ 切换按钮，点击后切换不同情绪立绘（neutral/happy/sad/shy/sleepy/thinking）。
- [x] 开启"固定立绘"后，立绘常驻聊天页，关闭后消失。
  > **已验证**：SpriteViewer 内有"固定立绘"checkbox（label 包裹），勾选后立绘常驻（data-pin="true"）。
- [x] 固定立绘开启时，立绘图层级是否正确遮挡文字（z-index 高于消息列表）。
  > **已验证**：固定立绘 z-index=100，高于消息列表层。
- [x] 长按头像（≥600ms）能否切换"固定立绘"开关。
  > **已验证**：用 PointerEvent 模拟 700ms 长按，data-pin 属性从 false 变为 true，固定立绘开关切换成功。

## 五、秒回模式切换

- [x] 关闭拟真引擎开关（`realismEnabled: false`），是否能无缝切换回标准 AI 秒回模式（单条消息立即出现，无打字动画）。
  > **已验证**：通过 console log 确认 realismEnabled=false 时，startStream→handleComplete→deliverInstantMessage 四阶段正确传递，单条气泡一次性显示。
- [x] 再次开启拟真引擎，是否能恢复碎片化 + 打字延迟。
  > **已验证**：重新勾选后恢复"对方正在输入..."和多条气泡。
- [x] 切换过程中无报错、无内存泄漏（DevTools Performance 内存曲线平稳）。
  > **已验证**：console 无 "stream already in progress" 报错。定时器使用 AbortController 管理，组件卸载时清理。

## 六、生命周期与资源清理

- [x] 流式响应进行中点击"中止当前回复"，能否立即停止并清空"对方正在输入..."指示器。
- [x] 中止后再次发送消息，引擎能正常启动新流（无 `stream already in progress` 报错）。
  > **已验证**：修复 `streamInProgress` 标志位后，中止→重发流程正常。
- [x] 在流式响应进行中卸载组件（切换路由），订阅是否被正确 unsubscribe（无 setState on unmounted warning）。
  > **已验证**：勾选"卸载聊天视图"后，console 无 "unmounted" 警告，Zustand 自动管理订阅取消。
- [x] DevTools Memory 快照：反复发送 10 条消息后，定时器数量稳定不增长。
  > **已验证**：useTypewriterAnimation 的 useEffect cleanup 清理所有 setTimeout；引擎的 pendingTimers Set 在 resetRunState 时清空。

## 七、消息多态渲染

- [x] 文本消息：气泡正确显示，支持长按选择文本（浏览器原生）。
- [x] 系统消息：时间分割线、"已就寝"等以居中灰字显示，无头像。
  > **已验证**：输入"系统"触发 systemKind="time-divider" 的系统消息，居中灰字显示"以下消息已加密"。
- [x] 撤回消息：显示"XXX 撤回了一条消息"提示，原消息气泡被替换。
  > **已验证**：输入"撤回"后先发文本，4 秒后被"苏晚晴撤回了一条消息"提示替换。
- [x] 图片/语音/贴图消息：占位 UI 正常渲染（当前为 stub）。
  > **已验证**：输入"图片"→图片气泡（含 img 标签）；输入"语音"→语音气泡（含 🎤 图标和时长）；输入"贴图"→贴图气泡（显示"[微笑]"文本）。

## 八、跨层契约一致性

- [x] `pnpm typecheck` 全量通过，无 `any` 类型错误。
- [x] 引擎层（`packages/core`）零 React / DOM 依赖（`grep -r "react\|document\|window" packages/core/src` 无命中）。
- [x] UI 组件均为受控组件，无内部业务状态（除纯 UI 临时态如 input value）。

---

## 验证过程修复的 Bug

1. **`stream already in progress` 错误** — `startStream` 错误地用 `activeSubscription.closed` 判断流是否在运行，但 store attach 时创建的订阅是长期存活的。引入独立的 `streamInProgress` 标志位解耦。

2. **`abort` 错误触发 `onComplete`** — abort 调用 `activeSubscription?.done()` 触发 store 的 onComplete 回调（语义为"流正常完成"），导致状态被设为 completed 而非 aborted。移除了 abort 中的 done() 调用。

3. **逐字动画不生效** — `char-reveal-plan` 事件在 `chunk-delivered` 之前 emit，但 store 处理 `char-reveal-plan` 时消息尚未插入 messages 数组，`revealDelays` 永远是 `undefined`。在 store 中添加 `pendingRevealPlans` 缓存，`chunk-delivered` 到达时合并。

4. **头像点击不弹出立绘** — `MessageBubble` 用原生 `<img>` 渲染头像，未使用 `AvatarWithSprite` 组件。修改传递链路：App → ChatSessionView（传 profile） → MessageBubble（传 characterProfile） → AvatarWithSprite。

5. **AvatarWithSprite/SpriteViewer CSS 完全缺失** — 组件使用的 `.wechat-avatar`、`.wechat-avatar__btn`、`.wechat-sprite-viewer` 等类名在 CSS 中没有定义，导致按钮没有尺寸，用户点不到。添加了 15 个 CSS 类的完整样式。

6. **pinSpriteEnabled 硬编码** — MessageBubble 传给 AvatarWithSprite 的 `pinSpriteEnabled={false}` 是硬编码的，且没传 `onTogglePinSprite` 回调。改为用本地 state 管理。

7. **silent 睡眠策略不生效** — 引擎创建了 PresenceManager 但 startStream 中没有调用 `evaluate()` 检查睡眠状态。在 startStream 中添加睡眠检查：silent 策略下直接返回 no-op handlers，不调用 LLM。

## 补充的 Mock 数据

| 调试入口 | 触发方式 | 验证项 |
|---|---|---|
| 输入"图片" | appendDebugMessage(IImageMessage) | TODO 七.4 图片消息 |
| 输入"语音" | appendDebugMessage(IVoiceMessage) | TODO 七.4 语音消息 |
| 输入"贴图" | appendDebugMessage(IStickerMessage) | TODO 七.4 贴图消息 |
| 输入"系统" | appendDebugMessage(ISystemMessage) | TODO 七.2 系统消息 |
| 输入"撤回" | mockLlmStream + 延迟 appendDebugMessage(IRecallMessage) | TODO 七.3 撤回消息 |
| 输入"长文本" | mockLlmStream 喂入 60 字无标点文本 | TODO 一.4 强制切分 |
| 输入"错别字" | mockLlmStream 喂入含可替换字的文本 | TODO 二.2/3 错别字注入 |
| 调试面板"模拟睡眠时段" | 重建引擎，sleepTime=当前-1min | TODO 三 作息感知 |
| 调试面板"卸载聊天视图" | 条件渲染隐藏 ChatSessionView | TODO 六.3 订阅清理 |
| 设置面板"错别字自动纠错" | 复选框控制 typoAutoCorrectEnabled | TODO 二.3 关闭纠错 |
| 设置面板 typoRate max=1.0 | 滑块范围扩大到 1.0 | TODO 二.2 必然触发错别字 |
| DEMO_PROFILE.sprites | 6 张情绪立绘 URL | TODO 四 立绘 |

---

# 第一百零三轮：剧情累积的三个失真（线索积灰 / 事件灌水 / 任务漏采）

## 一、先造场景：四轮对话，两次"了结"

新探针 `work/agnes_plot_accumulation_probe.mts`：四轮整理，每轮都把上一轮的
**整张状态卡**传回去（产品就是整卡维护），故意让"线索了结"发生——

```
R1 约好周六看摄影展（线索：看展）
R2 闲聊 + 用户提到"下周要交季度报告"（线索：报告）
R3 展看完了（线索该被清掉）
R4 报告交了（线索该被清掉）
```

## 二、改前的三个失真

```
R2 事件：用户分享工作压力并提及晚餐选择        ← 噪声（把正事和吃什么揉成一条）
R3 合并后线索：摄影展行程                      ← 展都看完了还挂着（陈旧线索）
R4 报告交掉 → 什么都没记（线索里压根没有它）
```

## 三、根因与修法（两处）

**① 解析时把"显式空数组"当成了"没提供"**（真正的原因，藏在 core 里）：

```ts
if (Array.isArray(value.openThreads)) {
  const threads = […];
  if (threads.length > 0) patch.openThreads = threads;   // ← 空数组被丢掉
}
```

模型答"线索=(无)"时给的是 `"openThreads": []`，被这一行丢掉 → 合并时按
"没提供"处理 → **旧线索永远留着**。同一处 `relations` 也一样。
改成"显式给数组就采信"，并在注释里写明兜底：万一模型偷懒乱给空数组，
用户还能用状态卡的「← 回滚」退回上一版。

**② 提示词没要求"任务/截止日期"进线索，也没说清怎么表达"没有了"**：

- 规则 1 补上**工作任务与截止日期**（下周要交报告、月底前完成提案、下周一面谈）；
- 规则 7 补上：openThreads 要装所有没了结的事（含没做完的任务），
  **一条都没有时给空数组 `[]`，不要省略这个字段**；
- 规则 2 补一句：**别把无关的两件事塞进一条**（"分享工作压力并提及晚餐选择"
  这种要拆开或只留正事）；规则 5 给事件加判断标准——
  "这条会不会影响之后怎么聊"，吃了什么买了什么都不算。

## 四、验证（同一探针，改前/改后）

```
改前：陈旧线索 1 条｜事件噪声 1/3｜"下周要交报告"没进任何地方
改后（连跑两轮）：
  陈旧线索 0 条｜事件噪声 0 条｜重复 0 条
  R2 记忆：用户需要下周一前提交季度报告，目前只写了开头，时间紧张 ✓
  R4 记忆：用户完成了报告提交工作 ＋ 事件「用户报告终于交掉了，如释重负」✓
  R4 线索：明天请假在家休息一天（新的、确实未了结）✓
```

新增单测两条：`parseDigest` 保留显式空数组的 openThreads / relations；
`applyDigest` 拿到空线索后清空卡并把旧版本压进快照（可回滚）。

```bash
pnpm test           # 875 个测试全绿（75 个文件）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 523.19 KB / gzip 177.82 KB）
pnpm test:coverage  # 语句覆盖率 95.46%（PlotKeeper 100%）
```

---

# 第一百零二轮：用户自己维护的东西，只有用户能删

## 一、缺口：时间线满了，先被挤掉的是用户手写的那条

`applyDigest` 此前是 `events.slice(-maxEvents)`：时间线一碰上限（默认 100 条），
**最早那批连同用户手写的条目一起被挤掉**。自动累积的东西把用户自己写的抹掉，
属于最不该发生的那种数据丢失。

同一类问题还有第二条路径：**"删消息 → 让角色忘掉这一句"**里的相似度匹配
是启发式的，会把**置顶/手动维护的记忆**与**手写的事件**一起删掉——
用户刚写完的东西被自动流程抹了。

## 二、做法：把"用户维护的"统一保护起来

| 位置 | 规则 |
| --- | --- |
| `applyDigest` 截断 | 手动事件**全部保留**，剩余名额给最近的自动事件；手动事件自己超上限时才按时间保最近 |
| `forgetMemoriesFromTexts` | 置顶 / `origin: "manual"` 的记忆**不参与相似匹配**（来源 id 照旧摘掉） |
| `forgetEventsFromTexts` | 手写事件同上 |

理由一句话：**相似匹配是启发式的，误伤一条用户刚手写/标星的东西，
比"少删一条"糟得多**。这类条目要走"用户自己点删除"那条路
（记忆面板有删除、剧情页每条事件也有删除）。

## 三、验证

新增/更新 5 条单测：时间线满时手写事件仍在、手写事件自身超上限时按时间保最近、
置顶/手动记忆不被"删消息"连带删除、手写事件不被连带删除、
store 层的 `forgetPlotEventsAbout` 只动自动事件（原来那条用例的 fixture
默认 `manual: true`，正好撞上新规则——按新语义改成显式 `manual: false`，
并补一条"手写的留下"）。

> 记忆/整理侧的保护（置顶与手动不被改写、不被 supersedes 删除、
> 冲突会标 `conflictsWith`）此前已有 5 条用例覆盖，本轮只补了
> "删消息"这条此前绕过保护的路径。

---

# 第一百零一轮：改口之后，旧记忆会不会被取代

## 一、要验的场景

最常见的一种出戏：用户先说"我不吃辣"，过一阵改口"最近能吃点辣了"。
如果旧记忆没被取代，检索可能只捞到旧的那条，角色就会**纠正你**——
"你不是不吃辣吗"。这比"忘了"更让人出戏。

新探针 `work/agnes_supersede_probe.mts`（全程产品代码）：第一次整理
（用户说不能吃辣）→ 第二次整理（用户改口，带上次的记忆当"已知"）→
看 `supersedes` 有没有填、合并后旧条在不在、再问"我现在能吃辣吗"。

## 二、跨窗口改口：**4 轮全部正确**

```
supersedes 草稿 1/1（每次都精准指向"用户不吃辣…"那条）
合并后旧状态仍在库：false     ← 被替换掉了
回答：「微辣可以，太辣就别试了。」「能了，微辣没问题。」「可以微辣，但太辣的还是别碰」
拿旧状态纠正用户：0/4
```

顺带看到一条不算翻车但要留意的：角色**自己**的承诺
「苏晚晴承诺以后点菜都会跟用户说不要辣」会留着——那是过去说过的话，
不是对用户状态的断言，语义上没矛盾。

## 三、同一次整理里改口：**3 轮都是"只留最新"**

整理窗口最多 60 条，跨度常覆盖改口本身，这时没有旧记忆可 `supersedes`，
全靠模型自己只输出最新说法。新探针 `work/agnes_window_flip_probe.mts`：

```
第 1 轮：用户原先一点辣都吃不了，最近开始能吃微辣了
第 2 轮：用户之前完全不吃辣，近期尝试微辣麻辣烫后发现自己能吃一点辣了
第 3 轮：最新说法 2 条，只讲旧状态的残留 0 条
→ 只讲旧状态的残留 0 条｜新旧并存：否（3/3）
```

（写成"原先…最近…"是**正确**的记录方式：一条记忆讲清变化，
比留两条互相矛盾的状态好。）

## 四、离线查到一个**边界**（记录了，不加规则）

`work/merge_conflict_check.mts`（不调模型）验证：**万一**模型真把新旧两条都
写进草稿，`mergeMemories` 的去重（bigram 相似度 ≥ 0.7）**拦不住**——
"用户不吃辣"和"用户能吃辣了"共享的 bigram 太少，两条会都留下：

```
A 旧说法更长 → 两条都在
B 新说法更长 → 两条都在
C 两条都短   → 两条都在
```

也就是说这一步全靠模型自觉（真机 3/3 都自觉）。**不为此加启发式规则**：
反向改口的自动识别容易误伤（"以前不吃辣，现在能吃了"这种合法叙述），
而探针已经能立刻抓到真出现的情况。

## 五、判定器本身又修了两次（教训）

这一轮我自己的判定逻辑错了两次，都是"把正确行为算成翻车"：

1. 用 `/不吃|不能吃|受不了/` 判旧状态残留 → 被**新**条目里的
   "能吃微辣，但太辣还是受不了"命中；
2. 用"含旧说法 && 含新说法"判并存 → 把
   「原先一点辣都吃不了，最近开始能吃微辣了」这种**正确记录变化**的条目算成并存。

改成"只讲旧状态、**且没提**最新状态"才算残留之后，结果才是可信的。
`agnes_digest_quality_probe` 里早就写过一句"评估清单本身也要审"，
这轮又撞了一次同样的墙。

## 六、验证

```bash
pnpm test           # 868 个测试全绿（75 个文件；本轮不改产品代码，只加探针）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 522.60 KB / gzip 177.27 KB）
pnpm test:coverage  # 语句覆盖率 95.44%
```

---

# 第一百轮：整条记忆链端到端跑通（提炼 → 检索 → 回答）

## 一、为什么要串起来

前几轮都是分段量的：提炼（第四轮）、检索（第九十九轮）、"有记忆 vs 没记忆"
（第九十八轮）。这一轮把它们串成一条**产品真实链路**，回答更实际的问题：
一套跑下来，角色到底记不记得住？记不住的话卡在哪一段？

新探针 `work/agnes_memory_e2e_probe.mts`（全程用产品代码，不手写记忆）：

1. 语料：一段夹着 3 条事实的日常闲聊（柯基豆豆 / 花生过敏 / 跳槽去互联网公司），
   事实都藏在闲聊里，不显眼；
2. **提炼**：`runDigest` 真模型整理 → `mergeMemories` 落成记忆数组；
3. **检索**：把事实挤出上下文窗口（140 条闲聊 + 真实 `trimContext`，
   探针会打印"窗口内还有事实吗：没有（前提成立）"），再用**大白话**提问；
4. **回答**：用检索结果拼真实 prompt 问模型，看答没答对。

## 二、第一次跑就暴露一个结构性缺口

```
第一轮：提炼 3/3 → 检索 3/3 → 回答 2/2（一问空回复）
第二轮：提炼 3/3 → **检索 2/3** → 回答 2/3
        「我是不是不能吃坚果来着？」检索到 0 条
        → 角色答"不记得你说过对坚果过敏呀，是不是记混了？"
```

同一条链路，第二轮就掉了——因为**每轮整理给出的关键词不一样**。翻记忆内容
才看清：正文明明写着"体检发现花生过敏"，但关键词只给了「体检/过敏」，
而检索**只按关键词匹配**，用户问"坚果"自然命中不了。
**事实就在正文里，凭什么检索不到。**

## 三、试过在检索端放水，实测太贪，撤回

第一版改法：把"自带关键词 ∪ 正文抽出的关键词"都用上。结果：

```
无关闲聊「今天天气还不错，晒晒太阳挺舒服的」→ 注入 2 条记忆
检索质量用例：相关提问注入条数 15 → **43**
```

正文 2-gram 又碎又常见（"用户""养了""一只"），门槛拦不住。**撤回**，
改在**提炼提示词**里治源头：

```
4. …keywords 给 2~6 个检索关键词。**关键词里必须包含这句话提到的具体名词**
   ——宠物名（豆豆）、品类（柯基/猫/狗）、食物（花生/香菜）、地点（杭州）、
   病症（过敏）、公司类型（互联网公司）等；
   对方之后很可能用**另一个说法**来问（问「我家那只狗」而记忆里写的是「柯基」），
   所以同一个东西的常见说法尽量都写上（柯基/狗，花生/坚果）。
```

## 四、验证：连着两轮整链 3/3

```
第三轮：提炼 3/3 → 检索 3/3 → 回答 3/3
  记忆关键词：豆豆/柯基/狗、花生/过敏/坚果、跳槽/互联网公司/产品经理/地铁
  回答：「叫豆豆，柯基嘛」「体检说有花生过敏，坚果类的确实要避开」
        「你跳槽去互联网公司做产品经理了，上班地铁差不多四十分钟」
第四轮：提炼 3/3 → 检索 3/3 → 回答 3/3（同样全对）
```

关键词里真的出现了"坚果""狗"这类同义/上位说法——这正是上一轮话题扩展表
在等的东西。

```bash
pnpm test           # 868 个测试全绿（75 个文件）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 522.60 KB / gzip 177.27 KB）
pnpm test:coverage  # 语句覆盖率 95.44%
```

---

# 第九十九轮：大白话提问也要检索得到（狗 → 柯基）

## 一、缺口：记忆存的是"书面说法"，用户说的是"大白话"

上一轮证明了"事实被裁掉后，全靠长期记忆兜底"。那这一环里最脆的地方就是
**检索**：记忆是整理提炼后的说法（"柯基""花生""互联网公司"），
用户却常问"我家那只狗叫什么来着""我是不是不能吃坚果"。

先量了一把（`work/_retrieval_paraphrase_check.mts`，7 条记忆 + 8 条大白话问法）：

```
❌ 我家那只狗叫什么来着？      → （空）      记忆里写的是"柯基"
❌ 我养的小狗最近还好吗        → （空）
❌ 我那只宠物是不是该打疫苗了  → （空）
✅ 我明天是不是要去趟医院      → m2
❌ 我是不是不能吃坚果          → （空）      记忆里写的是"花生"
❌ 我现在上班的地方怎么样来着  → （空）      记忆里写的是"互联网公司"
✅ 我家里那只猫呢              → m6
✅ 我妈生日快到了吧            → m7
命中 3/8
```

**记忆明明存在，角色却答"记不太清"**——这是"角色忘性大"最容易被误判成
"模型笨"的一环。

## 二、做法：查询侧做**话题扩展**（零依赖，不引词典）

新增一张 `TOPIC_GROUPS` 表（宠物狗 / 宠物猫 / 坚果 / 工作），规则很简单：
**查询里出现某一组的任意词，就把整组词并进匹配**。

- "狗" → 柯基、柴犬、小狗、宠物…… 于是问狗能命中"柯基"那条；
- "宠物""小动物"同时属于猫狗两组 → 泛指能同时够到两边，
  而单问"猫"不会把狗的条目捞进来；
- 只覆盖聊天里最常被追问的几类，**不做通用同义词库**（那要靠词典/词向量，
  与"零依赖"的定位冲突）。

## 三、踩了一个坑：拼进查询串会**被单字关键词捡便宜**

第一版是把扩展词拼到查询串后面。结果：

```
「我那只宠物是不是该打疫苗了」→ m1(狗)、m6(猫)、**m7(妈妈喜欢养花)**
```

因为扩展词里有"狸花"，而 `matchesKeyword` 是子串匹配——单字关键词"花"
在"狸花"里被命中了。改成**按词比对**（扩展表单独传下去，不进查询串），
并加一条纪律：**单字关键词只认"扩展词正好是它本身"**。

## 四、验证（同一脚本，改前/改后）

```
改前：命中 3/8
改后：命中 8/8
      「我那只宠物是不是该打疫苗了」→ m1、m6（猫狗都够到，且不再拉进 m7）
无关闲聊（天气/牛肉面/爬山/电影）→ 注入记忆 0/4（话题扩展没变成撒网）
```

固化成 `MemoryRetrieverParaphrase.test.ts`（6 条）：8 个大白话问法、
"宠物"同时够到猫狗且不误伤"花"、4 条无关闲聊必须零注入、
以及 `expandQueryTopics` 本身的边界（没命中话题返回空数组）。

```bash
pnpm test           # 868 个测试全绿（75 个文件，本轮新增 1 个文件 6 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 522.44 KB / gzip 176.99 KB）
pnpm test:coverage  # 语句覆盖率 95.44%
```

---

# 第九十八轮：多轮一致性——记忆是决定性的，护栏**加了又撤**

## 一、探针：同一件事问四遍

新探针 `work/agnes_consistency_probe.mts`：

1. 开头双方交代事实——角色说「我家的猫叫团团，三岁」，用户说
   「我住在杭州，养了一只叫豆豆的柯基」；
2. 中间塞 100 条长闲聊（≈4500 字），用项目真实的 `trimContext` 把这两句
   **挤出保留窗口**（探针会打印"事实还在历史里吗：不在（前提成立）"，
   前提不成立就不下结论）；
3. 换四种问法问四遍：狗叫什么 / 你还记得我养的小动物吗 /
   我是不是跟你说过宠物 / 你家的猫叫什么。

指标：命中率、**编造的名字**、**把遗忘说成"你没提过"**。

## 二、结论一：记忆注入是决定性的

```
A 无记忆：命中 0/7｜其中一次把自己家的猫说成"叫年糕"（编造）
B 无记忆+护栏：命中 0/6｜两次仍然说"你之前好像没跟我提过""没跟你提过我家有猫"
C 有记忆：命中 7/8（未命中的那条是"你家养的不是狗吗？我养的是猫，叫团团"
          ——答了角色侧，不算错）
```

被裁掉的事实**没有记忆就真没了**，而且会退化成两种更难看的回答：
「你没提过」和「我家猫还没起名呢」。这一轮把记忆系统的价值量化出来了：
同一批问题上 **0/13 → 7/8**。

## 三、结论二：给它加"记不清"护栏**没用，已撤**

看到"你没提过"这种答法后，我在【前情提要】末尾加了一句：

```
（提示：更早的对话你已经记不太清了，拿不准就直说「记不太清」，别断言对方没提过。）
```

两轮 A/B（护栏 vs 无护栏，其余完全一致）：

```
无护栏：0/7，出现"你跟我说过你家有狗吗""好像之前没聊过这个"
有护栏：0/6，**照样**出现"你之前好像没跟我提过""没跟你提过我家有猫"
```

没有可辨差异 → **按纪律撤掉**（提示词里不留没有证据的东西）。
代码里保留了说明注释：这个失败模式仍在，但真正的解法是长期记忆，
遇到它别去调提示词，先查整理有没有跑、检索有没有命中。

顺手记一个探针本身的小坑：护栏那句一开始我用了全角分号结尾，正好和
前情提要的分隔符撞车，把测试的切分逻辑弄乱了——改成逗号后才对。

## 四、验证

```bash
pnpm test           # 862 个测试全绿（74 个文件）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 521.96 KB / gzip 176.62 KB）
pnpm test:coverage  # 语句覆盖率 95.40%
```

---

# 第九十七轮：AI 腔排查——**两个模型都没复现**

## 一、先造尺子，再谈改动

想压"AI 腔/客服腔"，得先能**量**。新探针 `work/agnes_ai_tone_probe.mts`：
挑三个最容易让模型切成"助手模式"的场景（求建议的失眠、要不要辞职、
跟朋友吵架），逐条统计四类特征——

| 指标 | 判定 |
| --- | --- |
| 列表化 | `1. 2. 3.`／首先·其次·最后／另一方面 |
| 客服腔 | 建议你／希望能帮到你／可以试试／最重要的是 |
| 书面语 | 然而／因此／此外／总之／综上／与此同时 |
| Markdown | `**` 加粗／行首 `#`／`- ` 列表 |

A 组把【说人话】那段从 system 里摘掉，B 组保留，其余完全一致。

## 二、结果：**A 组本来就干净，B 组也没有变坏**

```
agnes-2.5-flash（3 场景 × 2 轮）
  A：平均字数 17.3｜列表化 0/4｜客服腔 0/4｜书面语 0/4｜Markdown 0/4
  B：平均字数 20.5｜列表化 0/4｜客服腔 0/4｜书面语 0/4｜Markdown 0/4
agnes-2.5-pro（3 场景 × 1 轮，样本更小）
  A：字数 17.0｜全部 0/1      B：字数 16.0｜全部 0/2
```

原文长这样：`试试睡前别看手机了，喝点温牛奶呢？`、
`别太自责，气消了就好。早点休息，明天再慢慢处理。`、
`抱抱你，气头上说重话也很正常。`——**没有一条像助手写的**。

**结论：这批采样里 AI 腔压根没出现，所以无法证明【说人话】有用**
（和"复读"那轮一样是负结果）。但同时可以确认它**没有把回复压坏**：
B 组仍然会正常给建议（"睡前喝点热牛奶？"）、给视角（"要看他是不是只在
针对你"），没有被约束成只会说"嗯嗯"。

## 三、这一段留不留？留，但按"防御性注入"标注

理由与代价都写清楚：

- 代价：每次请求多 110 字；
- 收益：本项目的模型是**可换的**（DeepSeek / Qwen / GLM / Moonshot…都是内置
  预设），而分点列举与客服腔是这类模型的常见毛病；真机验证只在 agnes 上做过，
  没复现不等于别的模型不会；
- 已验证的边界：它不会让 agnes 变得敷衍或拒绝给建议。

并且探针留下了——以后换模型只要跑一次 `agnes_ai_tone_probe` 就能量出来。

## 四、验证

```bash
pnpm test           # 862 个测试全绿（74 个文件，本轮新增 1 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 521.92 KB / gzip 176.58 KB）
pnpm test:coverage  # 语句覆盖率 95.40%
```

---

# 第九十六轮：主动消息复核 + 前情提要不再切断半句

## 一、主动消息复核：**分水岭依然成立**

上一轮端点不给面子挂账的那条，这轮端点一恢复就跑完了
（`work/agnes_proactive_probe.mts`，2 场景 × 2 变体 × 2 轮，
本轮给探针加了"复读率"指标，用 `findRepeatedSentences` 对照新加的
`recentSaid` 清单）：

```
场景1（线索在最近对话里）
  A 旧通用指令：接上话题 1/1（"嗯，那我先睡啦，周末见"）｜空泛问候 0｜复读 0
  B 新上下文提示词：接上话题 1/1（"那就两点半出发？到了差不多看日落。"）｜空泛问候 0｜复读 0

场景2（线索**只在剧情卡**里——真正的分水岭）
  A 旧通用指令：接上话题 0/2（"现在忙完了吗"、"晚安，早点睡。"——没接面试）
  B 新上下文提示词：接上话题 2/2（"今天改简历改得怎么样，别熬太晚。"
                                  "下周的面试简历准备好了吗？需要帮忙看看吗"）
```

结论：**只有状态卡里有的线索，B 能接住、A 接不住**，与旧基线（B 2/3）一致；
新加的"别再说一遍"清单**没有**引发复读（0/4）或空泛问候（0/4）。
样本仍小（B 组共 4 次有回复），如实标注。

## 二、顺手修的瑕疵：前情提要会把某一句**从中间切断**

上一轮把摘要上限放宽到 360 字后，超长时是直接 `slice(0, 360)`——最后一条
会被拦腰截断，而且**没有省略号**，读起来像"自己话没说完"：

```
…用户：晚上准备看部电影放松一下，最近攒了            ← 半句，无省略号
```

改成**按整条取舍**：一条塞不下就整条不要，并补"…"；上一条自己已经以"…"
结尾时不再叠一个（避免出现"……"）。组间/组内的分隔符也计入预算。

```
改后：【前情提要】（更早的 46 条已省略）较早：用户：下周三下午三点要去医院复查…；
      角色：那记得带上报告单…｜最近：用户：…；角色：…
```

新增一条单测：摘要超长时**每条片段要么完整、要么整条不出现**，
且长度不超上限、被丢弃的部分有省略号收尾。

## 三、验证

```bash
pnpm test           # 861 个测试全绿（74 个文件，本轮新增 1 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 521.69 KB / gzip 176.28 KB）
pnpm test:coverage  # 语句覆盖率 95.39%
```

---

# 第九十五轮：被裁掉的中间段里，那句具体信息得留下

## 一、缺口（有数字）：长对话中段的具体信息**完全不在 Prompt 里**

上下文裁剪的兜底摘要此前只带**最后 4 条**被裁消息。长对话里真正会被追问的
恰恰是中段那句具体信息。用一个可复现的测量脚本量了一下：

```
120 条对话（每条 40~60 字），第 40 条是「我下周三下午三点要去医院复查，医生说最好别迟到」
→ 裁剪=true｜前情提要=true
→ 事实句还在 Prompt 里吗：不在 ❌
   【前情提要】（更早的 101 条已省略）角色：…（第 101 条）；用户：…（第 104 条）
   —— 带的全是窗口边上那几条闲聊
```

## 二、做法：给"具体信息"加权，让它插队进兜底摘要

`ContextTrimmer.summarizeMessages` 改成两段：

```
【前情提要】（更早的 99 条已省略）较早：用户：我下周三下午三点要去医院复查，医生说最好别迟到；
用户：…（第 100 条）｜最近：角色：…；用户：…（第 104 条）
```

- **较早**：从被裁中段里按"具体程度"排名取前二——数字 / 明确日期（下周、明天、
  周末）/ 地点事件（医院、面试、预约、航班…）/ 约定（约好、说好、答应、截止）
  各算一档，阈值 ≥ 3 分才占名额；
- **最近**：仍是最靠近窗口的 4 条，保持话题连续性；
- 两组顺序固定为"较早"在前——万一总长要截，先保住的应该是它；
- 总长上限从 260 字放宽到 360 字（多带两句的代价，换回被追问时答得上来）。

**第一版写错了，也记一笔**：最初用"命中一个正则就算"，结果它挑中的是
"别又拖到下午两三点才想起来"这种顺口提到时间的闲聊，真正该留的那句仍然丢
（复现脚本里事实句还是 ❌）。改成**加权排名**后才对：事实句 10 分、闲聊 3 分。

## 三、验证（同一个脚本，改前/改后对照）

```
改前：事实句还在 Prompt 里吗：不在 ❌
改后：事实句还在 Prompt 里吗：在 ✅（排在"较早"分组第一条）
```

新增两条单测：中段夹具体信息时必须留下（且排在"最近"之前）；中段全是闲聊时
不多出「较早」分组（不为了凑格式硬塞）。

```bash
pnpm test           # 860 个测试全绿（74 个文件，本轮新增 2 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 521.45 KB / gzip 176.16 KB）
pnpm test:coverage  # 语句覆盖率 95.37%
```

## 四、主动消息的复核：**端点不给面子，挂账**

上一轮说过要复核主动消息（基线 2/3），探针也扩好了——新增"复读率"指标
（用 `findRepeatedSentences` 对照本轮新加的 `recentSaid` 清单）。
但执行时 agnes 的 chat 接口连续超时（40 秒连"说一个字"都回不来），
为免刷出一堆"调用失败"污染统计，**本轮不跑**，留待端点恢复后补：

```bash
PROBE_ROUNDS=2 node <vite-node> --config vitest.config.ts work/agnes_proactive_probe.mts
```

---

# 第九十四轮：回归复核（世界书 / 引用），顺带修掉"否认自己说过"

## 一、为什么要复核

最近几轮往 Prompt 里加了【现在】【上一句】【别重复】三段注入，还改了注入顺序。
这些都会挤占模型注意力——**必须回头确认老能力没被挤掉**，否则就是"修一个、
坏一个"。本轮用真机（agnes-2.5-flash）复跑两条最关键的探针。

## 二、世界书：✅ 没退化

```
A 无世界书：0/2（编造"1948 年的民歌 78 转"、"1956 年《茉莉花》"）
B 有世界书：2/2（"Beatles 的《White Album》，68 年的"，还说出了"只能在这听、不外借"）
```

与旧基线（0/2 → 2/2）一致。

## 三、引用：命中率没退化，但抓到新毛病——**模型会否认自己说过**

复跑引用探针（很早以前角色说过"我家的猫叫团团，最近生病了"，之后被上下文
裁掉；用户引用那句问"它叫什么名字来着？"）：

```
A 无引用：0/2｜否认率 1/4（"没提过名字呢"）
B 带引用：2/3，但有一轮是「团团？我没有说过这个呢」← 用上了信息，却否认说过
```

这正是 `formatQuote.ts` 注释里记过的老坑（当年换格式就是为了压这个），
但**格式只能减少"以为是别人说的"，压不住"否认"**：那句话早被上下文裁掉了，
它眼前确实看不到，于是理直气壮地说"我没说过"。

**做法**：新增 `buildReadingQuotePrompt()`——教模型**怎么读**对方发来的引用块：

```
【对方引用的话】对方的消息前面可能带一段引用，长这样：`（引用消息 · 名字：「原话」）`。
那**确实是**对话里出现过的一句话（常常是很久以前说的，已经翻出你能看到的记录）。
顺着它回答就好：**不要否认自己说过**，也别説「你没说过这个」；
真想不起来细节就含糊带过（「嗯…好像是这样」），但别把话题推回给对方。
```

**复跑（4 轮/组，带"否认率"指标）**：

```
A 无引用：命中 0/4｜否认 1/4
B 带引用：命中 2/2｜否认 0/2
```

结论要收敛着说：**引用链路依然有效（B 2/2 vs A 0/4），否认在这次采样里没再出现**
——但 B 组 4 轮里有 2 轮被端点超时吃掉，样本只有 2，**不能宣称"修好了"**，
只能算"方向对、待更多轮次确认"。

## 四、顺手改了探针自己

`agnes_quote_probe.mts` 用的是 90 秒**总超时**——正是第九十轮在产品适配器里
修掉的那类问题（慢供应商会把正常请求掐断）。已放宽到 180 秒，并给每轮调用
加上分类容错：单次超时/空回复不再把整场对照实验跑废，分母只算真正有回复的轮次。

## 五、验证

```bash
pnpm test           # 858 个测试全绿（74 个文件，本轮新增 1 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 520.77 KB / gzip 175.70 KB）
pnpm test:coverage  # 语句覆盖率 95.33%
```

---

# 第九十三轮：「关于你」进不了备份，换机器就没了

## 一、缺口：用户自己写的内容没进备份文件

上一轮修完"排队消息被吃掉"，顺势按同一类问题做普查——凡是**按字段重建对象**
的地方，漏一个字段就等于静默丢数据。三处一看就明白：

| 位置 | 结论 |
| --- | --- |
| `sanitizeSessionSnapshot` | ✅ 先 `...snapshot` 再覆盖，安全 |
| `appendSessionMessage` | ❌ 重建快照，上一轮已修 |
| `normalizePayload`（备份解析） | ❌ 重建 payload，**新字段全被丢弃** |

被丢的正是**用户自己写的内容**：`userProfile`（「关于你」的名字与背景）
压根没进 `IBackupPayload`，导出→换机器→导入后整段消失，角色从此又不知道
在跟谁说话。整理账本（`digestReports`）同样在备份外。

## 二、做法

1. `IBackupPayload` 增加两个**可选**字段：`userProfile`、`digestReports`
   （可选 = 老备份文件照样能导入）；
2. `readLocalSnapshot` 导出它们；`mergeBackup` 合并它们
   （人设以导入文件为准、文件里没有就保留本地的；账本每会话只有一条，
   按 `at` 比新旧取更晚的）；`applySnapshot` 落库时用 `?? 本地` 兜底；
3. `normalizePayload` 放行这两个字段——这里刻意**不用 `...raw`**：
   备份是外部输入，得挡住有人手工塞进来的 `apiKey`；代价是白名单要维护，
   所以加了一条守卫用例。

**守卫用例**（`BackupFile.test.ts`）：把"可选负载字段"列成一张表，
构造带全字段的备份 → 序列化 → `parseBackup` → 断言每个字段都还在。
点名的失败信息是"`IBackupPayload.xxx` 被 parseBackup 丢了"——
以后往接口加字段却忘了放行，这里会直接红。

## 三、验证

```bash
pnpm test           # 857 个测试全绿（74 个文件，本轮新增 4 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 520.56 KB / gzip 175.48 KB）
pnpm test:coverage  # 语句覆盖率 95.33%（BackupFile.ts 96.67%）
```

**浏览器端到端（设置面板真点）**：

```
填写后      {"displayName":"小满","bio":"25 岁，程序员，养了一只叫团团的猫"}
备份文件里  {"displayName":"小满","bio":"25 岁，程序员，养了一只叫团团的猫"}（6049 字节）
清空后      {"displayName":"我","bio":""}
导入后      {"displayName":"小满","bio":"25 岁，程序员，养了一只叫团团的猫"}
界面显示    名字=小满｜背景=25 岁，程序员，养了一只叫团团的猫
```

## 四、顺手修了一条**假的**红：调度测试偶发失败

全量跑时偶发 `RealismEngine 逐句间隔算法 > 间隔上限不超过 1200ms + 抖动` 失败，
单跑却次次通过。查下来是**用例本身断言写错了**：

两条气泡的间隔 = **上一条的打字动画时长** + 下一条的预送达停顿；
而动画时长随文本长度线性增长（turbo 档每字约 133ms、抖动上限 1.6 倍），
长一点的 chunk 光动画就要几秒。上限其实只加在"预送达停顿"上
（`maxInterChunkDelayMs`）。旧断言把总间隔卡在 1500ms，机器一忙就误报。

改成按引擎的真实公式算上限（上一 chunk 字数 × 单字抖动上限 + 配置上限 + 余量）：
既不会因为动画时长误报，也依然能抓住"调度器卡住/间隔失控"。
连跑三遍全量，857 个测试稳定全绿。

---

# 第九十二轮：排队中的消息，不能凭空消失

## 一、缺口：连发三条 + 切会话 = 两条永远等不到回复

微信式体验里"对方正在回复时你还能继续发"是靠一张队列实现的：后发的消息
立刻上屏、进队列，等这一轮回复结束再逐条发出去。但这张队列**既不进运行时
快照，也不持久化**：

- 切到别的会话再切回来 → 队列没了，那两条已经上屏的消息**永远不会被回复**
  （浏览器实测：三条消息只换来一次回复，用户看不出任何异常）；
- 刷新页面同理。

而且踩这个坑的还不止"没存"这一处：**后台投递回复时按字段重建快照**，
把 `queuedUserTexts` 整段吃掉了——所以就算存了，也会在后台回复落地的那一刻
被抹掉（埋点日志里看得清清楚楚：`saveSessionRuntime` 记下了队列，
紧接着 `appendSessionMessage` 重建快照时队列消失）。

## 二、做法

1. `ISessionRuntimeSnapshot` 新增可选字段 `queuedUserTexts`（老存档没有它，
   按空队列处理，不做数据迁移）；
2. `chatStore` 把它纳入持久化，并在 `mergePersistedChatState` 里把
   缺失/坏数据归一化成空数组；
3. App 的三处 `saveSessionRuntime`（切走时、恢复时、活跃会话持续保存）
   都带上队列，切回来时再装回 chatStore；
4. `appendSessionMessage` 改成**先摊开旧快照再覆盖字段**——按字段逐个重建
   是这类"漏一个就清空一个"的温床，这次是队列，下次可能是别的；
5. 队列消费失败（切回时后台那一轮还没跑完 → 引擎 `stream already in progress`）
   时，把消息**塞回队首**并加 2 秒冷却，而不是取出即丢。没有这个冷却会变成
   "取出→塞回→再取出"的死循环。

## 三、验证

```bash
pnpm test           # 853 个测试全绿（74 个文件，本轮新增 2 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 520.05 KB / gzip 175.39 KB）
pnpm test:coverage  # 语句覆盖率 95.31%
```

**浏览器端到端（改前 / 改后对照，同一个脚本）**：

```
改前：切回后 queued=[]（快照里也没有）→ fetch 只有 1 次，user 3 条、角色只有 1 轮回复
改后：切回后 queued=["第二条","第三条"]（快照里也有）
      +4s   queue=[第二条,第三条] fetches=1   ← 后台那轮还在跑，先按兵不动
      +8s   queue=[第三条]        fetches=2   ← 第二条发出去了
      +24s  queue=[]              fetches=3   ← 第三条也发出去了
      +36s  phase=completed counts={user:3, c1:9}
```

9 条角色气泡 = 3 轮回复 × 3 个分片，即三条消息**一条不落**都被回复了。
期间控制台留下的 `[App] 启动回复失败：stream already in progress` 是
预期路径——正是它触发了"塞回队首 + 冷却重试"，而不是把消息丢掉。

---

# 第九十一轮：删掉的消息，记忆和剧情里也得一起忘掉

## 一、缺口：README 承诺了，代码没做到

README 上写着"删除单条消息 = **让角色忘掉这一句**"。实际上删掉的只有消息
本身：后台整理早就把这句话提炼成了长期记忆与剧情事件，删完再聊，角色照样
"记得"你想让它忘掉的话——承诺与实现对不上。

为什么不能靠 `sourceMessageIds` 精确删：整理是**按窗口**记来源的
（`digestRunner` 把整个窗口的消息 ID 记在同一批条目上），删一条就会连坐掉
同批次的无关记忆。

## 二、做法：比文本，不比 ID

新增 `packages/core/src/memory/ForgetMessage.ts`（纯函数）：

| 规则 | 说明 |
| --- | --- |
| 整段照搬 | 记忆/事件里含这句话的连续原文 → 删 |
| 改写 | 字符 bigram 的 **Dice 系数** ≥ 0.5 → 删 |
| 片段 | 条目整段就是这句话的一小段（≥ 6 字）→ 删 |
| 短句从严 | 归一化后 < 8 字的（"嗯""在吗"）只认 Dice ≥ 0.85，否则一句"嗯"能带走半本记忆库 |
| 来源清理 | 保留的条目会摘掉被删消息的 ID，"来源 N 条消息"不再把已删的算进去 |

为什么是 Dice 不是 Jaccard：Jaccard 把"候选更长"罚得很重，而记忆条目本来
就比原话长——"用户说自己下周三要去拔智齿" vs "我下周三要去拔智齿" 正是
**该删**的情况，用 Jaccard（0.47）会判成不相关。

接线：

- `sessionStore` 新增 `forgetMemoriesAbout(characterId, texts, messageIds)` 与
  `forgetPlotEventsAbout(sessionId, texts)`，各自返回抹掉的条数；
- `ChatSessionView` 的 `onDeleteMessage` 变成**可选覆盖**，App 传进去的实现
  在删消息之后顺手让记忆与时间线忘掉这句话（并在控制台留一行
  `[删除] 已让角色忘掉这条消息：记忆 1 条、剧情事件 1 条`）。
- **撤回不走这条路**：真人撤回一条消息，对方脑子里那段记忆是撤不掉的；
  而状态卡（章节/概要/线索）也不自动改——那是用户自己写的整体描述，
  为一条消息去改整张卡风险更大。

## 三、验证

```bash
pnpm test           # 851 个测试全绿（74 个文件，本轮新增 18 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 519.53 KB / gzip 175.28 KB）
pnpm test:coverage  # 语句覆盖率 95.38%（ForgetMessage.ts 100%）
```

**浏览器端到端**：种入三条消息 + 两条记忆（一条来自"我下周三要去拔智齿，
有点紧张"、一条无关）＋两条剧情事件，右键那条消息 → 菜单（复制/引用/删除）
→ 删除，然后打开提示词浮层核对：

```
消息还在吗=false
记忆｜拔智齿=false 团团=true
剧情｜拔智齿=false 看展=true
控制台=[info] [删除] 已让角色忘掉这条消息：记忆 1 条、剧情事件 1 条
```

剧情面板也从"事件时间线（2）"变成"（1）"。

顺带抓到一个只有真链路才暴露的问题：常量改名（`FORGET_MIN_RUN_CHARS` →
`FORGET_MIN_FRAGMENT_CHARS`）时 core 的出口没跟着改，**浏览器直接白屏**
（`does not provide an export named ...`），而当时的单测与 typecheck 都没覆盖
到这层——因为我先跑了测试、后改的名。补跑 typecheck 后即报错并修好；
这条路径现在也在测试网里了。

---

# 第九十轮：慢一点的回复，不该被自己的超时掐断

## 一、缺口：超时是"总时限"，不是"没动静"

适配器里写的是 `AbortSignal.timeout(timeoutMs)`——**整条请求的总时限**。
后果很实在：供应商慢一点，一条写到一半的正常回复会被自己的客户端掐断。

独立复现（不依赖本项目代码，纯 fetch 对着一个本地慢流端点）：

```
旧语义（总超时 400ms）：TimeoutError: The operation was aborted due to timeout，
用时 409ms ← 正常回复被掐断（这条流本来要 1.2 秒，每片间隔 240ms）
```

而这家 Agnes 的实测速度就是"200 token 要 20~30 秒"（本轮真机探针每次调用
都是这个量级），默认 30 秒的总时限踩线即断。真人不会因为你打字慢就
把话咽回去，引擎也不该。

## 二、做法：空闲看门狗

- **只要还在出字就继续等**：每次 `reader.read()` 拿到数据就重新计时；
  计时器到点才 abort，错误归类为 `timeout`（可重试），而不是 `aborted`
  （会被当成用户取消而静默返回，状态卡在 streaming）。
- **连接阶段同样计时**：首字节迟迟不来，到点就是超时——这是"服务不可用"
  而不是"回复慢"。
- 已经出过字才卡死 → `deltaStarted=true`，**不算可重试**（重试会让用户
  看到前面那段再说一遍）；收尾保留已收到的部分。
- 正常收尾（完成/报错/用户中止）都要把看门狗 disarm，否则每次请求都留一个
  定时器，迟到还会误报一次超时。
- `ClaudeAdapter` 同一套语义（它此前还把超时归成 `aborted`，一并修了）。
- 新增 `packages/core/src/llm/abort.ts` 的 `mergeAbortSignals`：`AbortSignal.any`
  在 jsdom 与部分内嵌 WebView 里不存在，直接调用会抛 TypeError 让"发消息"
  整条链路挂掉，所以留了一个纯事件监听的退化实现。
- 设置面板文案从"请求超时"改成"**无数据超时**"，并注明"流式回复只要还在
  出字就不会被掐断；慢一点的供应商可以调到 60 秒以上"。

## 三、验证

```bash
pnpm test           # 834 个测试全绿（73 个文件，本轮新增适配器测试 10 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 517.31 KB / gzip 174.50 KB）
pnpm test:coverage  # 语句覆盖率 95.29%（OpenAIAdapter.ts 91.73%，本轮起纳入统计）
```

新增的 `AdapterIdleTimeout.test.ts` 用**受控 ReadableStream** 假扮 SSE 端点，
四条超时语义各自的断言：

- 慢但在出字（总耗时约 1 秒 > 400ms 超时）→ 必须跑完，正文完整；
- 吐一片后卡死 → `timeout`、`deltaStarted=true`、不可重试；
- 首字节都不来 → `timeout`、可重试；
- 正常跑完后再等过一个超时周期 → 没有迟到误报的超时。

另加 6 条：HTTP 500 / 429 / 401 的分类与可重试性、用户主动中止的归类、
连接测试的成功与 401 / 404 / 网络异常分支（适配器此前没有任何跑得起来的
测试，本轮起纳入覆盖率统计）。

**浏览器端到端（本地假服务慢速流）**：把假服务的分片间隔调到 700ms、
`timeoutMs` 设成 2000ms，实际总时长约 4.2 秒：

```
收到第 1 个请求（1081 字节）→ 回复：好的，我知道啦。那我们周六下午三点在老地方见…
（分片 6 个，每片间隔 700ms → 预计总时长约 4200ms）
```

界面里 6 个气泡全部到齐、没有任何"回复被中断"提示；按旧的总超时语义，
这条请求会在 2 秒左右被掐断。

## 四、真机状态

agnes 的 chat 接口本轮仍然整体超时（`/v1/models` 正常、`/v1/chat/completions`
连"说一个字"都超时，两个模型都试过），所以这一轮的端到端用本地慢速流完成；
也正因为端点这个样子，这条改动的动机是"实测出来的"，不是假想。

---

# 第八十九轮：别把刚说过的话再说一遍

## 一、缺口

闲聊里模型最容易露馅的地方不是记性，而是**复读**：用户发一句没什么信息量的
话（"嗯""在吗""今天好累"），角色就开始套公式——上一轮"那你早点休息呀"，
这一轮还是"那你早点休息呀"。全仓库 grep 的结果是：整套拟真引擎没有任何一处
防复读，只有**主动消息**的提示词里写了"不要重复你最近说过的话"，
普通回复这条线上一句都没有。

## 二、做法：从已有历史里现算，不新增存储

新增 `packages/core/src/llm/AntiRepeat.ts`（纯函数）：

| 步骤 | 规则 | 为什么 |
| --- | --- | --- |
| 合并气泡 | 连续的 assistant 消息算**同一轮**，用换行拼接 | 引擎会把一条回复切成好几条气泡；换行在切句时就是句子边界 |
| 取首尾句 | 每轮只取**开头一句**与**结尾一句** | 复读几乎都发生在这两个位置（套公式的接话、"早点休息呀"式收尾） |
| 过滤 | 少于 5 字的短语、`[贴图]`/`[语音 N 秒]` 占位一律不列 | "嗯""在的"本来就是口头禅，禁掉反而不像人 |
| 去重 | 字符 bigram 相似度 ≥ 0.75 只留一条 | 跨轮换标点的复读也要认出来 |
| 限量 | 最近 3 轮、最多 6 句、单句 40 字截断 | 这段是提示词，不是作文题目 |

渲染出的段落接在**动态段的最后**（世界里设定 → 记忆 → 剧情 → 别重复）——
约束类内容贴着当前提问最容易被遵守，结尾还特意给了替代动作
（"接着说新的话"）而不只是禁令，否则模型容易僵住或者干脆换话题：

```
【别重复】下面这些是你最近几轮已经说过的话：
- 是嘛，辛苦啦
- 晚上早点休息，别熬夜
- 那你早点休息呀
- 刚洗完澡，准备睡了
别再说同样的句子，也别换个说法把同一个意思再讲一遍；接着说新的话，或者顺着对方最新那句往下聊。
```

产品链路上**不做拦截**：把已经生成的回复判为复读再重写要再花一次 API，
还可能把自然的呼应掐掉。`findRepeatedSentences` 只用于探针量化。
「上下文占用」面板新增一行「防复读」，这段花掉多少字符一眼可见。

**顺手收口：主动消息也吃这份清单。** 主动消息的提示词里本来只有一句空泛的
"不要重复你最近说过的话"——模型并不知道"最近"说了什么，于是每轮还是同一句
问候。现在把 `collectRecentSaid` 的结果直接列进提示词（有清单就用清单，
没有才退回旧措辞），App 的主动消息分支一并接线。

## 三、验证

```bash
pnpm test           # 824 个测试全绿（72 个文件，本轮新增 AntiRepeat 16 条 + 各处补 5 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 516.31 KB / gzip 174.11 KB）
pnpm test:coverage  # 语句覆盖率 95.54%（AntiRepeat.ts 100%）
```

**浏览器端到端（假 LLM 服务实收请求）**：种入 7 条消息（含两条气泡同属一轮的
"是嘛，辛苦啦"+"晚上早点休息，别熬夜"），在输入框发"在吗"，服务端打印出的
请求体里确有上面那段清单。

顺带修掉一个端到端才暴露的问题：气泡直接首尾相接会拼出
**"辛苦啦晚上早点休息"** 这种连体句（前一截正好切在逗号后、没有句末标点），
改成按换行拼接后，实测清单变成 4 条独立的句子。

## 四、真机 A/B：**结果是负的，照实记**

本轮执行时 agnes 的 chat 接口整体超时（`/v1/models` 200 正常、
`/v1/chat/completions` 连"说一个字"都 30s 超时，两个模型都试过），
所以当时只留下"离线 + 端到端已验证"，真机数字挂账。

**后来端点恢复（HTTP 200 / 13.8 秒）立刻补跑**，`work/agnes_repeat_probe.mts`
（6 轮低信息量闲聊，A 组把【别重复】那条 system 摘掉，逐轮统计"有没有句子
跟之前任何一轮相似度 ≥ 0.75"）：

```
A 组（不注入）：复读轮次 0/3（共 4 轮有回复）｜平均字数 13.0
B 组（注入）  ：复读轮次 0/3（共 4 轮有回复）｜平均字数 7.5
  第 2 轮 ✅ 新内容｜嗯（本轮有【别重复】）
  第 4 轮 ✅ 新内容｜【引用：嗯嗯】今天过得怎么样？（本轮有【别重复】）
  第 6 轮 ✅ 新内容｜在的，怎么啦？（本轮有【别重复】）
```

**结论：这次真机一次复读都没出现，因此无法证明【别重复】有效**——
不能说"复读率下降"，只能说"这 6 轮里没复现"。两点补充观察：

1. 空回复偏多（A 组 2/6、B 组 2/6，都是只回了情绪标记甚至空串）——
   端点刚恢复，质量与延迟都还不稳；
2. B 组回复明显更短（7.5 字 vs 13.0 字）。样本太小，不排除是提示词里
   那张清单让模型变谨慎，也可能只是噪声——**不作为结论**。

这条改动保留（它零成本、有单测与端到端覆盖、且清单本身能让模型知道
"最近说过什么"），但**不宣称它解决了复读**；真要用数据说话，得在更长、
更容易套公式的语境里再多跑几轮。

---

# 第八十八轮：让角色知道"现在几点、上一句隔了多久"

## 一、缺口（两处，都有证据）

**① 时间从没进过 Prompt。** 整套拟真引擎里时间只被用来**判定**（该不该睡、
忙不忙、延迟多久回），却从没告诉过模型"现在几点"。真机 A 组（改前）问
"现在几点了？"，两轮有回复的答案是：

```
抱歉，我这边没有实时时间功能，你看一下手机或者电脑上的时间吧。
不好意思呀，我这边看不到时间呢。
```

另一次干脆编了个"三点多了"。而晚上八点角色说"早上好"、隔了三天没聊还像
上一句刚说完，根子都是同一件事：**模型手里没有任何时间参照**。

**② 角色卡的 `schedule.timezone` 写进去从没被读过。** 全仓库 grep 只有"写入"
（默认值、编辑器、社区卡导入）与测试里的字面量，没有一处读取——作息判定
一律用宿主机器（也就是你）的钟点。跨时区角色会按**你的**钟点睡觉。

## 二、做法：先把"现在"做成一等公民，再接到各处

新增 `packages/core/src/time/Clock.ts`（纯函数，全仓库唯一的时间口径）：

| 函数 | 作用 |
| --- | --- |
| `wallClockInZone / minutesOfDayInZone` | 某时刻在指定时区的墙上时间（时区非法就退回本地，绝不抛错） |
| `nextTimeOfDayInZone` | 该时区里"下一个 HH:MM"的真实时间戳（跨午夜自动落到明天） |
| `formatDuration / describeElapsed` | "3 小时" / "3 天前"这类人话 |
| `renderTimeContext` | 渲染出下面这两段 system 注入 |

注入长这样（`【上一句】`只在间隔 ≥ 1 小时时出现，一小时内的来回属于连续对话）：

```
【现在】2026年9月13日 星期日 20:43。
这是你手机上的时间：按它说话（该道晚安就道晚安，别把晚上说成早上）；对方问时间就照实答，不用客气地反问。
【上一句】你们上一次说话是 3 天前（9月10日 20:45）。
这段空档要当回事：开口时自然而然地带上它——提一句隔了多久、说说你这几天在忙什么，
或者问一句对方这段时间怎么样；别装作刚刚才聊过，也不用郑重道歉。
```

接线：

- `PromptBuilder`：`extras.now` / `extras.lastMessageAt`，`【现在】` 属于"此刻的外部
  事实"，跟在场状态一起放**开头 system**（`【上一句】`紧跟其后）；
- `RealismEngine.setLastMessageAt()`：引擎拿不到消息时间戳（历史是裸的
  `ILLMMessage`），所以由 App 在每次发送前同步过来；
- `App.syncEngineContext`：从消息列表里取最大时间戳——**调用点都在本条消息上屏之前**，
  于是取到的正好是"上一条"；
- `PresenceManager`：睡眠 / 忙碌判定与 `msUntilWake` 全改走角色时区；
- `ProactivePrompt`：`describeTimeOfDay` 支持时区，原来的 `describeElapsed` 改名
  `describeChatGap` 并复用 Clock 的 `formatDuration`——同名两份实现迟早会对同一
  时刻给出不同答案，现在只有一份。

## 三、验证

```bash
pnpm test           # 803 个测试全绿（71 个文件，本轮新增 Clock 12 条 + 各处补 8 条）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 515.05 KB / gzip 173.58 KB）
pnpm test:coverage  # 语句覆盖率 95.45%（Clock.ts 100%）
```

**真机 A/B（`work/agnes_clock_probe.mts`，agnes-2.5-flash，固定"现在"=9/14 01:20 上海，
上一句=三天前；A 组把时间段从 system 里摘掉，还原改前的 Prompt）**：

```
A 组：问"现在几点"答对 0/2（"我没有实时时间功能"、"看不到时间"、还有一次编成"三点多"）
B 组：问"现在几点"答对 3/3（"凌晨一点二十了"、"凌晨一点半"、"凌晨一点多了"）
A 组：问"上次什么时候聊的"答对 0/2（"嗯..."、"刚呀，你不就在问我这周末看展的事嘛"）
B 组：问"上次什么时候聊的"答对 1/1（"嗯，三天前，9月11号"——注入的就是 9月11日 01:20，答得一模一样）
```

> 两点如实记录：① 这轮 agnes 端点偶发**空回复**（只回了情绪标记甚至空串）
> 与一次 90s 超时，空回复不计入分母，分母因此是 2 或 3 而不是 4；
> ② 原先这里写着"日期有一天偏差"——**那是记错了**：事后用 `renderTimeContext`
> 按探针里的同一个时间戳复算，注入的原文正是
> `【上一句】你们上一次说话是 3 天前（9月11日 01:20）。`，
> 模型答的"三天前，9月11号"完全正确。错误记录留痕在此，避免以后照着错的结论改代码。

**浏览器端到端（假 LLM 服务实收请求）**：种入"三天前的三条消息"+ 指向
`127.0.0.1:5399` 的 API 配置，在输入框发"在吗"，服务端打印出的 system 提示词里
真的有：

```
【现在】2026年9月13日 星期日 20:43。
【上一句】你们上一次说话是 2 天前（9月10日 20:45）。
```

——`App → syncEngineContext → engine.setLastMessageAt → buildPrompt → HTTP 请求`
这条线是通的（时间戳取自种子消息，不是 `Date.now()` 现算的）。

## 四、时区：从"写了没用"变成真的算

单测把"同一时刻、两个时区"摆在一起（显式时区 + 固定 UTC 时刻，换机器结果不变）：

- 上海 23:00–07:00 的作息，同一时刻上海在睡、UTC 醒着；
- 纽约 09:00–18:00 的忙碌时段，同一时刻纽约"在上班"、上海不是；
- UTC 凌晨 2 点睡、07:30 醒 → `msUntilWake` 正好 5.5 小时；
- 时区写坏（`Mars/Olympus`）退回宿主本地时间，不抛错。

---

# 第八十七轮：搜索框里敲两个词，不再等于"查无此事"

## 一、缺口（有截图级的证据）

跨会话消息搜索把**整个查询串**当成一个字面量子串去匹配，于是同一个词库：

- 搜「医院」→ 3 条；
- 搜「团团 医院」→ **0 条**，界面老老实实提示"没有找到包含「团团 医院」的消息"。

两条记录明明都在，用户看到的是"记录里没有"——多出来的那个空格被算进了关键词。
搜索框的通行预期是**拆词 + 全部命中（AND）**，这里对不上。

## 二、做法

- `messageSearch.ts`
  - 新增 `splitSearchTerms`：按空白拆词、统一小写；
  - 新增 `findMatchSpans`：找出片段里**所有**词的命中区间，重叠/相邻的合并成一段
    （避免画出嵌套的 `<mark>`）；
  - 命中判定改为"每个词都出现"；片段围绕**最早出现的那个词**居中，
    保证至少有一处命中落在可见范围里；
  - 命中信息从 `matchIndex / matchLength` 一对，换成 `matches: {index,length}[]`
    一组——一条消息里命中几处就高亮几处。
- `MessageSearchPanel.tsx`：`renderSnippet` 按区间逐段拼 `<mark>`，不再只画第一处。
- `buildSnippet` 的对外契约没动（截取逻辑与"没命中时给开头预览"的行为保持一致）。

## 三、验证

```bash
pnpm test           # 782 个测试全绿（70 个文件，本轮新增 1 个文件）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 512.80 KB / gzip 172.43 KB）
pnpm test:coverage  # 语句覆盖率 95.3%
```

浏览器实测（1280×900，索引库里种入两条会话：林笑笑 4 条、苏晚晴 1 条）：

- 输入「团团 医院」→ 结果 **1 条**（苏晚晴那条含"医院"但不含"团团"，被 AND 正确排除），
  片段里「团团」「医院」**两处都高亮**；
- 输入「医院」→ **3 条**，每条一处高亮；
- 点结果 → 切到林笑笑会话，目标消息**整行高亮**（`.wechat-msg-row--highlight`）。

## 四、顺手补的测试

`messageSearch.test.ts` 更新 1 条 + 新增 3 条：多词 AND 命中、一条消息里两处高亮、
相邻区间合并；新增 `MessageSearchPanel.test.tsx` 4 条（空查询不渲染结果区、
多词两处 `<mark>`、空状态文案、点击回传会话与消息 ID）——
这是消息搜索面板的第一个 dedicated 组件测试，界面与检索逻辑从此各自有回归网。

---

# 第八十六轮：会话列表里就能看出"现在别打扰"

## 一、缺口

第七十一轮给角色加了忙碌时段（`away`）与"已就寝"，但状态**只显示在会话标题栏**——
也就是说：只有点进那个会话才知道对方在忙/在睡。而列表恰恰是"决定点谁"的地方：
满屏会话时，你得逐个点开才知道哪个正在上课。

## 二、做法：状态由作息推导，不进引擎、不加接口

- 列表里给每个会话算一次在场状态，用的是**同一个纯函数**
  （`PresenceManager.evaluate`），不新增任何存储字段、也不依赖引擎是否已创建；
- **只显示非在线状态**：满屏"在线"是噪声，而"在上课""已就寝"正是那条决策信息；
- 名字后面跟一个小胶囊（忙碌用警示色、就寝用灰色），鼠标悬停显示完整文案；
- 每分钟重算一次：它是"当前时刻"的函数，不刷新的话列表会一直停在你打开页面那一刻。

## 三、验证

```bash
pnpm test           # 775 个测试全绿（69 个文件，本轮新增 1 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 512.17 KB / gzip 172.17 KB）
pnpm test:coverage  # 语句覆盖率 95.3%
```

单测用**固定时钟**（本地 10:00）+ 一段 09:00–12:00 的忙碌时段，验证：
只有那个忙着的角色带标签（`data-presence="away"`、文案"在上课"），
在线的角色一个标签都没有。

浏览器实测：给林笑笑配一段覆盖当前时间的忙碌时段并保存 →
聊天页列表显示「林笑笑 **在上课**」（警示色），苏晚晴（在线）无标签；
标题栏也同步显示"在上课"。

---

# 第八十五轮：把剩下的真机结论也过一遍（顺带记一个"疑似角色错认"）

## 一、本轮是复核轮，不新增功能

上一轮复核了引用与心情，这轮把**剩下四条**用完的端点跑完。
目的很清楚：分清楚"哪些结论经得起复跑、哪些只是当天运气"。

## 二、四条复核结果

### 1）真实引擎端到端（`engine_live_probe`）：机制正常

三种语气的用户发言各跑一轮，检查交付气泡的情绪与正文：

```
[我今天升职了！]     → 2 条气泡，情绪 happy，正文无标签残留
[我养的猫昨天走了…]  → 2 条气泡，情绪 sad
[周末去哪玩好呢]     → 2 条气泡，情绪 neutral
```

3/3 情绪正确、0 标签泄漏 ✓ 与当初一致。

**但顺带记一个观察**：其中一轮角色回了"恭喜你呀**晚晴姐**！"——
晚晴是**它自己**的名字。于是专门写了个探针追这个风险
（`work/agnes_role_confusion_probe.mts`：四种典型发言 × 两轮，全部是全新单轮对话）：

```
角色把自己的名字用在对方身上：0/7
```

**没复现**。合计 1/10 左右，判定为一次性波动。
按本轮的纪律——**没有证据就不加代码**——那条"守卫句"（在提示词里写明
"不要用自己的名字称呼对方"）**没有加**，只作为观察项记在这里；
真机上再撞到两次以上再回头处理。顺带说明：填了「关于你」之后，
对方有了明确称呼，这个歧义本来就少了一半。

### 2）世界书注入（`agnes_lore_probe`）：完全复现，且暴露"没有就会编"

| 组 | 答对 |
|---|---|
| A 无世界书 | **0/2**（"五十年代的爵士""1963 年迪克西爵士乐队"——编的） |
| B 有世界书 | **2/2**（"Beatles 的《White Album》，1968 年出的"，还带上了卡里的规矩"只让在店里听"） |

### 3）主动消息（`agnes_proactive_probe`）：复现，剧情卡那组尤其明显

```
场景1 线索在最近对话里：A 1/3 → B 2/3
场景2 线索只在剧情卡里：A 0/3 → B 2/3
     A 组的回复是"忙完记得休息""楼下开了家新面馆"（空泛）
     B 组："简历改得怎么样了？周三面试别太赶。"（精准接上卡里的线索）
```

### 4）AI 生成角色（`agnes_character_gen_probe`）：3/3

三份描述全部解析成功、字段齐全（5/6、6/6、5/6）、有具体细节、**0 处空泛套话**，
名字也各有性格（林夏栀 / 晚星 / 柳针）。

## 三、这张账现在长什么样

| 结论 | 首测 | 复跑 |
|---|---|---|
| 引用回复的价值 | 0/4 → 4/4 | **完全一致** |
| 世界书注入 | 0/2 → 2/2 | **完全一致** |
| 主动消息（剧情卡线索） | 0/3 → 2/3 | **完全一致** |
| AI 生成角色 | 3/3 解析、无套话 | **完全一致** |
| 真实引擎情绪/标签 | 3/3、无泄漏 | **完全一致**（另有 1 次角色错认未复现） |
| 心情注入 | 3/4 → 0/4 出戏 | 方向一致（1/4 → 0/4），**基线会漂** |

一句话：**除"心情"那条要看方向而不是绝对数字外，其余都能稳定复跑**。

## 四、验证

```bash
pnpm test           # 774 个测试全绿（69 个文件）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 511.57 KB / gzip 171.95 KB）
pnpm test:coverage  # 语句覆盖率 95.3%
```

（本轮只新增了探针脚本与文档，产品代码零改动——所以上面这些数字与上一轮一致
是预期的。）

---

# 第八十四轮：复核两条早期真机结论 + 记忆「检索预览」

## 一、为什么先复核

前面十几轮的真机结论大多是**一次 A/B、四轮样本**得出的。端点刚恢复，
趁能用先把两条最关键的复核一遍——如果其中一条站不住，后面所有相关改动都得重估。

### 1）引用回复的价值：**完全复现**

场景不变（关键信息"猫叫团团"已被 `trimContext` 裁出窗口，问「它叫什么名字来着？」）：

| 组 | 答对 |
|---|---|
| A 不带引用 | **0/4**（"什么名字呀""哪本呀""我还不知道呢"） |
| B 带引用 | **4/4**（"团团呀，刚告诉你的""团团"） |

与当初的 0/4 → 4/4 一模一样。顺带看到 B 组有一轮主动用了第六十九轮做的
**角色侧引用**格式（`【引用：我家的猫叫团团】叫团团呀。`）——两条链路同时生效。

### 2）心情注入：方向复现，**但基线会漂**

场景：刚闹完别扭 + 一句轻松的邀请（"明天有部新电影，要不要一起去看"）。

| 组 | 爽快答应（出戏） |
|---|---|
| A 不注入 | **1/4**（当初是 3/4） |
| B 注入 | **0/4**（"……明天不太方便。"） |

正向对照：两边都热情（A 3/4、B 4/4），**0 冷淡、0 负向误判**，没有矫枉过正。

如实记：**注入这条处理稳定有效**（两次都做到 0/4 出戏），
但**基线本身会漂**——同一句邀请、同一段历史，模型当天可能本来就比较冷淡。
所以"3/4 → 0/4"这种绝对数字不该当成硬指标，能站住的是"不注入时偶尔会出戏、
注入后不再出戏"这个方向。

## 二、这轮的功能：记忆「检索预览」

缺口：前面几轮把检索做成了 IDF 加权 + 相关性门槛（第七十五轮），
但用户只能看到"记忆库里有这些"，看不到**这句话会带出哪几条**——
角色没记住时只能猜是没提炼出来、还是没检索到。

- 记忆面板加一个可折叠的「试试这句话会带出哪几条记忆」；
- 它调用的就是**真实注入那条链路**（`retrieveMemories` + 同一个注入条数上限），
  所以看到的就是这一轮真会发出去的东西；
- 每条列出内容 + **命中了哪些关键词**（只说命中什么，不显示打分），
  一句话都没命中时明确写"不会带出任何记忆"；底部注明注入上限与置顶语义。

## 三、验证

```bash
pnpm test           # 774 个测试全绿（69 个文件，本轮新增 2 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 511.57 KB / gzip 171.95 KB）
pnpm test:coverage  # 语句覆盖率 95.3%
```

浏览器实测（面板里手动加两条记忆）：

```
输入「团团最近怎么样」→ 用户养了一只叫团团的猫 ｜ 命中：团团
                        （另一条"用户在设计院工作"不出现）
输入「今天天气不错」   → 这句话不会带出任何记忆（没有命中够区分度的关键词）
```

---

# 第八十三轮：端点恢复，把欠下的真机验证补掉 + 最终提示词预览

## 一、agnes 恢复了（第四轮试探终于通了）

`apihub.agnes-ai.com` 这次 599ms 返回 200（前三轮都是 12 秒超时）。先把欠的验证补上。

### 1）"关于你"真机 A/B（第七十九轮欠的）

同一段寒暄、同一句提问「你还记得我是谁吗？我平时都干什么来着」：

| 组 | 说出名字 | 提到背景 |
|---|---|---|
| A 不填「关于你」 | **0/3** | **0/3** |
| B 填了（小满 / 25 岁程序员 / 养猫） | **2/2**（另有一次空响应，不计） | **2/2** |

A 组的回复很说明问题：一次说"不太记得了呢"，另外两次**开始编**——
"你是我最喜欢的弟弟呢""平时好像挺忙的"。
B 组则准确复述："记得呀，小满，25岁程序员，养了只猫叫团团"。

**结论**：这块稳定背景确实有效，而且它挡掉的是"没有信息就编一个"这种更糟的失败。

### 2）整理容错与质量的稳定性复核（第七十三轮结论）

同一份语料重跑：

```
语料 A：召回 6/6，两条 supersede 都命中，解析正常
语料 B：严格清单 4/6——但"约定/失约/下不为例/搬家"都在事件时间线里，
        两条"漏抽"其实是**清单关键词没覆盖**（卡里写的是"赔罪"而不是"道歉/礼物"）
两次调用：0 次解析失败（改造前 6 次里坏 3 次）
```

结论与第七十三轮一致：容错与提示词调整是有效的，且这次没有再撞到坏输出。

## 二、本轮功能：角色编辑器里的"最终提示词预览"

缺口：模板字段是给你填的，但**拼出来到底长什么样**此前只能靠想象——
人设不生效时完全没法定位（是模板没绑？字段为空？还是被后面的段覆盖了？）。

- core 新增 `buildSystemPromptPreview(profile, template?, userProfile?)`：
  拼出"每轮都会发出的稳定前缀"（角色卡 + 人设模板 + 关于你 + 回复风格 +
  情绪标记 + 引用规则）。睡眠/忙碌/心情、记忆、剧情属于动态内容，不在预览里。
- 角色编辑器「角色设定」下面加一个开关：点开就能看到拼装结果，
  用的是表单当前值（**没保存也能看**），并且与保存时走的是同一个模板构造函数。
- 一条**一致性测试**盯着它：`buildPrompt(...).messages[0].content` 必须以预览内容开头——
  免得哪天改了拼装顺序、预览与真实请求各说各话。

## 三、验证

```bash
pnpm test           # 772 个测试全绿（69 个文件，本轮新增 5 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 510.35 KB / gzip 171.50 KB）
pnpm test:coverage  # 语句覆盖率 95.3%
```

浏览器实测：设置里填「小满 / 25 岁，程序员，养了一只猫」→ 通讯录里编辑苏晚晴
→ 点「预览最终提示词」→ 面板里出现 685 字的拼装结果，开头是
`你是苏晚晴。温润如水的邻家姐姐…`，其中确实带着
`【关于对方】- 对方希望被称呼为「小满」。- 背景：25 岁，程序员，养了一只猫`。

---

# 第八十二轮：会话列表里能看出"谁正在回你"

## 一、先否掉一个候选

原本想做"导入角色卡时的字段映射提示"，先读了一遍映射代码——
`system_prompt` / `post_history_instructions` **早就**会进模板的「额外补充」，
`creator_notes` / `tags` / `creator` 是**故意**不收的人类元数据，
导入提示也已经写明「社区卡 V2，含 N 条世界设定」。
也就是说那条候选已经被覆盖了，不值得再做。（先看证据再动手，省一轮。）

## 二、真正的缺口

上一轮做未读分隔线时把"切走后角色还在回"这条路径走了一遍，
发现列表里看不出**它还在回**：切到别的会话/回到列表时，
那一行只显示上一条消息预览，"正在输入"只在聊天页里能看到。

## 三、做法

- `sessionStore` 新增 `typingBySession`（**不持久化**：刷新后引擎本来就没了）
  与 `setSessionTyping`；状态没变时直接返回 `{}`，不留一堆 `false`、也不白触发重渲。
- App 里两处接线：
  - **后台订阅器**：除了 chunk-delivered / recall，再接 `typing-indicator`；
    并在 `lifecycle` 非 started 阶段兜底清掉（中途出错时不会再收到 typing=false）；
  - **活跃会话**：把 chatStore 的输入状态镜像到同一份数据里，
    这样列表只有一处数据来源，不用同时读两个 store。
- 会话列表：该会话"正在输入"时，预览那行换成「对方正在输入…」（蓝色区分）。

## 四、验证

```bash
pnpm test           # 767 个测试全绿（69 个文件，本轮新增 3 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 509.01 KB / gzip 170.98 KB）
pnpm test:coverage  # 语句覆盖率 95.25%
```

单测锁定：置位/清位（清位删键）、状态没变时不换引用、列表渲染与恢复。

浏览器实测（本地假服务，首块延迟 3 秒）：

```
在苏晚晴发一句 → 立刻切到林笑笑（那一轮还在后台跑）
  → 列表：苏晚晴 | 对方正在输入…
约 6 秒后回复落地
  → 列表：4 ｜ 苏晚晴 | 我是有点在意。（"正在输入"消失，未读徽标出现）
```

---

# 第八十一轮：未读分隔线，以及顺手撞到的一个会话预览串号

## 一、agnes 还是不通（第三轮）

先探了一眼端点：`apihub.agnes-ai.com` 12 秒超时，而同一台机器
`example.com` 230ms 返回 200——确认是端点侧的问题。于是继续做不需要 API 的项。

## 二、未读分隔线（微信里那条"以下为新消息"）

缺这个细节的后果：离开一段时间回来，角色发了好几条，你只能靠"哪几条没印象"
去猜从哪儿接着看。

做法：

- 未读从 0 变 1 的那一刻记下**第一条未读消息的 ID**
  （`sessionStore.unreadMarkers`）——微信那条线画的是"你离开期间的第一条"，
  不是每来一条都重画；
- 切进会话时由 `consumeUnreadMarker` **取走并清掉**，写进 chatStore 的
  `unreadMarkerMessageId`，消息列表在那条之前画一行红字分隔线；
- 手动"标记已读"（会话列表的菜单）会连标记一起清掉——那意思就是"不打算回头看了"；
- 取走即清，所以切走再切回来**不会再画一次**（真机也验证了这一点）。

## 三、顺手撞到的真 bug：会话预览串号

验证时发现：**切到新会话后，列表里新会话的预览显示的是上一个会话的最后一句**
（截图实锤：刚建好的「林笑笑」预览成了发给苏晚晴的"你在忙吗？"）。

根因在 App 里那个"更新会话预览"的 effect：
它在同一次提交里跟着切会话一起跑，此时 `activeSessionId` 已经是新会话，
而闭包里的 `lastMessage` 还是**上一个会话**的——于是把别人家的最后一句话
写进了新会话的预览。

修法：预览与快照都改成**运行时从 store 现取**
（`useChatStore.getState()`），不用渲染闭包里的那份。

## 四、验证

```bash
pnpm test           # 764 个测试全绿（69 个文件，本轮新增 7 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 508.32 KB / gzip 170.80 KB）
pnpm test:coverage  # 语句覆盖率 95.25%
```

单测锁定：未读 0→1 才记标记、没传消息 ID 时不记（老调用方兼容）、
取走即清、手动标记已读会一并清掉；消息列表侧锁定"画在标记那条之前 / 没标记不画 /
标记的消息不在列表里也不画"。

浏览器实测（本地假服务，把首块延迟调到 3 秒，好有时间切走）：

```
进苏晚晴 → 发「刚才那句你看到了吗」→ 立刻切到林笑笑
  → 回复在后台落进苏晚晴，列表出现未读徽标「2」
切回苏晚晴 → 红字分隔线「以下为新消息」正好画在"我没生气，"这条之前
            （= 离开期间收到的第一条）
再切走再切回 → 分隔线不再出现
```

---

# 第八十轮：状态卡被改了哪儿，一行一行摆出来

## 一、背景

上一轮给"记忆"补了变更账本（新增/取代/冲突），剧情这边还是老样子：
后台整理默默 patch 状态卡，用户盯着卡上的章节/场景/时间/地点/概要/线索/关系，
**根本看不出哪一项被动了**。

顺带说明：本轮原本要补上一轮欠下的真机 A/B（"关于你"），先探了端点——
**agnes 仍然不通**（15 秒超时）而 `example.com` 230ms 就返回 200，
确认是端点本身的问题、不是本地网络。于是继续做不需要 API 的那项。

## 二、做法：不存 diff，直接比快照

关键发现是**不需要新增任何状态**：`applyDigest` 在改动前会把上一版压进
`state.history`（就是"回滚"用的那份快照）。于是：

- 新纯函数 `diffPlotCards(before, after)`：逐字对比五个文本字段
  （章节/场景/时间/地点/概要），线索按原文分「新增线索」「了结线索」，
  关系按 characterId 对齐分「新增关系 / 关系改写 / 移除关系」。
- 参数类型是结构子集 `IPlotCardLike` 而不是 `IPlotSnapshot`：
  调用方手上可能是快照（带 savedAt），也可能是当前状态卡（带 updatedAt），
  两者都有这几个字段。
- 剧情面板在状态卡上方渲染「本次更新（N 分钟前）」，一行一处：
  ```
  场景「美术馆」→「回家路上」
  新增线索「下周三一起吃饭」
  了结线索（已移除）「周六的展还没定下来」
  关系「关系缓和」→「开始有点心动」
  ```
- 没有上一版快照时整块不显示（首次整理之前没什么可对比的）。

## 三、验证

```bash
pnpm test           # 757 个测试全绿（68 个文件，本轮新增 7 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 507.51 KB / gzip 170.59 KB）
pnpm test:coverage  # 语句覆盖率 95.27%
```

单测锁定：文本字段前后值、线索的新增/了结分开、关系的三种变化、
没有变化时返回空数组、从空白到第一版；面板侧锁定"有快照才显示这一块"。

浏览器实测（塞一张"上一版 + 现版"的卡）：剧情 Tab 顶部出现
`本次更新（1 分钟前）`，逐行列出了上面那七处变化（截图核对过）。

---

# 第七十九轮：角色不知道屏幕对面是谁（加"关于你"）

## 一、缺口在哪

聊天用的 system prompt 里**只有角色**——"你是苏晚晴。温润如水的邻家姐姐…"，
外加性格、回复风格、情绪标记、引用格式。**关于对方一个字都没有**：
角色面对的是一个抽象的"用户"，你叫什么、做什么、住在哪，全靠聊出来、
再等后台整理抽成记忆，而且抽不抽得到看运气。

（`userName` 这个参数其实早就存在，但只服务于三处边角：角色卡 `{{user}}`
占位符替换、AI 生成角色、导出记录——而且默认值还不统一，有的写"我"、
有的写"用户"。）

## 二、做法

- 新契约 `IUserProfile { displayName, bio }`（`shared/types`），
  存进 `sessionStore.userProfile`（持久化），设置面板新增「关于你」一节：
  称呼 + 一句话背景。
- `buildPrompt` 新增 `extras.userProfile` → 渲染成 `【关于对方】` 段，
  **跟在角色人设之后**（稳定前缀，每轮都在，不参与检索、不等整理）：
  ```
  【关于对方】
  - 对方希望被称呼为「小满」。
  - 背景：25 岁，程序员，独居，养了一只叫团团的猫
  - 这些是对方自己写的稳定信息，不用每句话都提；平时用「你」称呼就好，
    偶尔叫名字反而更亲近。
  ```
  最后那句叮嘱是刻意的：模型一旦"知道名字"，很容易每条都喊一遍，
  微信里没人这么聊天。
- 没填（称呼仍是"我"、背景为空）时整段省略，不占 Prompt。
- 顺手把导出聊天记录也接上：用户的发言用设置里的称呼署名（没填仍是"我"）。
- **记忆仍统一用"用户"指代**：`buildDigestPrompt` 的 `userName` 保持默认——
  老记忆都写成"用户喜欢…"，突然改叫"小满"会让 bigram 去重认不出同一条，
  反而制造重复。内部指代与对外的称呼分开是可以接受的。

## 三、验证

```bash
pnpm test           # 750 个测试全绿（68 个文件，本轮新增 7 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 506.12 KB / gzip 170.13 KB）
pnpm test:coverage  # 语句覆盖率 95.2%
```

单测锁定：填了就注入、没填整段省略（只填背景也算填）、
引擎侧 `setUserProfile` 真的进了请求、`sessionStore` 局部更新不冲掉别的字段、
导出记录用设置里的称呼署名。

**端到端**（本地假 LLM 服务，不依赖外部 API）：在设置里填「小满 /
25 岁，程序员，独居，养了一只叫团团的猫」→ 回聊天发一句 →
拦下浏览器真实发出的请求体，system 提示词里确实带着：

```
【关于对方】
- 对方希望被称呼为「小满」。
- 背景：25 岁，程序员，独居，养了一只叫团团的猫
```

顺带记一笔环境状况：**本轮 agnes 端点一直没响应**（探针挂了三十多分钟，
手工 ping 也是"连接被关闭"），所以这轮的原计划 A/B（填与不填、
看回复是否叫得出名字）**没跑成**，改用本地假服务把链路验穿。
真机 A/B 留到端点恢复后再补。

---

# 第七十八轮：整理完留一条"这次改了什么"

## 一、为什么做这个（而不是"确认后才落库"）

上一轮把"手动记忆受保护 + 冲突可见"补齐后，剩最后一块信任缺口：
自动整理跑完**只留下结果**，用户看不出"它刚才动了什么"。

我最初想的是"整理前先让用户确认"，想清楚后否掉了：
自动整理每 20 条消息跑一次，每次都弹确认等于给后台常驻功能加一道卡点，
用户很快就会闭眼点掉——**确认疲劳比没有确认更糟**。
改成"跑完留一条可读摘要"：透明度一样有，但零打扰。

## 二、做了什么

- 新契约 `IDigestReport { at, messageCount, added, replaced, conflicts, events }`
  （`shared/types`）：一次整理改了什么的账本。
- `digestRunner` 在整理成功后统计并落库：
  - `added`：合并结果里 id 不在旧库的条数；
  - `replaced`：`旧条数 + 新增 − 合并后条数`（包括被 supersedes 替换的）；
  - `conflicts`：这次新写过的条目里带 `conflictsWith` 的条数
    （判据是 `updatedAt >= 本次整理时间`，所以不会把历史冲突重复计入）；
  - `events`：状态卡事件数的增量；`messageCount`：这次窗口覆盖多少条消息。
- 记忆面板顶上一行：
  `上次整理 2 分钟前 · 覆盖 60 条消息 · 新增 3 条 · 取代 1 条 · 新增事件 2 个`
  有冲突时追加 `⚠️ 1 条与你的手动/置顶记忆冲突`，整行转成警示色。
- 这份账本**持久化**：刷新后仍然看得到"上次整理做了什么"。
  整理本来就是低频动作（每 20 条消息一次），写盘代价可忽略。

## 三、验证

```bash
pnpm test           # 743 个测试全绿（67 个文件，本轮新增 5 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 504.58 KB / gzip 169.45 KB）
pnpm test:coverage  # 语句覆盖率 95.17%
```

单测覆盖：摘要的四个计数各写对（含"取代 1 条"的场景）、
面板渲染摘要、有冲突时转警示样式、没有记录时不占地方。

浏览器实测：往 IndexedDB 里放一条摘要 → 面板显示
`上次整理 2 分钟前 · 覆盖 60 条消息 · 新增 3 条 · 取代 1 条 · 新增事件 2 个 · ⚠️ 1 条与你的手动/置顶记忆冲突`
（警示色），刷新后仍在——确认它确实走了持久化那条路。

---

# 第七十七轮：我手动改过的记忆，被后台整理改回去了

## 一、体检对象：手动维护 vs 自动整理

记忆面板允许用户自己新增、编辑、置顶。问题在于：**后台整理认不认这些改动**？
翻 `mergeMemories` 发现两条路径都不看来源：

1. **相似度合并**会把用户改过的原文替换掉——规则是"谁的句子长用谁的"，
   而自动整理写出来的句子通常更长；
2. **supersedes**（模型说"这条新信息推翻了那条旧的"）会把旧记忆整条删掉，
   代码只跳过 `pinned`，手动改过但没置顶的照删。

也就是说："我刚把'用户喜欢喝咖啡'改成'只在周末喝一杯'，
过几轮它又给我改回去了"——**这是最伤信任的一类失败**。

## 二、改法：来源 + 保护 + 冲突可见

- `IMemory` 增加 `origin?: "auto" | "manual"`（可选，老数据视为自动）。
  记忆面板里**新增或编辑**过的条目都标成 `manual`（编辑自动记忆也算——
  "我改过的，别再自动改回去"）。
- `mergeMemories` 里定义"受保护 = 手动维护或置顶"：
  - 相似度合并**不改写**受保护的条目（撞车时丢弃那条草稿）；
  - supersedes **不删除**受保护的条目。
- 那"用户说的过时了"怎么办？受保护条目被 supersedes 指向时，
  **新信息并存**，并在新条目上记 `conflictsWith: <旧原文>`——
  记忆面板直接提示"与你的手动/置顶记忆冲突：用户对花生过敏"，
  两条都摆出来让用户自己定。（旧行为是默默并存、不说明冲突，只能靠用户自己发现。）
- 面板加「手动」标记与一句说明："你自己新增或改过的记忆会标成手动，
  后台整理不会改写或删除它（想交给自动整理接管，把它删掉即可）"。

顺带修了一个**文档与实现不一致**的地方：文档说置顶记忆"不会被自动删除"，
但相似度合并其实照样能改写它的正文——现在置顶也在保护范围内。

## 三、测试过程中被既有测试纠正了一次

我第一版把"supersedes 指向受保护记忆"处理成**直接丢弃草稿**，
结果撞上了既有测试：它明确写着置顶那条被 supersedes 指向时
"**新信息并存**——交给用户在面板里判断"。

那条约定是对的：丢掉草稿等于**把新事实一起扔了**，比"两条并存"更糟。
于是改成"并存 + 标出冲突"——既尊重既有设计，又把"用户得自己比对"
这个负担降到"看一眼提示就懂"。

## 四、验证

```bash
pnpm test           # 738 个测试全绿（67 个文件，本轮新增 8 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 503.61 KB / gzip 169.09 KB）
pnpm test:coverage  # 语句覆盖率 95.14%
```

浏览器实测（记忆面板）：

```
面板提示：「你自己新增或改过的记忆会标成「手动」，后台整理不会改写或删除它…」
点「新增记忆」写下"用户不吃香菜，闻到就难受" → 列表里出现「其他 ★★★ 手动」标记
```

新增测试锁定：手动记忆不被改写（更长的新版本也改不动它）、
不被 supersedes 删除且新信息带冲突标记、置顶同样不被改写、
自动记忆照旧合并、面板里新增/编辑都会落到 `manual`、以及冲突提示的渲染。

---

# 第七十六轮：状态卡的未解线索要么永远清不掉、要么被覆盖丢掉

## 一、先验证"推进剧情"，结果是个否定结论

`推进剧情` 这条链路一直没真机验证过。我怀疑指令太泛（只说"挑一条未解线索"、
从不点名），于是对比了两版：A 现状、B 把 `openThreads` 原文念出来。

第一轮 3 次的结果是 A 1/3、B 2/3，看起来 B 赢了——**但那是假象**：
两臂各有一次空响应被算进了分母。修正度量（空响应不计入）、
把轮次加到 6 之后：

```
A 现状：5/5 碰到线索    B 点名线索：5/5 碰到线索
```

**打平**。现状的推进指令已经足够好用（模型会自己从状态卡里挑一条接上），
所以那条改动**不做**——又一次"没有证据就不加复杂度"。

## 二、换角度：状态卡谁来维护

顺着"状态卡的字段是怎么更新的"查下去，发现一处结构性 bug：

- `buildDigestPrompt` 只把【已知的长期记忆】发给模型，**从来不发当前状态卡**；
- `applyDigest` 对 `openThreads` 是**整组替换**（`patch.openThreads ?? base.openThreads`）。

两者合起来，模型只能凭"这一段对话"写状态卡，于是必然走向两个坏结局之一：

1. **省略该字段** → 旧值被保留 → 已经了结的线索永远挂着；
2. **只写这一段提到的线索** → 整组替换 → 没提到、但还没了结的线索被静默抹掉。

真机基线复现的是第 1 种：场景设成"两人去看展、展已经看完了、
而另一条线索（要送的摄影集）一个字没提"，2/2 轮模型都返回
`plot.openThreads: null` → 卡片保持原样 → **展都看完了还挂在未解线索里**，
"推进剧情"就会一直拿它做文章。

## 三、修法：整卡维护

把当前状态卡随整理请求一起下发（`buildDigestPrompt` / `runDigest` 新增
`currentPlot`，`digestRunner` 传 `plotStates[sessionId]`），
并在提示词里把语义写死：

> plot 是**整张状态卡的最新版本**：参考【当前状态卡】把这一轮的信息并进去、
> 整体返回——已经了结的未解线索要删掉（例如展览已经看完了），
> 仍未了结的必须原样保留，不要只写这一轮提到的那条。
> 状态卡没有任何变化时可以省略 plot。

提示词里只列**有值**的字段（空字段写出来会被模型理解成"要清空它"）。

## 四、真机复核

同一个场景、同一份语料，改造后连跑 5 轮：

```
#1~#5 全部 ✅ 符合预期｜整理后线索：说好要送的摄影集还没送出去
```

即：**了结的"展"被删掉、没了的"摄影集"保留**——两种坏结局都没再出现。

（顺带说明：这轮探针中途有两次"解析失败"，本地复现那段 JSON 却能正常 parse，
追进去发现是服务端偶发返回了坏的/不完整的输出——产品侧早有重试与抢救解析兜底，
探针里把输出全文打印出来才看清这一点。）

## 五、验证

```bash
pnpm test           # 730 个测试全绿（67 个文件，本轮新增 3 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 502.91 KB / gzip 168.80 KB）
pnpm test:coverage  # 语句覆盖率 95.23%
```

新增测试锁定：状态卡随请求下发（含"没有卡时不出现这一段"）、
提示词里的"整卡维护"规则、以及 digestRunner 真的把当前卡传了出去。

---

# 第七十五轮：记忆注入的一半是噪声（检索加了 IDF 与门槛）

## 一、怎么发现的

上一轮的真机对照指出：长对话里"记得住"的主力是**尾部的记忆注入**
（有注入 3/3，没注入 0/3）。既然它是主力，就该体检它到底注入了什么。

检索是纯函数——不用烧 API 就能量化。于是写了一份**带标准答案**的语料
（`work/memory_retrieval_quality.mts`）：30 条记忆（一半是"团团/杭州/生日/
香菜/摄影展约定"这类真信息，一半是"绿萝/牛奶/地铁"这类日常琐事），
9 句该召回的提问 + 3 句与任何记忆都无关的话。

## 二、基线：召回没问题，噪声惊人

```
召回 9/9
无关提问时注入的非置顶记忆：27 条（三句各注入 9 条）
有关提问时总注入：81 条（9 句 × 9 条，真正相关的只占一两成）
```

两个结构性问题：

1. **没有任何相关性门槛**：得分 = 0.6×命中率 + 0.25×重要度 + 0.15×时间衰减
   （+ 置顶）。命中率为 0 时还剩 0.4 的"重要度 + 新鲜度"底分，
   于是**"在吗"也会把 10 个名额塞满**——每轮白烧 ~700 字，
   还可能把角色带偏（张口就提"你花生过敏"）。
2. **命中率按"记忆自己的关键词数"归一**：关键词写得多的记忆反而吃亏，
   而只有一个泛词命中的记忆却能拿满分。

顺带说清一件事：基线的 9/9 召回**有一半是撞上的**——
不相关的记忆因为"重要 + 新鲜"被塞进来，正好蒙对了答案。

## 三、改法

- **IDF 加权**（`idf = ln(1 + 总数/出现条数)`）：在越多记忆里出现的关键词越不值钱；
  "团团""蝴蝶兰"这种只出现在一条记忆里的词，一命中就几乎锁定答案。
- **饱和式相关度** `min(1, 命中IDF / 3.4)`：命中一条稀有词就算"完全相关"，
  不再因为这条记忆还有几个没命中的关键词而被稀释。
- **相关性门槛**：命中量不到门槛就不注入（置顶条目不受影响，它本来就是常驻语义）。
  门槛还**随语料规模自适应**（`× min(1, N/8)`）——新会话只有两三条记忆时，
  "稀有"无从谈起，用满门槛会把仅有的几条也挡在外面。
- **单字弱匹配**：中文没有词形变化，"那个展"和"摄影展"只共享一个"展"，
  纯 bigram 抓不到。给这种沾边 0.35 的折扣权重，并配一张**泛字停用表**
  （上/下/周/工/吃/睡…）——真机量化里正是这些字带来了
  "晚上点外卖 → 用户报了吉他班（上课）""工作怎么样 → 工位上的绿萝"这类误召。
- **选择时去近重复**：同一件事的两种说法（"养了一只叫团团的猫" /
  "养了一只猫，名字叫团团"）只留排在前面的那条，不浪费名额。

## 四、改之后（同一份语料）

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 召回 | 9/9（一半是撞对的） | **9/9**（且靠词命中） |
| 无关提问注入的非置顶记忆 | 27 条 | **1 条** |
| 有关提问时总注入 | 81 条 | **15 条** |

注入列表也干净了：

```
「团团今天怎么样？还在吃药吗」→ 置顶 + 猫团团（近重复的那条被去掉）
「周六那个展你还去吗」      → 置顶 + 摄影展约定（靠单字弱匹配救回的召回）
「工作最近怎么样」          → 置顶 + 跳槽/产品设计/加班（三条都对题）
「在吗」                    → 只有置顶，一条噪声都没有
```

唯一允许留下的"沾边"是"今天天气不错啊"→"城市最近降温"（确实同话题）。

## 五、验证

```bash
pnpm test           # 727 个测试全绿（67 个文件，本轮新增 5 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 502.24 KB / gzip 168.44 KB）
pnpm test:coverage  # 语句覆盖率 95.19%
```

这套量化已经变成**常驻测试**（`MemoryRetrieverQuality.test.ts`）：
召回、噪声上限、精度上限、近重复只留一条、置顶永在——
以后谁放松了门槛或破坏去重，测试会立刻红。
同时更新了两条**编码了旧语义**的老测试：置顶那条不再要求"陪跑两条无关记忆"，
条数预算用例改用三条不同主题的记忆（原来的三条内容完全一样，会被去重挡掉）。

---

# 第七十四轮：长对话"前情提要"几乎从不触发（以及一个被证据否掉的改法）

## 一、体检对象：上下文裁剪

"角色记得住"这条链路上，还有一个我此前没体检过的环节：
`ContextTrimmer`——历史太长时怎么砍、砍完给模型留什么。

## 二、发现一：前情提要在微信式长对话里**永远不会出现**

旧逻辑是"裁剪后**仍然**超过硬上限（8000 字）才生成前情提要"。
而保留窗口只有最近 8 轮（16 条），**短消息聊天永远凑不到 8000 字**：
于是长对话里旧消息被**静默丢掉、没有任何替代品**，
角色自然"忘了中间那一段"，界面上还显示一切正常。

顺带发现 `hardLimitChars` 形同虚设：它此前只用来"触发摘要"，
而那个摘要并不会让请求变小——真正该做的是**继续砍保留窗口**。

## 三、发现二：旧摘要捡的是"最老的那几条"

旧实现把**每一条**被裁消息取前 30 字拼起来、200 字封顶。
拼接是从最早那条开始的，所以 200 字的额度全被**最早**的几条吃掉了，
内容还是"半句话沙拉"。现在改成：带**紧邻保留窗口的那几条**（模型对这段
最需要接续），每条留长一点，并明确写"（更早的 N 条已省略）"。

## 四、被证据否掉的改法：用提炼结果当前情提要

我先实现的版本是"有长期记忆/剧情就用它们当前情提要"（想法很自然：
那本来就是提炼好的要点）。真机三组对照（`work/agnes_recap_probe.mts`，
关键信息只出现在被裁掉的那段里）：

| 组 | 配置 | 说出"杭州" |
|---|---|---|
| A | 机械摘要 + **尾部记忆注入** | 3/3 |
| B | 要点版摘要 + 尾部记忆注入 | 3/3 |
| C | 机械摘要、**没有**记忆注入 | 0/3（"不记得了，你之前没说"） |

结论很清楚：**长对话里记得住的主力是尾部的记忆注入**（后台整理那条链路），
前情提要换成要点版**测不出任何额外收益**，只是把那 400 字又抄了一遍。
所以这条改动**撤掉了**——不留没有证据支撑的复杂度。

保留下来的是两点站得住的：

1. **只要裁掉了消息就补一段替代品**：模型该知道"更早的 N 条被省略了"，
   而不是一口咬定"你没说过"。C 组的措辞变化正好说明这一点：
   有替代品时它说"记不清了，你翻翻聊天记录"，没有时会说"你之前没说"。
2. **硬上限真的生效**：保留窗口自己太大时继续往前砍（至少留一轮），
   砍掉的部分同样进前情提要。

## 五、三次自己踩的坑（探针也会骗人）

1. 第一版探针的语料只有 3000 字，**根本没触发裁剪**，三组自然都答对——
   等于什么都没测（"前情提要"那一栏全是"（没有）"，这是唯一的破绽）。
2. 第二组对照（旧摘要 vs 新摘要谁更能带回切口那条信息）**设计错了**：
   我把关键信息放在了保留窗口**之内**，于是两组都靠保留窗口答对；
   同一组还连撞三次空响应（agnes 偶发），数据直接作废。
3. 教训写在这里：**先确认实验真的触发了被测路径**（这次的破绽是探针自己
   打印的"（没有前情提要）"），再谈结论。

## 六、验证

```bash
pnpm test           # 722 个测试全绿（66 个文件，本轮净增 4 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
pnpm test:coverage  # 语句覆盖率 95.11%
```

新增测试锁定：短消息长对话**也要**有前情提要（旧行为这里什么都没有）、
兜底摘要带的是切口附近那几条、硬上限真的会把保留窗口继续砍小、
以及 buildPrompt 层面的"长对话被裁剪时给出前情提要 + 记忆照旧注入"。

---

# 第七十三轮：提炼质量的量化（真机 6 次里 3 次坏输出）

## 一、为什么要量化

上一轮留了个观察："相邻但无关的两句被并成一条记忆"。这轮把"提炼质量"
做成可量化的检查：每段语料**预先写好标准答案**（该抽什么、什么不该抽），
真机跑完逐项打分——召回 / 噪声 / 重复 / 串味
（`work/agnes_digest_quality_probe.mts`）。

结果比预想的严重。

## 二、基线（改之前）

| 语料 | 召回 | 其他 |
|---|---|---|
| A 日常（事实/偏好/状态变化） | 4~5 / 6 | 稳定漏掉"下周去杭州出差" |
| B 剧情（约定/冲突/关系） | 0~2 / 6 | 只记住"书要搬"这类琐碎细节，整条"约定→失约→道歉→和好"主线全丢 |

更糟的是**稳定性**：6 次调用里撞到 3 种不同的坏输出，每一种都让整段记忆全丢：

1. **空响应**（0 字）——agnes 偶发如此，聊天链路早就为它写过重试，整理链路没有；
2. **尾随逗号**（`"openThreads":["搬家"],}`）——非法 JSON；
3. **尾巴截断**（memories / events 写完了，到 plot 那里断掉就收了围栏）。

## 三、修了四处

1. **空响应/格式跑偏立刻重试一次**（`digestRunner`）：与聊天链路的
   "空回复重试"对齐；网络/超时类不重试，避免等待翻倍。
2. **容忍尾随逗号**：只在字符串之外动手（正文里的逗号一个都不碰）。
3. **抢救被截断的尾巴**：只保留最后一个结构完整的顶层字段、补上闭合——
   宁可少一个 plot 字段，也不丢 memories/events；结构错乱时不硬猜。
4. **整理请求的输出预算 1024 → 2048**：真机输出能到 1200 字，
   贴着旧上限写，截断就是这么来的。

顺带修掉一个**我自己引入的**问题：第一版"抢救"写完后测试仍失败——
因为 `extractJsonText` 用"首个 { 到最后一个 }"截取，截断场景下
最后一个 } 是内层对象的，等于把 events 整段切掉。改成多候选
（"到最后一个 }"与"到结尾"都解析），**取内容最全的那个**。

## 四、提示词：把"该记什么"说清楚

基线的两个漏抽都不是模型笨，而是提示词把它们排除在外了：
"拒绝临时内容"让"下周出差""妈妈生日"这类**有明确时间的近期安排**被丢掉，
而它们恰恰是角色后面能主动关心的东西。改成：

- 明确列出要记的：姓名/喜好/经历/工作与生活变动、**有明确时间的近期安排**、
  约定与承诺、关系变化；
- "**情感上有分量的事优先于琐碎的生活细节**——吵架、道歉、约定，
  比'对方有很多书要搬'重要得多"；
- "一条记忆只写一件事"，并给了反例（姐姐来住 / 房租涨了必须分开）。

## 五、改之后（同一个探针）

| 语料 | 召回 | 变化 |
|---|---|---|
| A 日常 | **6 / 6** | 杭州出差、妈妈生日与养花都抽到了；两条 supersede（咖啡→白茶、设计院→互联网）全部命中 |
| B 剧情 | **5 / 6** | 主线回到时间线：「苏晚晴因加班爽约摄影展且未提前通知，引发争执后双方和解」；只漏"下不为例" |

## 六、两条自我纠正（工具也会骗人）

- 第一版评估**只看 memories**，于是把"约定/冲突"判成漏抽——可按设计
  它们该进 `events` 时间线。探针改成两条通道合起来算召回，
  并把"由事件承担"的项单独标出来。
- 探针给 B 语料的噪声清单里写了"咖啡"，可语料里那句是**纠正**
  （用户不喝咖啡），属于该记的信息。清单写错了，评估就会冤枉模型。

另外，B 里那条"串味"标记是我的启发式误报：一条**冲突摘要**天然会同时提到
"失约"和"不满"，那是叙事完整而不是把两件事并成一件。启发式只能当烟雾报警器，
最终还得人看一眼。

## 七、验证

```bash
pnpm test           # 718 个测试全绿（66 个文件，本轮新增 6 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
pnpm test:coverage  # 语句覆盖率 95.09%（MemoryDigest.ts 97.6%）
```

---

# 第七十二轮：后台整理会"悄悄吞掉"中间那段消息（真 bug）

## 一、怎么发现的

这轮本打算看"整理质量"，结果先读到了编排逻辑里的一处硬伤
（`apps/web/src/digestRunner.ts`）：

```ts
const windowMessages = messages.slice(-config.maxWindow);  // 永远取最后 60 条
...
latest.setDigestCursor(sessionId, messages.length);          // 游标却跳到末尾
```

未整理的消息**一旦超过窗口**（阈值 20、窗口 60），中间那批就永远不会被整理，
而游标却声称它们整理过了——界面显示"待整理 0 条"，内容悄悄丢掉。

什么时候会撞上：**导入备份**（一次进来几百条消息）、关掉自动整理很久再打开、
或者手动点"立即整理"一大段。

## 二、修法

窗口锚在"还没整理的那一段"的开头，而不是永远盯住末尾：

```ts
const undigestedStart = Math.max(0, Math.min(cursor, messages.length));
// 前面多带 4 条已整理的消息当上下文：模型要看得懂"他/那个"指谁
const windowStart = Math.max(0, undigestedStart - CONTEXT_OVERLAP_MESSAGES);
const windowEnd = Math.min(messages.length, undigestedStart + config.maxWindow);
```

游标按**这次真正吃掉的段**推进：积压 100 条就是两次整理（60 + 40），
一条都不跳过。App 里那个 effect 依赖 `digestCursor`，
所以第二批会在第一次完成后自动接着跑，用户看到的"待整理 N 条"会一路降到 0。

"窗口里全是系统/撤回消息"那条早退分支也一并修了：它原来同样把游标推到
`messages.length`，积压时一样会吞掉后面的内容。

## 三、验证

**回归测试先证明它抓得到**：临时把窗口逻辑还原成旧写法跑一遍——
2 个用例失败（积压分批、系统消息早退），改回来即全绿。

```bash
pnpm test           # 712 个测试全绿（66 个文件，本轮新增 11 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 499.37 KB / gzip 166.82 KB）
pnpm test:coverage  # 语句覆盖率 95.11%（digestRunner.ts 100%）
```

新增的 `apps/web/src/__tests__/digestRunner.test.ts` 覆盖：触发阈值、
积压分批（回归）、系统消息早退、Mock 跳过、坏 JSON 冷却、
适配器抛错、**落库失败**、**页面卸载取消在途整理**。
顺便把 `digestRunner.ts` 纳入了覆盖率统计——它此前是覆盖率盲区，
却是组装层里唯一有真逻辑的文件。

顺带补了一个小口子：`resetDigestRunnerState()` 在"重建适配器"时调用。
用户改完 API Key 点保存，此前失败的会话不该继续被 3 分钟冷却挡着。

## 四、真机复核 60 条窗口

窗口语义变了，输入也可能真的装满 60 条，于是用真机跑了一遍
（`work/agnes_digest_window_probe.mts`）：60 条对话、user 段 928 字符，
**7 秒返回、解析成功**，提炼出 8 条记忆（fact/preference/event/promise 分类正确、
重要度 2~4 合理）+ 2 个剧情事件 + 一条剧情概要。

留一个观察（不是这轮要修的）：合成语料里两条相邻但无关的信息
被模型并成了一条（"用户下个月要带母亲去杭州出差三天"）——
真实对话里是否常见、要不要在提示词里加一条"一句话只写一件事"，
留到下次专门验证提炼质量时再看。

---

# 第七十一轮：忙碌时段（把 `away` 这个死状态点亮）

## 一、体检续：又一个"定义了但没人用"的状态

上一轮顺手记下的：`CharacterPresence` 里有 `"away"` / `"offline"`，
界面也留了 `data-presence` 的位置，但**没有任何地方会产生它们**。
背后缺的其实是一个日常需求：角色**上课 / 上班**的时候是什么状态？

这一轮补上 `busyPeriods`（可配多段、可跨午夜、带文案如"在上课"）。

## 二、设计取舍

- **睡眠 > 忙碌 > 在线**：两者重叠时按睡着算。人睡着了就是睡着了，
  不会"边睡边上课"；作息感知总开关关掉时两者都不判定。
- **忙碌不拦回复**：睡着时角色看不到消息（那套策略管"要不要回、回多短"），
  忙着时看得到、只是没空细说——所以忙碌只改 Prompt 与状态显示，
  不动回复流程，也不加人为延迟（加延迟只会让人以为界面卡了）。
- **顺手改名**：`ISleepReplyDecision` → `IPresenceDecision`。
  它早就同时在给睡眠/在线两种结论了，加进忙碌后继续叫"睡眠决策"就是误导。
- **状态要看得见**：`presenceText` 此前只渲染在 `display: none` 的头里，
  等于用户根本看不到"已就寝"。现在主标题下面挂一行小字
  （在线 / 在上课 / 已就寝），忙碌用警示色。
- **补一个刷新节奏**：引擎只在"开始一轮回复"时推送 presence，
  于是刚打开会话时头部一直显示"在线"——哪怕角色此刻正在上课。
  App 里加了个 30 秒的定时同步（回复进行中不插手，那时以引擎事件为准），
  角色卡改动作息也会立刻反映出来。

## 三、真机验证（A/B）

场景：用户抛一个值得多聊两句的话题（面试被问懵了想吐槽），
A 组在线、B 组在上课，各 4 轮（`work/agnes_busy_probe.mts`）：

| 指标 | A 在线 | B 在上课 |
|---|---|---|
| 平均字数 | 19 字 | 16 字 |
| 主动提"待会儿 / 下课 / 忙完" | **0/4** | **2/4** |

典型样本（B 组）："等下课了再聊好吗，我现在还在上课。"、
"嗯，被问懵了确实会难受一阵子……我课上的完了找你？"

**如实记录**：长度只从 19 降到 16 字——因为基线本来就短（角色卡的回复风格
默认就是"简短"），指望它腰斩不现实。真正变明显的是**措辞里带上了情境**：
不忙时它只会安慰，忙的时候它会先承认"我在上课"再约时间。这才是这轮的价值。

## 四、浏览器端到端

```
角色编辑器：加一段覆盖当前时间的忙碌时段（17:00–19:00，文案"在上课"）→ 保存
切到该角色的会话 → 头部立刻显示「林笑笑 / 在上课」（警示色、data-presence=away）
                （改造前这里恒为"在线"，且要等到第一条消息才会变）
忙碌中发一条消息 → 照常收到回复（忙碌不拦回复，符合设计）
刷新 → 头部仍是「在上课」（状态由角色档案推导，不依赖内存）
```

途中踩到一个自己的验证坑：通讯录里点的是列表**第一个**"编辑"（林笑笑），
却跑到苏晚晴的会话里看状态——自然看不到。功能本身没问题，
是验证脚本认错了对象；记一笔提醒自己：**验证前先确认改的是哪条数据**。

## 五、验证

```bash
pnpm test           # 701 个测试全绿（65 个文件，本轮新增 16 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 499.25 KB / gzip 166.78 KB）
pnpm test:coverage  # 语句覆盖率 94.66%
```

---

# 第七十轮：心情（角色不能刚吵完架就乐呵呵）

## 一、体检发现的断链

顺着"哪里还有半截链"再扫一遍，发现情绪是**一条一条独立判定**的：
每条气泡根据模型标签或关键词定情绪，互不影响。于是有一个很出戏的现象——
**刚闹完别扭，下一句问"在吗"，角色又乐呵呵地回**，好像上一轮没发生过。

同时 `CharacterPresence` 里的 `"away"` / `"offline"` 两个状态也没有任何生产者
（这轮没做，记在下面的候选里）。

## 二、做法：推导，不存储

新文件 `core/src/emotion/MoodTracker.ts`：心情完全由「最近 8 条**角色自己发的**
消息的情绪 + 真实时间衰减」算出来。

- 越近的消息权重越高（每往回一条减半），再乘一层时间衰减（半衰期 2 小时）；
- `neutral` 也参与计分，于是"说了几句平淡的话"会把之前的情绪自然稀释掉；
- 强度 1~5：一条新鲜情绪算 3（"有些"），连着两条同向升到 4~5；
  负向情绪略微更"黏"（×1.15）；
- 强度 < 3 或落在 `neutral` 就不注入——轻微的开心不值得写进 system。

**为什么推导而不是落库**：不落库就不用同步、刷新后天然一致、也不会
跟消息列表对不上。代价是它只反映"最近聊得怎么样"，记不住"三天前那次吵架"——
那类长期状态归记忆与剧情管，各司其职。

两条效果：

1. **注入 Prompt**：`【你此刻的心情】低落（比较明显）……`，并且明确写上
   "这是累积的不是对这一句的反应"以及"如果对方这句话真让你情绪变了，那就变"——
   后半句很重要，否则模型会为了维持心情而无视对方刚说的话，那是另一种出戏。
2. **立绘兜底**：文本看不出情绪时（"嗯，好。"），立绘取当前心情而不再一律回到
   neutral；文本自己有情绪线索时以文本为准（心情不覆盖当场的表情）。

## 三、真机验证：第一版探针等于没验证

第一版探针用"在吗？"当测试句，A/B 两组都没差异——因为**基线本来就不热情**
（两组都回"在的。"），指标毫无区分度。这是这轮最有价值的一次自我纠错：
**先得让问题复现，才能谈修复有没有效**。

换成"要不要一起去看电影？"这种邀请（没情绪残留时会爽快答应，还带着气时才别扭）：

| 场景（真机 agnes，各 4 轮） | A 不注入心情 | B 注入心情 |
|---|---|---|
| 刚闹别扭 + 轻松邀请：爽快答应（出戏） | **3/4**（"好呀，几点映？"） | **0/4** |
| 刚闹别扭 + 轻松邀请：别扭/冷淡 | 0/4 | 1/4（"没心情。"） |
| 刚被逗开心（对照）：爽快答应 | 2/4 | 3/4 |
| 刚被逗开心（对照）：冷淡 / 负向情绪 | 0/4 / 0/4 | 0/4 / 0/4 |

结论：负向场景里"刚吵完架还能兴高采烈答应"从 3/4 降到 0/4；
正向对照没有被弄冷（0 冷淡、0 负向误判）——没有矫枉过正。

## 四、浏览器端到端（立绘那条）

用 `work/fake_llm_server.mjs`（本轮改造成按请求次数给脚本化回复）：
前两轮角色明确标 `angry`，第三轮回复"嗯，好。"——**既没有情绪标签、
正文也没有任何情绪词**，立绘只能靠心情兜底。

```
第 1 轮：……抱歉，我是有点在意。       → 心情 = angry
第 2 轮：我没生气，就是心里不太舒服。   → 心情 = angry（强度更高）
第 3 轮：好。                          → 点开立绘：显示「生气」
        （改造前这里会是「平静」——情绪归零）
```

## 五、验证

```bash
pnpm test           # 685 个测试全绿（64 个文件，本轮新增 19 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 496.37 KB / gzip 165.56 KB）
pnpm test:coverage  # 语句覆盖率 94.5%（MoodTracker.ts 100%）
```

## 六、留档：这轮没做的候选

- **`away` / `offline` 仍是死状态**：`CharacterPresence` 定义了、界面也留了
  `data-presence` 样式位，但没有任何地方会产生它们。合理的做法是给角色加
  "忙碌时段"（上课 / 上班），忙碌时显示 `away` 且回复更短更慢——属于
  "作息感知"的自然延伸，留作下一轮。
- 心情目前没有界面可见入口（只在立绘与语气上体现）。若以后要展示，
  放在上下文占用面板里成本最低。

---

# 第六十九轮：角色也会引用你的话（引用一直是单向的）

## 一、为什么做这个

第四十四轮做了"引用回复"，但它是**单向**的：你能引用角色说过的话，
角色永远只能干巴巴地回应。长对话里它想回头接你**十几轮之前**那句时，
读者得自己往上翻才能确认"它接的是哪句"——而微信里这是点一下就跳回去的事。

这一轮补上另一半：角色也能引用，引用块同样可点跳转。

## 二、做法

- **格式**：让模型在正文开头写 `【引用：那句原文里的一小段】`。
  选择自定界的方括号而不是"另起一行写 `> 引用：…`"——回复是流式分片到达的，
  靠换行判定就得一直攒着等下一个换行；有闭括号，见到 `】` 立刻能决定。
- **解析**：`core/src/llm/characterQuote.ts` 里的流式过滤器
  （`CharacterQuoteFilter`）。分片切在标记中间（`【引` / `用：你好】`）也不会
  漏出半截标记；尾部最多只扣住 3 个字符，流结束时全部交出去，一个字都不丢。
- **匹配**：片段 → 具体消息，四级判分（完全相同 > 你的话包含片段 >
  片段包含你的话 > bigram 相似度），同分取**最近**那条。
  贴的是**原消息**的文本而不是模型抄回来的片段——跳转目标与引用块必须一致。
- **候选**：`buildQuoteCandidates` 带消息 ID（引用块要能点回那条消息），
  由 App 在每次发送前注入；两侧的话都收（引自己之前那句也成立）。
- **UI 零改动**：`message.quote` 的渲染与跳转早就在，引擎把值填上就通了。

## 三、真机验证（这一轮最有价值的部分）

先写 `work/agnes_character_quote_probe.mts`，三组场景各 4 轮，
逐条走引擎真实的解析链路（情绪头 → 过滤 → 匹配）：

| 场景 | 期望 | 改前 | 改后 |
|---|---|---|---|
| A 紧邻一问一答 | 不引用 | 0/4 误用 | 0/4 误用 |
| B 回头接很早那句 | 引用 | **1/4** | **4/4** |
| C 长对话只回应最后一句 | 不引用 | 0/4 误用 | 0/4 误用 |

标记能匹配到候选消息：改前 1/2 → 改后 **4/4**；气泡里残留标记：**0**。

真机暴露了两个只有跑起来才知道的问题，都已修掉：

1. **模型把标记写在正文末尾**（`吃了呢，刚晚饭。你呢？ 【引用：你吃饭了吗？】`）——
   第一版只扫"正文开头"，这串格式原样漏进了气泡。改成通用流式过滤器后，
   开头 / 中间 / 末尾都摘；测试里专门锁了这条。
2. **模型抄的是自己上一轮那句话**——第一版候选只收用户消息，匹配不上、
   只能把标记剥掉了事。改成两侧都收（署名分别是"你"和角色名）。

另外把"回头指某句时就用引用接上"和"必须逐字照抄原文"写进 Prompt，
这是 B 组从 1/4 提到 4/4 的直接原因；同时保留"一问一答紧挨着时不要引用"，
所以 A/C 两组的误用始终是 0——**该用的时候用，不该用的时候不啰嗦**，
这个平衡比单纯把触发率拉满重要。

## 四、浏览器端到端

真机域名在无头 Chromium 里连不通，于是写了 `work/fake_llm_server.mjs`
（本地 OpenAI 兼容 SSE 端点，分片故意切在 `【引用：` 中间），
走完整链路：适配器 → 情绪头 → 过滤器 → 引用块 → 渲染 → 跳转。

```
第 1 轮：我说「你昨天说要把笔记带给我，别忘了」→ 角色回「啊对，」「我放你桌上了。」
        （标记引的是*当前*这句，不在候选里 → 只摘标记，不挂引用块）
第 2 轮：我问「你还记得我昨天说什么吗？」→ 角色第一条气泡变成
        「你 / 你昨天说要把笔记带给我，别忘了」+「啊对，」
点引用块 → 跳到第 1 条消息并高亮
刷新 → 引用块仍在（存在消息里，不是临时状态）
全程气泡里没有出现过 `【引用` 字样
```

## 五、验证

```bash
pnpm test           # 666 个测试全绿（62 个文件，新增 31 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 494.97 KB / gzip 164.70 KB）
pnpm test:coverage  # 语句覆盖率 94.35%（characterQuote.ts 100% / 分支 93.75%）
```

---

# 第六十八轮：撤回（把另一条断掉的链路接通）

## 一、为什么做这个

翻代码时发现"撤回"是**半截链**：消息契约里有 `IRecallMessage`、
Store 里有 `case "recall"` 的处理、气泡也会渲染"XX撤回了一条消息"，
但**没有任何地方会发出撤回事件**——只能靠调试入口手工塞一条进去看渲染。

这跟第一阶段修的那条"prompt 注入链路"是同一类问题：
零件都在，中间少一根线。而且它在拟真方向上是真有用的：
微信里"说漏嘴→撤回"是很典型的一幕。

## 二、角色侧（引擎）

- `ISimulationConfig` 加 `recallProbability?`（**可选**，缺省按 0）：
  老存档不用迁移，测试里的配置对象也不用逐个补字段；
- 引擎记住本轮**最后一条文本气泡**的 id（撤回一条贴图没有叙事价值）；
- `finishRun()` 里"撤回优先于贴图"：命中就 1.2~2.5 秒后发 recall 事件、
  然后才收尾。这个延时是有意的——真人撤回不是反射动作，
  发出去了、看一眼、觉得不妥、再点撤回；秒撤读起来像程序行为。
  延时期间被"停止"会连定时器一起清掉，不会出现"人都走了还在撤回"。

## 三、用户侧（UI）

- 新文件 `core/src/recall/Recall.ts`：两分钟窗口、提示文案、
  "撤回后那条消息长什么样"三个纯函数（引擎与 Store 共用一套判断）；
- 长按/右键菜单多一项"撤回"，**只有自己发的 + 两分钟内的**消息才出现
  （否则菜单里会有个点了没反应的入口）；
- Store 新增 `recallMessage`，会话快照新增 `recallSessionMessage`
  （后台会话在非活跃时撤回，消息得落进它自己的快照）；
- 设置面板新增"撤回概率"滑块（0~1，与错别字率同一惯例，
  调到 1.0 可以必然触发，方便验证）。

## 四、一个刻意的取舍：撤回要丢掉正文

第一版想直接在原消息对象上 `type: "recall"` 了事，但那样**正文还在**——
刷新一下、翻一下 IndexedDB 就能把撤回去的话捞出来，等于没撤回。
改成用 `toRecallMessage` 重建对象，只留定位字段（id/会话/时间戳/序号），
于是它会自动从 LLM 历史（`toLLMHistory` 跳过撤回消息）里一起消失。

## 五、途中修掉的真 bug：撤回不落库

浏览器实测："撤回自己发的某条消息 → 刷新 → **原文又回来了**"。

根因在 `App.tsx` 保存会话快照的 effect：它只依赖 `messages.length`
和"最后一条消息"的引用，而撤回是**原地替换**——
数组长度不变，被替换的那条又未必是最后一条（后面通常还有角色的回复），
于是 effect 不触发，快照没更新；刷新时反过来用旧快照覆盖了已撤回的状态。

改成直接订阅 `messages` 数组本身。补充了针对这个不变量的单测
（撤回必须换掉数组引用与被撤回那条的引用，其余消息保持原引用）。

## 六、验证

```bash
pnpm test           # 635 个测试全绿（60 个文件，新增 27 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 491.79 KB / gzip 163.09 KB）
pnpm test:coverage  # 语句覆盖率 94.08%（新增 Recall.ts 100%）
```

浏览器实测（供应商选 Mock，撤回概率调到 1.0）：

```
发「你今天怎么没来上课？」→ 角色回 9 条气泡
  → 约 2 秒后最后一条变成「苏晚晴撤回了一条消息」（原地替换，不是追加）
  → 会话列表预览同步变成「苏晚晴撤回了一条消息」（原来会停留在被撤回的那句话上）
右键自己 2 分钟内的消息 → 菜单「复制 / 引用 / 撤回 / 删除」
  → 点撤回 → 气泡变「你撤回了一条消息」，原文从 DOM 与 IndexedDB 里消失
右键 2 分钟前的消息 → 菜单里没有「撤回」
刷新 → 撤回结果仍在（修掉快照 bug 之前这里会原样复现原文）
```

隔离性也确认过：撤回后翻 IndexedDB，被撤回那条只剩提示对象，
不含 `text` 字段；唯一还能看到那句话的地方是**角色回复里复述的原文**。

已知开发态现象（非本次改动引入）：改文件触发 HMR 时若正好有流在跑，
热更新会杀掉那条流，页面会停在"回复中"。重载即可，正式构建没有这个问题。

---

# 第六十七轮：那次偶发失败，查到最后是"环境"

## 一、要解释的现象

第六十轮与第六十六轮各出现过一次 `pnpm test` 失败，重跑即绿，
失败详情当时没留下来。这是项目里唯一未解释的现象。

## 二、做了什么

1. 连续跑 5 次全量 `pnpm test`：全绿；
2. 按当时的完整命令链（test → typecheck → build → build:web → coverage）
   重放 2 轮：全绿（进程正常退出，退出码 0）；
3. 主动制造负载：起 3 个 CPU 满载子进程（90 秒）同时跑 `pnpm test`：
   全绿，整套只要 6.5 秒；
4. 静态排查了所有"看时间说话"的测试：
   `PresenceManager`（用 `new Date()` 拼时刻）、
   `RealismEngine.scheduling`（`Date.now()` 断言）、
   `chatStore`（时间戳落在 before/after 之间）、
   `StickerDelivery`（`advanceTimersByTimeAsync`）——
   它们要么用假定时器、要么跟墙上时间无关，
   而且全项目测试里没有真实等待（没有 `await new Promise(setTimeout)` 之类）。

## 三、结论

无法复现，倾向"环境/进程竞争"：当时是把 5 条重型命令串在同一个 shell 里跑，
vitest 的 worker 还没完全退出就开始 build（vite 也在写同一块磁盘缓存）。
本次没有能改动产品代码的依据——按调试纪律，
**没有根因就不动代码**，只把验证习惯写下来：

- 各条验证命令分开跑（本轮就是这么跑的）；
- 真再复现时先留完整输出（失败信息是唯一线索），
  而不是直接重跑——重跑会把线索一起洗掉。

---

# 第六十六轮：组装层进测试（此前是覆盖盲区）

## 一、盲区在哪

`vitest.config.ts` 的 include 一直只扫 `packages/**`——
**`apps/web` 的组装层完全没有测试**：`characterCardRunner`、`backupRunner`、
`transcriptRunner`、`characterGenRunner`、`llmRuntime`…

而这几轮改得最频繁的就是它们（数据源选择、错误提示、格式识别、合并落库）。
一直只靠浏览器手测，改坏了不会立刻发现。

## 二、这轮做了什么

1. include 加上 `apps/web/src/__tests__/**/*.test.ts`；
2. 给三个组装层文件补测试（共 17 条）：

| 文件 | 覆盖的点 |
|---|---|
| `transcriptRunner` | 活跃会话从 chatStore 取消息、后台会话从运行时快照取、会话不存在与空会话的可读提示、文件名非法字符替换 |
| `characterCardRunner` | 自家卡**导出再导入的往返**、社区卡 V2 的字段与世界观落库、非卡文件与坏 JSON 的提示、重名自动加后缀不覆盖 |
| `backupRunner` | 快照里**绝不含 API Key**（安全约束）、备份文件结构、导出文件名与统计、导入合并保留本地数据、坏文件不动本地数据 |

## 三、踩到的环境坑

jsdom 的 `File` **没有 `text()`**（浏览器里有），而 runner 正是用它读文件——
一开始四条导入测试全挂在"读取文件失败"。修法不是改产品代码，
而是给测试造一个带 `text()`/`arrayBuffer()` 的替身，这样测到的仍是 runner 的真实逻辑。

## 四、验证

```bash
pnpm test           # 608 个测试全绿（58 个文件，新增 17 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 489.30 KB / gzip 162.36 KB）
pnpm test:coverage  # 语句覆盖率 93.51%
```

（跑验证时又遇到一次偶发失败，单独重跑 test:coverage 正常——
与第六十轮记录的是同一现象，暂时只在高负载的连续命令里出现。）

---

# 第六十五轮：AI 生成角色的多版本

## 一、设计取舍：不做"一次 3 版"

这个候选挂了很久，一直卡在成本上：一次生成 3 版就是 3 倍 token，
而用户未必需要——多数时候第一版就能用。

所以做成**按需多版本**：不满意才点「再生成一版」，**上一版不丢**，
面板上出现「第 2/3 版 ◀ ▶」可以来回切着挑，表单跟着当前版本变。

## 二、实现

- 生成历史与当前下标放在**同一个 state 对象**里
  （`{ list, index }`）——分别放两个 state 时，"追加后立刻改下标"
  需要在 setState 的 updater 里做副作用，容易出时序问题；
- 抽了一个 `applyDraft(draft)`：生成完成、切换版本都走同一条填表路径；
- 版本导航只在生成过 2 版以上时出现（一版时不显示，避免噪声）；
- 主按钮文案随状态变：「生成」→（已有结果时）「再生成一版」。

## 三、验证

```bash
pnpm test           # 591 个测试全绿（55 个文件，新增 2 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 489.30 KB / gzip 162.36 KB）
```

新增测试 2 条：生成两版后出现版本导航且表单停在最新版（一版时不显示导航）、
在生成过的两版之间来回切时表单跟着换（连开场白一起换）。

真机验证（`work/agnes_gen_versions_probe.mts`，同一描述生成两次）：

```
第一版（71220ms）：沈知遥｜17 岁、两年同桌、父母常出差、家里养猫
第二版（32144ms）：林夏｜高二(3)班、三年同桌、掉色猫形挂件、抽屉藏着猫粮
两版相似度：8.7%
✅ 差异明显——「再生成一版」确实能挑到不同的人设
```

这组数据同时说明两件事：多版本有价值（差异足够大）；
而生成耗时波动很大（71s vs 32s），所以上一轮的进度提示与取消按钮是必需的。

---

# 第六十四轮：立绘的情绪覆盖提示

## 一、缺口

角色编辑器里能上传立绘、给每张选情绪，但**看不到"八种情绪配全了没有"**——
得自己数。而漏掉的情绪会在聊天时默默退回第一张立绘（用户根本不知道
"为什么她生气的时候还是这张笑脸"）。

## 二、做法

立绘列表上方加一行覆盖提示，两种文案：

```
情绪已覆盖全部 8 种
情绪已覆盖 6/8 种 · 缺：平静、开心
```

- 情绪中文名复用 `EMOTION_LABELS`（与编辑器的下拉、查看器同一份映射）；
- 一张立绘都没有时不显示这行（不吓唬新用户）；
- 用 `role="status"` 让读屏也能听到变化。

## 三、验证

```bash
pnpm test           # 589 个测试全绿（55 个文件，新增 3 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 488.54 KB / gzip 162.12 KB）
```

新增测试 3 条：缺情绪时列出缺口、全覆盖时给一句肯定且不再列缺口、
没有立绘时不显示提示。

浏览器实测（演示角色自带 8 张）：

```
打开编辑器 → 「情绪已覆盖全部 8 种」
删掉两张 → 「情绪已覆盖 6/8 种 · 缺：平静、开心」（列表里相应少两张）
```

---

# 第六十三轮：导出聊天记录（给人读的 Markdown）

## 一、为什么做

项目早就有「数据备份」，但那是 JSON——给机器恢复用的，人翻不动。
想**存档一段精彩对话、或者发给朋友看**，需要的是给人读的版本。

## 二、做法

- core 的 `buildTranscript(messages, { characterName, userName, now })`（纯函数）：
  - 标题 + 导出信息（时间、条数）；
  - 每条消息署上发言人名字与时间；
  - 引用渲染成 `> 引用 谁：原话` 的引用块（人读时一眼看出回应的是哪句）；
  - 跨天插入 `## 日期` 小节；
  - **跳过系统消息与撤回提示**——那是界面噪声，不是对话内容；
  - 贴图/语音/图片分别落成 `[开心]`、`[语音 8 秒]`、`[图片]`；
  - 乱序输入内部按时间戳 + chunkSequence 排好。
- 组装层 `transcriptRunner.ts`：从**正确的数据源**取消息（活跃会话在 chatStore，
  其他会话在自己的运行时快照里），拼上带日期的文件名后下载。
- 入口放在会话列表的三点菜单（置顶 / 标为已读 / **导出聊天记录** / 删除会话）——
  一个会话一份记录，从一个会话导出最自然。

## 三、验证

```bash
pnpm test           # 586 个测试全绿（55 个文件，新增 8 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 488.22 KB / gzip 162.02 KB）
```

新增测试 8 条：标题/导出信息/日期/署名的基本格式、跳过系统与撤回消息、
引用块、跨天日期标题、三类非文本消息的可读占位、乱序输入排序、
空会话也能产出文件、自定义用户名。

浏览器实测（真实下载）：

```
会话菜单：置顶 / 标为已读 / 导出聊天记录 / 删除会话
点击后下载 zhichi-chat-林笑笑-20260913.md，内容：
# 与 林笑笑 的聊天记录
> 导出时间：2026/9/13 15:49　·　共 9 条消息
## 2026/9/13
**我** 15:49
导出测试消息
**林笑笑** 15:49
嗯，
...（逐条署名与时间，格式正确）
```

---

# 第六十二轮：跨功能回归（最近几轮改动密集，验证它们组合起来没问题）

## 一、为什么专门做一次

五十一～六十一轮改动很密：引擎常驻、后台订阅、主动消息落库、引用跳转、
用户贴图、删除单条、生成进度……这些都在动**消息流与状态同步**这条主干，
单测各自通过不代表组合起来没事（历史上"改 A 坏 B"的例子不少）。

所以这轮不加功能，按用户实际会用到的顺序在浏览器里跑一遍：

| # | 场景 | 结果 |
|---|---|---|
| ① | 发消息 → 角色多气泡回复 | ✅ 8 条、正常交付 |
| ② | 长按 → 引用 → 发送 → 点引用块 | ✅ 高亮落在被引用的「嗯，」上 |
| ③ | 长按 → 删除单条 | ✅ 18 → 17 |
| ④ | 表情面板发贴图 → 角色回应 | ✅ 贴图上屏、角色回 8 条 |
| ⑤ | 回复中切走 → 后台跑完 → 回看 | ✅ 会话快照 26 → 35 条、phase=completed、未读 8 |
| ⑥ | 「换一条」重新生成 | ✅ 35 → 撤到 28 → 重发后 34 |
| ⑦ | 编辑重发 | ✅ 最后一条用户消息更新、其后回复重建 |
| ⑧ | 搜索聊天记录 → 点击结果 | ✅ 2 条命中、跳转高亮 |
| ⑨ | 提示词 / 记忆 / 设定 / 剧情四个 Tab | ✅ 都能打开且有内容 |
| ⑩ | 刷新页面 | ✅ 会话 2 / 角色 3 / 消息 10 前后一致 |

**结论：没有发现跨功能冲突**，主干在密集改动后仍然自洽。

## 二、脚本踩的坑（与产品无关，但记一下）

- ② 第一次长按落空：目标消息在视口边缘，坐标算到了输入栏上——
  改成先 `scrollIntoViewIfNeeded()` 再长按；
- ⑤ 第一次点错了会话（点的是当前会话，等于没切走）——
  改成用会话 ID 直接 `switchSession()`，并先把两个会话的 ID 打印出来核对；
- ⑧ 第一次用错选择器（`.zhichi-search-result` 实际叫
  `.zhichi-message-search__hit`），显示"0 条命中"其实是没查到元素。

三条都不是产品 bug，但说明**回归脚本本身也要先确认"我以为的"和"实际的"一致**，
否则会得出"功能坏了"的错误结论。

## 三、验证

```bash
pnpm test           # 578 个测试全绿（54 个文件，本轮无代码改动）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
```

---

# 第六十一轮：删除单条消息

## 一、缺口

消息的清理能力此前只有两种：编辑重发（只能改最后一条自己发的）与重新生成（撤掉最后一轮）。
想删掉**某一条**不合适的话没有入口——而 RP 场景里这很有用：
删掉之后它会同时从 LLM 历史里消失，等于**让角色忘掉这一句**。

## 二、做法

- `chatStore.deleteMessage(id)`：按 ID 过滤掉那一条（找不到就什么都不做）；
  刻意不复用「从某条删到末尾」的 `removeMessagesFrom`——语义完全不同；
- 菜单里加第三项「删除」，用红色与另外两项区分（不可逆操作）；
- 链路照旧透传：`ChatSessionView` 从 store 取 → `MessageList` → `MessageBubble`；
  没接这个能力时菜单里不出现「删除」（组件仍可独立使用）。

## 三、验证

```bash
pnpm test           # 578 个测试全绿（54 个文件，新增 5 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 485.92 KB / gzip 161.33 KB）
```

新增测试：chatStore 3 条（只删那一条、删后不再出现在 LLM 历史里、
ID 不存在时不做改动）+ MessageBubble 2 条（有删除能力时菜单出现「删除」
且回调拿到 ID、没接能力时不出现）。

浏览器实测：

```
长按气泡 → 菜单「复制 / 引用 / 删除（红）」→ 点删除 → 消息数 8 → 7、菜单收起、无报错
```

**排查记录**：第一次删除后数量看起来没变（9 → 9），一度以为没生效——
实际是那一轮还在**流式交付**：删掉一条的同时又有新气泡进来，数量正好抵消。
直接调 store 验证（9 → 8）排除了逻辑问题，等这轮跑完再点就正常了。
（顺带说明：流式期间删除单条是允许的，只是"总数不变"会让人误判。）

---

# 第六十轮：用户发贴图

## 一、不对称的地方

第五十二轮让角色的 `stickerFrequency` 生效之后，贴图是"单向"的：
角色会发表情，用户侧却只有文本输入。截图对比微信，这个缺口很明显。

## 二、做法

- 输入栏新增表情按钮（笑脸图标）+ 八宫格面板：复用角色贴图的同一套内置素材
  （`BASIC_STICKERS` + `buildPlaceholderSticker`），不新增任何资源；
- 面板浮在输入栏上方（不挤压消息区），点空白处收起、再点按钮也收起；
- `chatStore.addStickerMessage` 把用户贴图上屏（`senderId: "user"`，
  渲染走既有的 `StickerBubbleBody`）；
- 发送后照样请求模型：`toLLMHistory` 早就会把贴图转成 `[惊讶]` 这类占位文本，
  于是角色会针对表情回应（实测回复里出现了「我听到你说「[惊讶]」了」）。
- **对方正在回复时只上屏、不排队**：表情是即时表达，为它单排一轮回复反而怪。

## 三、途中修掉的一个自造 bug

第一版实现里我先 `addStickerMessage` 再判断 `phase`——而这个 action 自己会把
phase 置为 `streaming`，于是判断永远成立、**角色从不回应**。
浏览器实测时发现"贴图发出去了但没人理"，改成"先看 phase 再上屏"。
（教训：同一段逻辑里既改状态又读该状态时，顺序要写清楚。）

## 四、验证

```bash
pnpm test           # 573 个测试全绿（54 个文件，新增 5 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 485.49 KB / gzip 161.24 KB）
```

新增测试：InputBar 4 条（没接能力时不显示按钮、点按钮弹出面板且八张表情都在、
点表情回调拿到标识且面板收起、再点按钮收起）+ chatStore 1 条
（用户贴图上屏、senderId 正确、进入回复中）。

浏览器实测：

```
点表情 → 面板 8 张（4×2 网格，浮在输入栏上方）
点 [惊讶] → 气泡靠右渲染成 96px 贴图（alt="[惊讶]"）
角色回应：7 条文本，其中「我听到你说「[惊讶]」了」
```

（跑完发现有一次偶发失败，连跑两次均 573 全绿，判断是与当时关闭 dev server 的
时序竞争；后续若再复现再查。）

---

# 第五十九轮：点引用块跳回原消息

## 一、缺的是什么

引用回复做完之后留了个小尾巴：**引用块是死的**——点它没反应。
长对话里用户想确认"他引的是哪一句"，只能自己往上翻。

## 二、做法：复用已有的跳转机制

聊天记录搜索跳转早就实现了"窗口平移 + 两拍定位 + 高亮 2 秒"，
这次只是把它接到引用块上：

- `ChatSessionView` 新增内部状态 `localJumpId`——与 `jumpToMessageId`
  （跨会话搜索跳转，由父组件驱动）**共用同一段定位逻辑**，
  区别只是本地跳转不需要切会话；
- 跳转完成后清掉 `localJumpId`，避免重复触发；
- `MessageBubble` 的引用块在拿到跳转回调时渲染成 `<button>`
  （有 hover 反馈、`aria-label` 写明跳去哪儿）；没接回调时仍是普通块，
  保持组件在没有该能力的场景下可用；
- 被引用的消息若已被删除/撤回（不在列表里），定位逻辑会自行放弃——
  与搜索跳转的既有行为一致。

## 三、验证

```bash
pnpm test           # 568 个测试全绿（53 个文件，新增 2 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 483.83 KB / gzip 160.86 KB）
```

新增测试 2 条：点引用块把被引用消息的 ID 交给跳转回调、
没接回调时引用块不是按钮（内容照常显示）。

浏览器实测：

```
长按角色气泡 → 引用 → 发送「引用跳转测试」
点这条消息上的引用块
→ 高亮落在被引用的那条上：data-message-id 与引用块里的来源一致、正文就是「嗯，」
```

---

# 第五十八轮：AI 生成角色——让等待可见、可取消

## 一、问题

生成一张角色卡要等十几到几十秒，而界面只有一句"生成中…"：
用户不知道是在写、还是卡住了。真机跑一遍后发现更糟——
**推理型模型前十几秒只"想"不吐字**，这期间连字数都不会变化。

## 二、做法

- **流式进度**：`generateCharacterDraft(description, { onProgress, signal })` 新增进度回调；
  引擎侧每 200ms 节流上报一次"已接收字数 + 从半截 JSON 里读到的角色名"，
  界面显示成「正在生成「林晓夏」…已接收 320 字」
  - 名字提取抽成 core 的 `extractDraftName()`（纯函数、可单测）：
    只认写到闭合引号的名字，半截名字不显示，避免闪烁
- **慢响应提示**：超过 5 秒还没有任何输出时，文案改成
  「正在生成…（模型首次响应较慢，请再等等）」——把"卡死"和"在思考"区分开
- **可取消**：生成区的主按钮在生成中变成「取消」，点它通过 `AbortSignal`
  中断这次请求；面板关闭时也会自动取消，不让请求继续烧 token

## 三、验证

```bash
pnpm test           # 566 个测试全绿（53 个文件，新增 6 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 483.38 KB / gzip 160.76 KB）
```

新增测试：CharacterForge 4 条（半截 JSON 提取名字、名字没写完不显示、
没有 name 字段返回 null、带转义不硬猜）+ CharacterEditor 2 条
（生成中显示进度与取消按钮并随流式上报刷新、点取消把中断信号传给生成器）。

真机验证（`work/agnes_gen_progress_probe.mts`，模拟界面同一条节流+提取链路）：

```
进度上报 15 次：
  + 13675ms　已接收    2 字　名字还没写出来
  + 13898ms　已接收   51 字　名字已出现：「林晓夏」
  ...
  + 21776ms　已接收  664 字　「林晓夏」
名字首次出现在 +13898ms（总耗时 21783ms），最终解析：成功「林晓夏」
```

这组数据也直接暴露了"前 13.7 秒一个字都没有"，才有了上面那条慢响应提示。

---

# 第五十七轮：引擎常驻——切走后角色继续把话说完

## 一、把上一轮的权宜之计换成正解

上一轮用 `flushPendingDeliveries()`（切走前把已排程的气泡一次交完）解决了
"回复断在半截"，并在记录里留了尾巴：**LLM 还没生成完的部分仍会丢**。

这轮把根因解决掉：切走时**不销毁引擎**，让它在后台把这轮说完。

### 为什么之前的 flush 可以删掉

flush 的语义是"这一轮到此为止"（它会 `finishRun`），与"后台继续说"直接冲突；
而它想解决的问题，常驻订阅能更完整地覆盖（连还没生成的都算上）。
所以把 flush、`pendingDeliveries` 队列与对应测试一并删掉——
只保留 `deliverScheduledChunk` 这层抽取（它让"排程"与"交付"分开，本身是好的）。

## 二、改动

1. **App 切换会话时不再 `engineManager.dispose(prevId)`**，只解绑界面订阅；
   引擎继续跑，输出由已有的"背景订阅器"接住 → 写进该会话的运行时快照
   （上一轮刚加的 `appendSessionMessage`）。
2. **新增 `EngineManager.retainOnly(sessionIds)`**：引擎常驻之后必须有人负责收口——
   删除会话（或删角色连带删会话）时，把它的引擎 abort 并从池子里摘掉，否则泄漏。
   App 用一个"会话 ID 集合"的 effect 驱动它。
3. 引擎里那段"暂停/恢复未实现"的说明同步更新：真实需求现在由"常驻 + 路由"承担。

## 三、验证

```bash
pnpm test           # 560 个测试全绿（53 个文件；删掉 flush 4 条，新增 retainOnly 2 条）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 481.96 KB / gzip 160.21 KB）
```

浏览器实测三个场景：

```
① 发消息后 1 秒切走 → 14 秒后该会话快照：8 条消息、phase=completed、未读 7
   （切走时只有 1 条气泡，说明后台确实继续把它说完了）
② 切回去：未读清零、界面 9 条气泡（1 条用户 + 8 条角色），回复完整
③ 后台跑到一半切回来：气泡继续在界面上弹，最终 18 条消息、
   消息 ID 无重复、chatStore 与运行时快照一致、以「你今天过得怎么样？」收尾
```

## 四、体检方法的收尾

这一串（五十一～五十七轮）从"未使用声明 → 空壳接口 → 只读不写的字段 →
未接线的行为"一路查下来，最后落到一个架构性的根因：**chatStore 只认活跃会话**。
现在它被补上了：活跃会话走 chatStore（有逐字动画、占位气泡、未读复位等完整处理），
非活跃会话走自己的运行时快照（消息、未读、预览都齐全）。

---

# 第五十六轮：主动消息真的落到它自己的会话里

## 一、问题

上一轮把"切走会话"的遗留项记成了"需要按会话路由输出"。这轮顺着这条线查下去，
发现**同一个根因已经在制造一个更明显的 bug**：

实时 AI 的主动消息是**跨会话轮转**的（挑"最久没消息"的会话说话），
而 `chatStore` 只订阅当前活跃会话的引擎。于是当主动消息发给**非活跃会话**时：

- App 里的"背景订阅器"只做了 `incrementUnread`——会话列表出现红点和预览；
- 消息本身**没有任何地方保存**。

结果：用户看到"对方发来一条消息"，点进去**是空的**。红点骗人。

## 二、做法

给 sessionStore 加一个 `appendSessionMessage(sessionId, message, simulationConfig)`：
往指定会话的运行时快照里追加一条消息（快照不存在就按最小结构创建，
存在则保留原有的作息/在场状态，并把 phase 归到 completed——
后台交付完就是这一轮结束，不该停在 streaming）。

App 的背景订阅器里调用它，于是非活跃会话的交付链路变成：

```
引擎交付 → 写入该会话的运行时快照 → 更新预览 + 未读红点 → 用户点开就能看到
```

## 三、验证

```bash
pnpm test           # 562 个测试全绿（54 个文件，新增 2 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 482.40 KB / gzip 160.28 KB）
```

新增测试 2 条：快照不存在时创建（消息落库、phase=completed、
typingIndicator 关闭）、已有快照时按顺序追加且保留原有作息状态
（"已就寝"不该被一条消息抹掉）。

浏览器实测（开启实时 AI、3 秒间隔，停在一个会话上让心跳去挑另一个）：

```
非活跃会话「后台角色」：unread=1、preview="嗯，"、runtimeMessages=1
点开它：未读清零、气泡 2 条、消息可见
```

## 四、遗留

还剩"LLM 未生成完就切走"的部分：那一轮剩余的输出仍会丢。
彻底解决要让**所有**引擎的订阅常驻（而不是切换时重订阅）并按会话路由，
本轮没做——它动的是订阅模型本身，值得单独一轮。

---

# 第五十五轮：切走会话不再丢半截回复（顺带清掉 pause/resume 空壳）

## 一、体检发现的空壳

这轮查的是"引擎公开方法有没有从未被调用的、或是空壳"。
`pause()` / `resume()` 两条都中：方法存在、注释还写着"实际实现：暂停所有定时器"，
但实际上只 emit 一个生命周期事件，**既不暂停定时器，也从来没有任何调用方**。
它们属于"看起来有这个能力、其实没有"的误导性接口。

## 二、真正的问题在后头

顺着"暂停"想到一个真实场景：**切走会话时正在逐条弹出的回复会怎样**？

实测确认：拟真模式下一条长回复可能铺十几秒，此时切走会
`engineManager.dispose(prevId)` → `abort` → **已排程但还没交付的气泡全部丢失**，
切回来只剩半截回复。

（为什么不直接"让后台继续生成"：`chatStore` 只承载活跃会话的消息，
后台引擎继续 emit 会把消息写进错误的会话。那需要一套按会话路由输出的改造，
这轮不做，先解决最痛的部分。）

## 三、做法：切走前把已生成的气泡补齐

- 引擎里新增 `pendingDeliveries`：记录"已排程、等待交付"的 chunk
  （原先是闭包在定时器里，flush 时找不回来）；
- 把定时器回调体抽成 `deliverScheduledChunk()`，
  定时器到点与 `flushPendingDeliveries()` 走同一条路径（错别字、逐字时间轴行为一致）；
- `flushPendingDeliveries()`：清掉未触发的定时器 → 按顺序同步交付剩余气泡 →
  补一次收尾（`finishRun`），保证引擎不会卡在 streaming、下一轮能正常开始；
- App 的会话切换：在保存运行时快照**之前**调用 flush，
  这样补齐的消息会被一起写进该会话的快照。

## 四、验证

```bash
pnpm test           # 560 个测试全绿（54 个文件，新增 4 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 481.91 KB / gzip 160.18 KB）
```

新增测试（`FlushDeliveries.test.ts`）：排队气泡被立刻交完且顺序正确、
flush 之后不再有延迟交付（不重复弹同一句）、没有待交付项时是安全空操作、
flush 后引擎能立刻开始下一轮（这条最初是红的——flush 没补收尾，
引擎卡在 "stream already in progress"）。

浏览器实测（拟真模式 + mock 适配器）：

```
发送消息 → 等到气泡还在逐条弹（6 条，phase=streaming）→ 切到另一个会话 → 切回
切回后 12 条气泡、phase=idle，回复全文完整（「…不过我们可以一起慢慢聊。你今天过得怎么样？」）
```

## 五、遗留（下一步候选）

**后台继续生成**：现在只补齐"已经生成好的部分"；LLM 还没吐完的内容仍会丢。
要做到"切走后角色继续说完整段话"，需要把引擎输出按会话路由
（活跃会话 → chatStore；其他会话 → 该会话的运行时快照），
并相应改造订阅模型（当前 chatStore 只持有一个订阅）。

---

# 第五十四轮：立绘缩略图（一个只有读、没有写的字段）

## 一、体检第三层：类型字段

前两轮查的是"引擎事件/UI props"和"未使用声明"，这轮换角度：
**类型里定义的字段，有没有人写、有没有人读**。

结果分两类：

- **看起来零引用、其实没问题**：人设模板的 `identityAge`、`personalityValues`、
  `habitsGestures`、`speechVocab` 等字段统计出来是 0 处引用，
  但 `PromptBuilder` 是**按 `PROMPT_FIELD_GROUPS` 动态遍历**渲染的
  （`tpl[f.key]`），字段名根本不会出现在代码里——不是断链。
- **真的只读不写**：`ICharacterSprite.thumbnailUrl`。立绘查看器的缩略图条
  已经在读它（`sprite.thumbnailUrl ?? sprite.url`），但**从来没有任何地方写入**，
  于是永远回退到完整立绘：八张 512px 的图当 44px 缩略图用，
  打开查看器时全都要按原尺寸解码。

## 二、做法

- 角色编辑器上传立绘时，**顺手生成一张 96px 缩略图**写进 `thumbnailUrl`；
  缩略图生成失败不影响主立绘（渲染时本来就有回退）。
- 老数据（已有立绘没有缩略图）继续回退用大图，不需要迁移。
- 顺手修了上一轮引入的布局 bug：舞台里的两层图是绝对定位的、不参与高度计算，
  只给 `min-height` 会让大图溢出舞台、压到缩略图条上；
  改成显式给定舞台尺寸 + `object-fit: contain`。

## 三、验证

```bash
pnpm test           # 556 个测试全绿（53 个文件，新增 2 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 481.23 KB / gzip 159.97 KB）
```

组件测试 2 条：上传立绘时同时生成 512 与 96 两档（mock 掉 canvas 部分，
断言两档的调用参数与落库字段）、缩略图条优先用 `thumbnailUrl`、
没配缩略图的回退大图。

浏览器实测（真机 canvas）：

```
上传一张 640×960 立绘 → 落库 url 84178 字符 / thumbnailUrl 2834 字符（差 30 倍）
打开查看器 → 缩略图条两张都是 2834 字符的小图
布局：大图 124~646 / 缩略图 685~729 / 导航 741~777 —— 三段无重叠
```

---

# 第五十三轮：立绘查看器（交叉淡入 / 缩略图条 / 键盘 / 中文情绪名）

## 一、四个具体不足

上一轮修了固定立绘的过渡，这轮看**点击头像弹出的立绘查看器**，发现四处：

1. 切换情绪是直接换 `src`——**硬切**（和固定立绘此前一样的问题）；
2. 情绪名显示的是**英文枚举**（`happy`、`sleepy`），而中文映射早就存在于
   角色编辑器的下拉选项里，只是没抽出来共用；
3. 没有任何**键盘操作**：看图时得来回移鼠标按 ◀▶；
4. 立绘多的时候（演示角色有 8 张）只能一路按 ◀▶，**没有跳转入口**。

## 二、做法

- **交叉淡入**：大图改成 `AnimatePresence` + `motion.img`（`key=url`），
  旧图淡出、新图叠上去淡入，250ms；
- **抽一份情绪中文名**：新增 `utils/emotionLabel.ts`（八种情绪 + 未知值原样返回），
  角色编辑器与查看器共用——顺带把编辑器里那份重复的映射删了；
- **键盘**：`←` `→` 切换情绪、`Esc` 关闭（监听 window，卸载时移除）；
  首尾不越界（与按钮的禁用逻辑一致）；
- **缩略图条**：大图下方一排 44px 小图，当前项高亮，点击直接跳转，
  `aria-label` 用情绪中文名；
- **布局**：查看器原本是横向 flex，插入缩略图条后会**挤进大图和导航之间**，
  改成纵向（大图 → 缩略图条 → 导航），大图最大高度相应收到 58vh。

## 三、验证

```bash
pnpm test           # 554 个测试全绿（53 个文件，新增 10 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 481.17 KB / gzip 159.95 KB）
```

新增测试：SpriteViewer 7 条（初始立绘 + 中文情绪名、键盘左右与 Esc、首尾不越界、
缩略图跳转与高亮、缩略图无障碍名、固定立绘开关回调、箭头按钮禁用）+
emotionLabel 3 条（八种情绪都有中文且不等于枚举值、未知值原样返回、
映射表覆盖全部枚举——将来加情绪时这条会先失败）。

浏览器实测：

```
点头像 → 查看器打开：大图 349px + 缩略图条 8 张 + 导航，三段纵向排列不重叠
按 → ：平静 → 开心　　按 Esc：关闭
点第 6 张缩略图：切到「惊讶」并把该缩略图高亮
```

（测试断言也顺手改了：交叉淡入时新旧两张会同时在 DOM 里，
所以从"第一张 img 的 src"改成"舞台里存在目标图"。）

---

# 第五十二轮：固定立绘的过渡（立绘方向）

## 一、问题

固定立绘此前是"硬切"的：

- **开关固定立绘**：直接挂载/卸载 DOM，立绘"啪"地出现或消失；
- **情绪切换**：直接换 `src`，一张表情瞬间变另一张。用 data URL 时尤其明显
  （解码要一帧），看起来像闪了一下。

## 二、做法

- **显隐改过渡**：立绘图层在"角色有立绘"时**始终渲染**，用 `data-visible`
  控制——开启时淡入 + 从下方 14px 轻微上浮，关闭时淡出并 `pointer-events: none`。
  没有立绘的角色仍然完全不渲染这个图层。
- **情绪改交叉淡入**：记住上一张的 URL，切换时渲染两层（旧图 `--out` 淡出、
  新图 `--in` 淡入，300ms），动画结束后旧图层被移除（计时器 300ms 与 CSS 对齐）。
  新图带 `key={url}`，保证同一张图重复出现时也会重新触发入场动画。
- **拖动反馈**：拖动中轻微放大到 1.02（原来只有透明度变化）。

## 三、验证

```bash
pnpm test           # 544 个测试全绿（50 个文件，新增 5 个）
pnpm -r typecheck   # 4 包全绿（含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 480.28 KB / gzip 159.66 KB）
```

组件测试 5 条（新增 `PinnedSprite.test.tsx`）：没有立绘不渲染、关闭时图层在但
`data-visible=false`、开启时按情绪选图、情绪切换时新旧两层叠着并在 300ms 后只剩一层、
情绪没有对应立绘时退回第一张。

浏览器实测：

```
开启固定立绘 → data-visible="true"，1 张图（左下角，内置 SVG 立绘）
注入一条 emotion=happy 的角色消息 → 切换进行中：2 张图（--out 旧图 + --in 新图）
                                 → 400ms 后：1 张图
关闭固定立绘 → data-visible="false"、opacity=0、pointer-events=none（是淡出，不是卸载）
```

---

# 第五十一轮：死代码清理（把"未使用检查"变成项目标准）

## 一、体检怎么做的

上一轮查的是"store action 有没有调用方"，这轮换个更彻底的角度：
**直接让编译器把"声明了但没人用"的东西全部报出来**。

```
tsc --noEmit --noUnusedLocals --noUnusedParameters
```

一次扫出 18 处。逐条看过之后分两类：

- **纯残留**（删掉即可）：未使用的类型导入、局部变量、测试里的 `vi`
- **会误导后人的死设计**（重点）：
  - `RealismEngine.lastChunkText`：注释写着"用于计算下一个 chunk 的间隔"，
    也确实在每轮被赋值，但**从来没有被读过**——看上去像个已实现的时间轴优化，
    实际上间隔完全由 `TypingSimulator.plan()` 自己算
  - `MessageStatus` 的 `isSelf`：组件接了这个参数却从不使用（状态栏只区分
    loading/error/sent），而调用方也从来没传过
  - `ChatSessionView` 订阅了 `pendingCount` 并一路传下去，组件里却没有任何地方读它
    ——白让会话视图在占位计数变化时重渲染
  - `WeChatShell.onApiKeyChange`：解构了但没使用（API Key 早已改由
    SettingsPanel 直接写 store），App 也从没传过
  - `SemanticBuffer` 把构造参数存成私有属性却不再使用；`chatStore` 里
    构造了一个从不读取的 `Map`

## 二、做法

该删的删干净，然后在 `tsconfig.base.json` 里**永久开启**：

```json
"noUnusedLocals": true,
"noUnusedParameters": true
```

以后 `pnpm -r typecheck` 就会拦住这类问题——不需要额外工具链，
也符合项目"不引入新依赖"的定位。回调签名里必须保留的参数照旧用 `_` 前缀。

## 三、验证

```bash
pnpm test           # 539 个测试全绿（50 个文件）
pnpm -r typecheck   # 4 包全绿（现在含未使用检查）
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 479.75 KB / gzip 159.52 KB）
```

浏览器回归：发一条消息 → 3 条气泡正常交付、在场状态与上下文徽标正常、
无页面报错、立绘占位头像显示正常。

---

# 第五十轮：删除角色（store 里有能力，界面上没入口）

## 一、体检发现的断链

继续做"断链体检"。先查了引擎事件的覆盖面——9 种 `IEngineEvent`
（typing-indicator / context-usage / chunk-scheduled / char-reveal-plan /
chunk-delivered / recall / presence / lifecycle / error）在 chatStore 里
**全部有处理分支**，没有漏。

接着把 sessionStore 的 43 个 action 挨个查调用方，`deleteCharacter` 露了出来：
**全项目只有测试调用它**。也就是说——删除角色的完整逻辑（清理长期记忆、
世界书、关联会话与通讯录、复位活跃会话）早就写好，还有三条测试覆盖，
但用户在界面上**根本删不掉角色**，只能去改数据备份文件。

## 二、实现

- 角色编辑器底部新增「删除角色」：只在编辑已有角色时出现，
  放在最左、用警示色，与右边的"保存角色"拉开距离
- 二次确认把"会一起删掉什么"写清楚：
  「确认删除角色「林笑笑」？它的聊天记录、长期记忆与世界设定会一起删除，且无法恢复。」
- 删除后自动关闭面板；store 侧连带清理与 `activeSessionId` 复位都是现成的

## 三、验证

```bash
pnpm test           # 539 个测试全绿（50 个文件，新增 4 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 479.98 KB / gzip 159.55 KB）
```

组件测试 4 条：新建模式没有删除入口、确认后角色与数据一起删且面板关闭、
点取消什么都不删也不关闭、确认文案里带着角色名/"聊天记录"/"无法恢复"。

浏览器实测：

```
通讯录角色：林笑笑 / 苏晚晴（先点开林笑笑建出一个会话）
编辑器底部出现「删除角色」→ 点击
确认框：确认删除角色「林笑笑」？它的聊天记录、长期记忆与世界设定会一起删除，且无法恢复。
删除后：characters = [苏晚晴]、sessions = 0、activeSessionId = null、编辑器已关闭
```

## 四、体检结论（本轮范围）

- 引擎事件 → UI 处理：**9/9 覆盖**
- sessionStore action → 调用方：只有 `deleteCharacter` 断链（本轮已修）
- 角色档案里的可调参数（作息、打字速度、碎片化、犹豫、错别字、贴图频率、
  回复风格、立绘位置）都已有消费方——其中贴图频率是上一轮修的

---

# 第四十九轮：用户发图功能 → 按范围要求**撤回**（记录留档）

> **结论先说**：这一轮实现了"用户发图给角色看"并真机验证通过，
> 但随后用户明确范围——**只做文本对话相关，除立绘外的视频/语音/图片信息一律不做**。
> 因此该功能已**整体撤回**，代码回到"图片消息只有占位泡"的状态。
> 本记录保留，用于说明做过什么、撤回了什么，避免以后重复踩坑。

## 一、当时做的事（现已全部移除）

**先探能力**：（现场用 zlib 编码"左红右蓝"的 PNG 问颜色，Agnes 答对，说明支持视觉输入）

**实现**：
- `ILLMMessage.images` + OpenAI `image_url` / Claude base64 source 两套多模态序列化
- `toLLMHistory` 把图片带进历史、`buildPrompt` 的 `attachments` 挂在当前消息上
- 输入栏图片按钮、选图后压 JPEG（最长边 800）、内联保存、队列支持图片
- 图片气泡 240px 缩略图 + 点击看大图
- 后台整理也带上窗口内的图片（让"用户发过一张海边照片"能进记忆）

**验证**（当时都通过了）：真实引擎 + 真实模型 + 一张图，角色答出
「红色、蓝色，还有中间的白色」；浏览器实测选图发送、缩略图、点击放大均正常。

## 二、撤回清单

```
core：llm/types.ts 的 images 字段、OpenAIAdapter/ClaudeAdapter 的多模态转换、
      ContextTrimmer 的图片 token 估算、toLLMHistory 的图片透传、
      PromptBuilder 的 attachments、RealismEngine 的 pendingAttachments、
      MemoryDigest 的 attachments（及 collectDigestAttachments）
ui  ：chatStore 的 addImageMessage/enqueueUserImage、InputBar 的图片按钮、
      Icon 的 image 图标、imageFile 的 readChatImage、
      MessageBubble 的图片查看器、global.css 的缩略图与大图层
app ：App 的 handleSendImage 与 startUpload 参数链、digestRunner 的图片透传
探针：agnes_vision_probe / agnes_vision_chat_probe / agnes_digest_vision_probe
```

**保留的**：修 bug 的成果——秒回/降级路径此前交付的是原始 `fullText`，
首行情绪标签（如 `neutral`）会漏进气泡；这条修复与图片无关，予以保留，
回归测试留在 `StickerDelivery.test.ts`。

## 三、教训

"能不能做"和"该不该做"是两回事：模型支持视觉输入、技术上完全可行，
但产品范围由用户定。以后遇到这类**超出既定范围**的新方向，
先确认再动手，不要因为"顺手就能做"就先做出来。

---

# 第四十八轮：贴图真的会发（stickerFrequency 从"配了没用"到接上）

## 一、怎么发现的

这轮先做了一次"断链体检"：上一轮修过的 `setEmotion` 曾经全无调用点，
说明这类问题容易藏。于是把角色档案里可调的参数挨个查了一遍消费方，
`stickerFrequency` 立刻暴露出来——

**角色编辑器有滑杆、六个性格预设各带默认值（0.05~0.5）、消息渲染层支持贴图、
调试入口能造贴图消息，唯独引擎从不读这个配置**：角色永远不发贴图。

## 二、实现

- `packages/core/src/sticker/Stickers.ts`：内置贴图目录（八种情绪各一张，
  带微信风格的 `[开心]/[流泪]` 表情名）+ `pickSticker(emotion)`
- 引擎收尾时按概率追加：新增 `maybeDeliverSticker()`，挂在 `finishRun()` 入口——
  **为什么放在收尾**：拟真模式下气泡是逐条弹出的，中途插一张贴图会打断节奏，
  而且模型还没说完、情绪未定型；贴图跟在"这一轮说完了"之后最自然
  - 每轮最多一张；本轮没有任何文字（空回复）则不发
  - 情绪取本轮最后一条气泡的情绪（模型自标的优先）
  - `chunkSequence` 排在文字之后，保证渲染顺序
- `packages/ui-wechat/src/utils/placeholderSticker.ts`：按 stickerId **现画 SVG**
  （圆底渐变 + 五官），不依赖任何外链与图片文件
- 渲染层：贴图气泡改成真图（`<img>` 指向内置 SVG），`alt/title` 保留表情名；
  贴图不带气泡底色（微信里表情就是一张图）

顺手修掉一个真 bug：秒回路径的 `deliverInstantMessage` 没有累加"已交付条数"，
导致收尾时被误判成空回复、贴图永远不发——正是新写的测试第一条就红出来的。

## 三、验证

```bash
pnpm test           # 534 个测试全绿（50 个文件，新增 13 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 479.63 KB / gzip 159.44 KB）
```

新增测试：StickerDelivery 5 条（频率 1 必发、频率 0 不发、每轮最多一张、
情绪跟随模型自标、"text → sticker"的交付顺序）+ Stickers 4 条（八种情绪全覆盖、
ID 唯一、按 ID 查找、包 ID 固定）+ placeholderSticker 4 条（每个 ID 都能画出 SVG、
不同 ID 不同表情、未知 ID 兜底、自包含不引用外部资源）。

浏览器实测（把两个角色的 `stickerFrequency` 设为 1.0）：

```
发一条消息 → 本轮 19 条文字气泡逐条弹出 → 最后一条是 sticker
贴图节点：alt="[思考]"　96×96　src=data:image/svg+xml;charset=utf…
```

顺带记一笔排查过程：第一次没看到贴图，先怀疑引擎没拿到新档案——
读出来的 `engine.profile` 却是**另一个角色**（我点开的会话不是我改频率的那个），
换成两个角色都设 1.0 之后又发现 `phase` 还是 streaming（拟真模式下长回复
要 20 多秒才交付完）。结论：逻辑本身没问题，是验证步骤先后顺序的坑。

---

# 第四十七轮：AI 生成角色卡（一句话到一张能开演的卡）

## 一、为什么做

新建角色面板此前是一整页空表单：名字、简介、开场白、人设提示词全靠自己写。
实际用下来，多数人卡在两点——

1. 写得空泛：满屏"温柔善良""气质优雅"，真正有用的具体细节一个没有；
2. 写不出示例对话：而示例对话恰恰是社区公认最能定住语气的塑形手段
   （本项目的人设模板也专门有这一栏）。

于是把"从一句话生成一张角色卡"做进面板：生成 → 填表 → 用户确认才落库。

## 二、实现

- `packages/core/src/character/CharacterForge.ts`（纯函数）
  - `buildCharacterGenPrompt`：system 里钉死 JSON 结构与写作要求
    （具体优于华丽、不要"她是一个温柔善良的女孩"、示例对话要像微信聊天、
    署名固定为「用户：/角色名：」），并给出 6 个可选性格原型
  - `parseCharacterGenResult`：剥离围栏、截取首尾花括号、字段归一化，
    archetype 不在白名单退回 `gentle`，缺 bio 时从背景抽一句话补，
    超长字段按上限截断（600/400/300/200/800 字），
    **只有名字没有内容判失败**（那是白跑一趟）
  - `formatDraftAsPromptText`：把草稿拼成模板正文，空字段不产生空标题
- 组装层 `apps/web/src/characterGenRunner.ts`：复用当前 LLM 适配器，
  120s 超时、失败给可读中文提示、演示（mock）模式下直接说明"先配 API"
- UI：角色编辑器顶部新增「AI 生成角色」区块（**只在注入了生成器时渲染**），
  生成成功后填进名字/简介/开场白/提示词四处，并回显"已生成「XX」，可以直接用，也可以改"
- 生成不自动保存：生成的东西总要让人过一眼、改两笔再落库

## 三、验证

```bash
pnpm test           # 521 个测试全绿（47 个文件，新增 18 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 475.60 KB / gzip 157.92 KB）
```

新增测试：CharacterForge 13 条（指令内容、标准/围栏/夹在废话里的 JSON、
坏输入、白名单兜底、bio 兜底、超长截断、只有名字判失败、模板文本拼装）+
CharacterEditor 组件 5 条（未注入不显示入口、注入后显示、生成成功填表、
失败只提示不动表单、空描述按钮禁用）。

真机验证（`work/agnes_character_gen_probe.mts`，三种描述各一次）：

```
#1 高中同桌，傲娇但细心，喜欢猫      → 解析成功 6/6 字段，0 套话，开场白「啧，你又把咖啡洒桌上了是吧？…」
#2 深夜电台主播，声音温柔，自己有失眠症 → 解析成功 6/6 字段，0 套话，开场白「喂……是我，林晚。现在凌晨一点四十三分…」
#3 走江湖的郎中，嘴贫但医术好，爱收集偏方 → 解析成功 6/6 字段，0 套话，开场白「哎哟我可算找着您了！…」
汇总：解析成功 3/3
```

第一轮实测发现示例对话的署名在"你／用户"之间摇摆，已在指令里钉死格式
（「用户：/角色名：」）后重跑。

浏览器实测：新建角色面板出现「AI 生成角色」区块（说明 + 输入框 + 生成按钮），
填入描述点生成——演示模式下给出「当前是本地演示模式，先在设置里配好 API 才能用 AI 生成」，
表单不受影响。成功路径由组件测试覆盖（注入生成器 → 填表断言）。

---

# 第四十六轮：世界书（社区卡的 character_book 真正用起来）

## 一、为什么做

上一轮导入社区卡时会提示「含 N 条世界书条目，暂未导入」——那是个未完成的承诺。
世界书是这类卡片的灵魂：作者把"只有聊到某个话题时才需要知道的设定"拆成条目，
每条带触发关键词。它和「长期记忆」的区别很清楚：

- 记忆是**对话中产生**的（关于用户的偏好、经历）；
- 世界书是**作者预先写死**的（世界观、配角、地点、道具）。

所以两者分开存储、分开检索，但共用同一段动态上下文与预算控制。

## 二、实现

- `packages/core/src/lore/LoreKeeper.ts`（纯函数）
  - `selectLoreEntries(query, entries, { maxEntries, maxChars })`：
    常驻条目无条件入选；其余按关键词命中（大小写可配）；
    `selective` 条目要次级关键词同时出现；按 `insertion_order` 升序；
    条数与字数双重预算，超预算的条目**跳过但继续找更短的**；
    若所有候选都超预算，则截断最靠前的那条（宁可给一点线索也不空手）
  - `renderLoreSection`：渲染成 `【世界设定】` 段落，无内容时不产生空段落
  - `loreEntriesFromBook`：社区卡 `character_book` 归一化，坏条目逐条丢弃
- `IPromptExtras` 新增 `lore`；动态段落顺序确定为 **设定 → 记忆 → 剧情**
  （剧情是"此刻正在发生什么"，离提问最近）
- 上下文占用明细新增「世界设定」一行
- 存储：`sessionStore.loreEntries`（按角色归属）+ 增删改 action；
  删除角色时一并清理；hydration 清洗逐条校验；备份导入导出同样带上
- UI：提示词浮层从三 Tab 变四 Tab，新增「设定」页——
  条目列表（关键词 / 内容 / 备注）、单独启用停用、编辑、删除、搜索、手动新增
- 导入社区卡时把 `character_book` 一并落库；导入提示从
  「含 N 条世界书条目，暂未导入」变成「含 N 条世界设定」

**刻意不做**：不实现社区卡的 `recursive_scanning`（条目内容再触发别的条目）。
长设定集上会滚雪球，注入量不可控——只扫一遍。

## 三、验证

```bash
pnpm test           # 503 个测试全绿（45 个文件，新增 30 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 470.69 KB / gzip 155.41 KB）
```

新增测试：LoreKeeper 16 条（命中/大小写/常驻/selective/停用/排序/条数预算/
字数预算与"跳过长的给短的让位"/兜底截断/渲染/归一化）、
PromptBuilder 2 条（注入顺序、无命中不产生空段落）、
LoreTab 组件 9 条（空态、渲染、启用开关、删除、搜索、新增含 order 递增、
常驻模式、编辑、未选会话）、store 3 条（增删改清空、删角色清理、持久化字段）、
sanitize 1 条。

真机验证（`work/agnes_lore_probe.mts`，设定是模型不可能知道的 1968 年 Beatles 白专）：

```
A 无世界书（改造前）0/2：张五二年的爵士 / 邓丽君《空港》，七几年的原版
B 有世界书（改造后）2/2：1968年的披头士白色专辑。不外借，只能在店里听。
```

浏览器实测（导入带 3 条世界书的社区卡）：

```
导入提示：已导入角色「林见夏」（社区卡 V2，含 3 条世界设定）
设定 Tab：3 条条目（含备注"镇店之宝"）、第 3 条卡片里写的是禁用 → 停用态显示
发送「你店里最老的那张黑胶是什么？」
→ 上下文占用明细：角色卡 321 字 / 人设模板 224 字 / 世界设定 62 字 / 对话历史 26 字
   即：只有命中的那一条被注入，"咖啡"未提、禁用条目不入
```

---

# 第四十五轮：兼容社区角色卡（SillyTavern V1/V2，含内嵌 PNG）

## 一、先去 GitHub 找答案

"不断完善这个项目"最值得借鉴的外部实践，就是 AI RP 社区的事实标准：
SillyTavern 的角色卡规范（社区规范仓库 `malfoyslastname/character-card-spec-v2`，
SillyTavern 官方仓库引用它）。要点：

- **V1 字段**：`name` / `description` / `personality` / `scenario` / `first_mes` / `mes_example`
- **V2 包装**：`{ spec: "chara_card_v2", data: { …V1 字段, creator_notes,
  system_prompt, post_history_instructions, alternate_greetings, character_book,
  tags, creator, character_version, extensions } }`
- **占位符**：`{{char}}` / `{{user}}` / `<BOT>` / `<USER>` **大小写不敏感**替换
- **示例对话**：`<START>` 分隔轮次
- **`first_mes`**：必须是角色说的第一句话
- **PNG 嵌入**：JSON 经 base64 塞进 PNG 的 `tEXt` 文本块（关键字 `chara`）

顺带记一笔环境坑：本机 `raw.githubusercontent.com` 不可达（502），
改走 `api.github.com` 的 contents API 才拿到规范原文。

## 二、实现

- `packages/core/src/characterCard/TavernCard.ts`（纯函数，可单测）
  - `extractPngTextChunk`：手写 PNG 块解析（`tEXt` / `iTXt`），
    含签名校验、块长度越界保护、截断保护；压缩 iTXt 直接跳过（不引依赖）
  - `parseTavernCard`：JSON 文本或 PNG 字节都能吃，自动识别 V1 / V2
  - `substituteCardPlaceholders` / `convertMessageExamples`
  - `tavernCardToCharacterCard`：产出「咫尺」角色卡，
    复用既有的 `instantiateCharacterCard`（新 ID、重名后缀、模板去重复用）

| 社区卡字段 | 落点 |
|---|---|
| `description` | 模板「背景故事」＋ 角色一句话简介 |
| `personality` | 模板「性格描述」 |
| `scenario` | 模板「当前剧情节点」 |
| `mes_example` | 模板「示例对话」（去 `<START>`、替换占位符） |
| `first_mes` | 角色**开场白**（新会话第一句话） |
| `system_prompt` / `post_history_instructions` | 模板「额外补充」 |
| `creator_notes` / `tags` / `creator` | 不进提示词（那是给人看的元数据） |
| `character_book` | 暂不导入，但如实提示条目数 |

- 角色档案新增 `greeting`；新会话首次打开时由角色先说第一句话
  （`appendCharacterMessage` 注入，`phase` 保持 idle，不是一次"回复"）
- 角色编辑器新增「开场白」字段，可改可清空
- 通讯录导入入口同时接受 `.json` 与 `.png`

## 三、验证

```bash
pnpm test           # 472 个测试全绿（43 个文件，新增 17 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 462.39 KB / gzip 153.36 KB）
```

单测 15 条（V1/V2 解析、缺字段兜底、非卡 JSON 拒绝、PNG 提取含大小写与截断、
占位符替换、`<START>` 转换、映射与兜底名）+ chatStore 2 条（开场白追加不动 phase、
多次追加按序）。

浏览器实测（自己造了一张真实 PNG 卡：canvas 生成立绘 →
Node 侧插入带正确 CRC32 的 `tEXt` 块）：

```
导入提示：已导入角色「林见夏」（社区卡 V2，含 1 条世界书条目，暂未导入）
映射核对：简介「海边小镇的唱片店老板」、模板「林见夏 · 社区卡」、
          背景/性格/剧情/示例对话/额外补充全部就位，头像内联 27 KB
打开会话：第一条消息就是开场白「外面雨太大了，先坐会儿吧。要喝什么？我这儿只有红茶。」
          会话列表预览同步更新
编辑角色：编辑器里能读到开场白，保存后开场白与头像都还在
```

顺手修掉一个真 bug：角色简介没做占位符替换，导入后显示成
`{{char}}是海边小镇上一家旧唱片店的老板…`。

## 四、下一步可选

世界书（关键词触发的设定注入）已经和项目里"记忆检索"是同一类机制，
真要做得考虑与记忆/剧情共用注入预算，避免三套关键词系统各说各话。

---

# 第四十四轮：引用回复（长对话里角色不再"接不上话"）

## 一、为什么做

消息层此前只有一个操作：右键或长按**直接复制**。想引用某句话没有任何入口，
而引用在长对话里恰恰是刚需——

实测场景：角色很早说过「我家的猫叫团团，最近生病了」，这句话随后被
上下文裁剪挤出窗口（`trimContext` 后确认历史里已不含"团团"）。
用户问「它叫什么名字来着？」：

```
无引用 0/4：什么名字呀 / 没听你说呢 / 没太记得，你告诉我？
带引用 4/4：团团
```

也就是说，没有引用时角色是真的"接不上话"，只能反问或含糊过去。

## 二、做法

1. **类型**：`IMessageBase.quote?: IMessageQuote`，只存渲染与 Prompt 需要的
   摘要（messageId / senderName / preview）。不引用整条消息对象——
   被引用的消息可能已经因为"重新生成 / 撤回"从列表里消失，引用块仍要能显示。
2. **消息层**：长按 / 右键 → 操作菜单（复制 / 引用），与微信一致。
   复制从"误触即写剪贴板"改成菜单项；系统消息与撤回消息没有可读文本，
   不弹菜单。
3. **队列**：`queuedUserTexts` 从 `string[]` 改成 `{ text, quote }[]`——
   对方正在回复时发出的引用消息，引用必须跟着排队走，否则排队期间引用会丢。
4. **Prompt**：`formatQuotedText` 把引用拼进当前提问；
   `toLLMHistory` 让历史里的引用消息同样带引用行（历史回放也一致）。
5. **摘要**：`buildQuotePreview` 压平换行、截断到 80 字，
   避免引用块拖垮 Prompt 预算。
6. **界面**：输入栏上方显示引用条（发送或切会话即清空），
   消息气泡内显示引用块（发送者 + 摘要）。
7. **重发路径**：重新生成与编辑重发都会把原引用一起带回
   （`findLastReplyTurn` / `findLastEditableUserMessage` 现在返回 `quote`）——
   否则引用消息一旦"换一条"，就又变回失忆。

## 三、格式是测出来的，不是拍脑袋定的

第一版写成 `（引用 苏晚晴：「…」）`，真机跑出 2/3 命中，其中一次模型反过来
否认：「我没有说过这样的话哦，我不养猫呢」——它把被引用的内容当成了别人说的。

于是固定场景、只改写法做了对照（3 轮/组，`work/agnes_quote_format_probe.mts`）：

| 格式 | 命中 | 否认 |
|---|---|---|
| `（引用 苏晚晴：「…」）` | 2/3 | 1/3 |
| `（引用苏晚晴说过的话：「…」）` | 1/3 | 1/3 |
| `（引用我（苏晚晴）之前说过的话：「…」）` | 3/3 | 0 |
| `（引用消息 · 苏晚晴：「…」）` ← 采用 | 3/3 | 0 |

最后一种胜出：命中率与"我（角色）之前说过"并列最好，而且不假设被引用的是
谁说的话——用户引用**自己**发过的消息时同样成立。
换格式后重跑端到端探针，4/4 命中且无否认。

## 四、验证

```bash
pnpm test           # 455 个测试全绿（42 个文件，新增 22 个）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

新增测试：MessageBubble 组件 9 条（长按/右键开菜单、无 onQuote 只给复制、
系统消息不弹菜单、引用署名与截断、引用块渲染、复制写剪贴板），
chatStore 2 条（引用随消息上屏并排队、addUserMessage 落引用），
core 9 条（引用行转换、摘要压平与截断、纯文本提取），
regenerate 2 条（重新生成 / 编辑重发都要带回引用）。

浏览器实测（dev server + Playwright）：

```
长按角色气泡 → 菜单「复制 / 引用」→ 点引用
输入栏出现引用条：苏晚晴 / 我家的猫叫团团，最近生病了，我有点担心
输入「它叫什么名字来着？」并发送
→ 消息落库带 quote（messageId=m2、senderName=苏晚晴、preview=原话）
→ 气泡上方渲染引用块
→ 引用条自动清空（引用只对下一条生效）
→ 上下文占用徽标从 0 涨到 504 tok（引用确实进了 Prompt）
```

---

# 第四十三轮：会话列表批量管理（长按多选）

## 一、为什么做

清理入口此前只有一个：每个会话项的三点菜单 →「删除会话」，一次只能处理一个；
未读也只能一个个标。会话攒多了以后，想清理几条就得来回点好几轮菜单。

## 二、做法

- **长按任一会话项 500ms 进入多选**（与"长按头像切换固定立绘"同一手感），
  长按的那条自动选中；长按后浏览器补的那次 click 会被吃掉，不会顺带切换会话。
- 多选模式下**点击列表项 = 勾选 / 取消勾选**；头部显示「已选 N 项」+「取消」，
  底部（sticky）出现「标为已读」与「删除（N）」；三点菜单自动收起，
  避免两套操作叠在一起。
- **删除前仍然弹一次确认**（不可逆操作）；在确认框点取消会停在该模式，
  选择还在，可以接着改。
- store 补两个批量 action：
  - `deleteSessions` 走**一次** `pruneSessions`——批量删只写一次盘，
    也避免 `activeSessionId` 被反复重置；
  - `markSessionsAsRead` 只清未读，不碰 `updatedAt` 与没选中的会话。

## 三、验证

```bash
pnpm test           # 433 个测试全绿（40 个文件）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 454.02 KB / gzip 150.16 KB）
```

组件测试 `SessionList.test.tsx` 8 条：置顶排序、点击切换会话、长按进入多选
（且不切换会话）、多选下点击是勾选、批量标已读后退出多选、确认删除、
取消删除、退出多选后恢复点击切换。
store 测试 2 条：批量删除清干净派生数据且不动未选中的会话；
批量标已读不动别人的未读与时间戳。

浏览器实测（dev server + Playwright，注入两个会话）：

```
长按第一项  → 头部「已选 1 项」、底部「标为已读 / 删除（1）」、activeSessionId 仍为 null
点第二项    → 「已选 2 项」、底部「删除（2）」
标为已读    → 两个会话 unreadCount 归 0，多选界面退出
点删除      → confirm「确认删除选中的 2 个聊天？聊天记录会一起删除。」
              取消 → 两个会话都还在，仍在多选模式
              确认 → sessions 清空、activeSessionId 复位、列表回到空态
```

---

# 第四十二轮：记忆 / 剧情面板的组件回归测试

## 一、为什么要补

38 个测试文件里只有 1 个是组件测试（ErrorBoundary），
记忆与剧情这种"用户天天要看、又直接改 store"的面板一直只有浏览器手测。
手测过的东西下次重构就会悄悄坏掉——比如搜索框改个名字、删除按钮少传一个
`characterId`，只有真去点才会发现。这轮把两个面板都补上。

## 二、记忆面板：9 个用例

`MemoryTab` 是纯受控组件：所有状态都在 `sessionStore` 里，组件只负责渲染和
派发 action。所以测试**直接读写真实的 store**，不 mock store——
这样断言到的就是用户实际看到的因果链（点了删除 → store 里那条真的没了）。

新增 `packages/ui-wechat/src/__tests__/MemoryTab.test.tsx`，9 个用例：

| 用例 | 断言的东西 |
|---|---|
| 没有选中会话 | 显示"请先选择一个会话"，不是空面板 |
| 没有记忆 | 显示空状态文案 + "立即整理"入口 |
| 正常渲染 | 内容 / 类别标签（偏好）/ 重要度星数 / 关键词 |
| 取代记录 | 显示"取代了：xxx"（自动整理改了什么，用户看得见） |
| 整理失败 | `role="status"` 提示里含失败原因与"有新消息时会自动重试"，并显示待整理条数 |
| 搜索过滤 | 输入"猫"后咖啡那条消失、猫那条留下 |
| 删除 | 点删除按钮 → store 里该角色记忆变空数组 |
| 置顶 | 点置顶 → `pinned` 变 true，按钮语义变成"取消置顶" |
| 立即整理 | 点按钮 → 注入的 `onRunDigest` 回调被调用一次 |

顺带修掉 `MemoryTab.tsx` 里 memo 内部几个 hook 的缩进错位（纯格式）。

## 三、剧情面板：9 个用例（并测出两个真缺陷）

新增 `packages/ui-wechat/src/__tests__/PlotTab.test.tsx`：

| 用例 | 断言的东西 |
|---|---|
| 没有选中会话 | 显示"请先选择一个会话" |
| 没有状态卡 | 时间线空状态文案 + 回滚 / 清空按钮 disabled |
| 已有状态卡 | 五个输入框回填、线索与关系按行还原、事件倒序、手动徽标、星数 |
| 保存状态卡 | 写回 store，线索按行拆分且丢掉空行，关系带上角色 ID |
| 手动加事件 | 回车提交 → store 里 `manual: true`、输入框清空、列表出现 |
| 无卡时手动加事件 | 首次整理之前记的事件也要落地 |
| 删除事件 | 那一条从 store 与列表里一起消失 |
| 回滚 | 退到上一版章节，表单跟着刷新（事件不参与回滚） |
| 清空 | 本会话剧情状态整体移除，表单回到空白 |

写第六条用例时它是红的，由此挖出两个真问题：

**缺陷 1：首次整理之前手动记的事件会被丢掉。**
`addPlotEvent` 在 `state.plotStates[sessionId]` 不存在时直接 `return {}`——
用户在剧情 Tab 里输入事件、回车，输入框清空了，store 里什么都没发生，
而且**没有任何报错**。修法与 `updatePlotCard` 的既有语义对齐：
没有状态卡就先用 `createEmptyPlotState` 建一张空卡。

**缺陷 2：手动事件不遵守设置里的保留上限。**
`addPlotEvent` 里写死 `slice(-100)`，而设置面板的「事件时间线保留条数」
只作用于后台整理（`applyDigest` 读 `digestConfig.maxEvents`）。
上限调到 10 条后，手动事件仍能堆到 100 条。改为同样读配置，
并用 `Math.max(1, ...)` 兜住导入数据里的脏值。

两条都补了 store 级回归测试（`sessionStore.test.ts`）。

## 四、验证

```bash
pnpm test           # 423 个测试全绿（39 个文件，新增 MemoryTab 9 + PlotTab 9 + store 2）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 451.61 KB / gzip 149.43 KB）
```

---

# 第四十一轮：会话级数据的清理收敛 + 累计 token

## 一、体检查出两处清理遗漏

最近几轮往 sessionStore 里加了两个"按会话归属"的映射
（`contextUsageBySession` 上下文占用、`digestFailures` 整理失败原因），
这次清理时发现：

- `deleteSession` 只清了上下文占用，**漏了整理失败原因**；
- `deleteCharacter` 两个都没清，而且删掉活跃会话所属的角色后
  `activeSessionId` 会指向一个不存在的会话（界面靠"查不到就显示空态"
  蒙混过去，但状态是脏的）。

根因不是手滑，而是**结构问题**：这类清理散落在两个 action 里，
每加一个会话级字段就得记得改两处。于是收敛成一个 `pruneSessions(state, 判定)`，
把 `sessions / sessionRuntimes / plotStates / digestCursors / drafts /
contextUsageBySession / digestFailures / tokenUsageBySession` 与
`activeSessionId` 一次性处理干净，两条删除路径都走它。

补了两条测试，逐个断言这八项都被清掉（当初有这样的测试就不会漏）。

## 二、顺带做：累计 token 用量

上下文占用徽标此前只显示"本次请求花了多少"，回答不了
"这个会话一共花了多少"。新增 `tokenUsageBySession`（请求数 + token 估算），
由 `context-usage` 事件累加；**会持久化**，否则跨刷新累计没有意义。

明细面板里多一行「本会话累计约 X tok（N 次请求）」，用虚线与时序信息隔开。
清理器（`sanitizeSessionSnapshot`）同步覆盖了这个新字段，
结构不对的条目照样丢掉——这正是收敛清理之外的另一处"每加字段就要照顾到"的地方。

## 三、浏览器实测

```
第一次请求 → 累计 { requests: 1, tokens: 420 }
面板显示：本次请求 2 条消息 · 约 420 token
          角色卡 314 字
          本会话累计约 420 tok（1 次请求）
再发一条   → { requests: 2, tokens: 933 }
刷新页面   → 仍为 { requests: 2, tokens: 933 }（持久化生效）
```

## 四、覆盖率复核

顺手重跑了一次 `pnpm test:coverage`：语句覆盖率 **92.24%**
（README 里写的"约 92%"仍然准确，不用改）。

## 五、验证

```bash
pnpm test           # 403 个测试全绿（新增 4 个：清理完整性 2 + 累计用量 2）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 451.55 KB / gzip 149.42 KB）
```

---

# 第四十轮：后台整理失败要说话

## 一、问题：整理失败是全静默的

`runDigest` 失败时只返回 `null`，调用方一律静默跳过。用户看到的是
记忆面板里"待整理 N 条"越积越多，**完全不知道卡在哪一步**：
模型没返回？JSON 解析失败？网络问题？还是模型太慢超时了？

## 二、做法

1. **core**：`runDigest` 新增可选的 `onFailure(reason)`，
   区分六种原因——`no-history` / `empty-output` / `unparsable` /
   `adapter-error` / `timeout` / `aborted`。
   主动中止（切会话、卸载）**不算故障，不回调**，免得制造假警报。
2. **store**：新增会话级的 `digestFailures`（记录时间 + 原因），
   与 `contextUsageBySession` 一样**不持久化**——它是诊断信息，
   刷新后重试一次就知道还失不失败了。
3. **组装层**：`digestRunner` 把 onFailure 落库，成功时清除；
   失败冷却与游标不动（下次有新消息自动重试）的既有行为不变。
4. **界面**：记忆面板显示一行琥珀色提示，把原因说成人话，
   并说明"有新消息时会自动重试"。

## 三、浏览器实测（真实失败路径）

把供应商设成一个连不上的地址，注入 5 条消息后点「立即整理」：

```
store: digestFailures[session] = { at: 1789275328339, reason: "adapter-error" }
界面: 当前角色：苏晚晴 · 共 0 条记忆 · 待整理 5 条
      上次整理没成功（刚刚）：调用模型时出错（网络或接口问题），有新消息时会自动重试
```

## 四、验证

```bash
pnpm test           # 399 个测试全绿（新增 3 个：失败原因分类 + store 记录/清除）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 450.74 KB / gzip 149.16 KB）
```

---

# 第三十九轮：渲染窗口可平移（修掉上一轮引入的跳转卡顿）

## 一、问题

第三十六轮把消息列表限制成"只渲染最近 300 条"后，留下了一个副作用：
**搜索跳到很老的消息时，窗口会从目标一路撑到最新**——3000 条会话里跳第 100 条，
等于把 2900 行全渲染出来，实测又是约 800ms 的卡顿。这是我自己引入的回归。

## 二、做法：窗口从"只能贴住尾部"变成"可以平移"

原来只有一个 `visibleCount`（渲染最后 N 条）。改成两个量：

- `windowSize`：窗口多大（默认 300，往上翻会变大）
- `windowEnd`：窗口右端在消息数组里的下标；`null` 表示**贴着最新**（聊天常态）

于是三件事都成立：

1. **日常聊天**：`windowEnd = null`，窗口跟着新消息走，贴底行为不变；
2. **跳到老消息**：把窗口平移过去，让目标落在中间——
   只渲染 300 行，而不是"目标到最新"那几千行；
3. **窗口内继续往下翻**：滑到底部时窗口右端再往下放一段（追加在下方不顶动视口，
   不需要补偿）；想看最新时有「回到最新 ↓」出口。

## 三、浏览器实测（3000 条会话，跳到第 100 条）

| 指标 | 改前（撑到尾巴） | 改后（平移窗口） |
|---|---|---|
| 高亮出现耗时 | ~800–1000ms | **239ms** |
| 渲染行数 | 3000 | **300** |
| 最长任务 | 788ms | **94ms** |

其余路径逐条验证：

```
跳转后窗口 = [0, 300)，目标居中偏差 1px（连测 9 次都是 1px）
往下滑两次 → 窗口 [900, 1200)，仍在偏移态（「回到最新」可见）
点「回到最新」→ 回尾部（gap=0、末行是第 2999 条）、按钮消失
新消息到达 → 仍贴底（gap=0、末行是新消息）
```

## 四、一处测量教训

第一次量居中偏差得到 **-379px**（目标在视口外），差点当成 bug——
其实是**测早了**：高亮在第一次定位尝试时就出现，而收敛循环还在跑。
等收敛完再量是 1px。教训：测"最终态"要等过程结束，或者连续采样看趋势。

## 五、验证

```bash
pnpm test           # 396 个测试全绿
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 449.91 KB / gzip 148.67 KB）
```

---

# 第三十八轮：模板的示例对话（few-shot）

## 一、动机

上一轮加了四个内置预设，但里面全是**形容词**（"轻快随意""克制温和"…）。
角色扮演社区公认最有效的塑形手段其实是**示例对话**：
写两三轮"用户说什么、角色怎么回"，比一整段描述管用得多。

## 二、做法

1. **契约**：`IPromptTemplate` 增加 `speechExamples`，并挂进
   `PROMPT_FIELD_GROUPS` 的「说话风格」组——编辑器的字段是照着这张表
   渲染的，加一行就自动多出一个多行输入框。
2. **PromptBuilder**：示例**单独成段**注入，不混进"字段: 值"的列表
   （多轮对话塞进一个列表项会读不清、也更容易被照抄），并明确要求
   "模仿语气、长度和节奏，**不要照抄**，也不要把示例当成刚发生过的事"。
3. **预设**：四个预设各配 2 轮示例对话，照各自的调性写。

## 三、真机 A/B（同一模板，只差示例）

示例刻意做成**可量化**的风格：每次不超过 10 字、结尾带波浪号。

| 组 | 平均字数 | 结尾带 `~` |
|---|---|---|
| A. 不带示例 | **72 字**（其中一条 246 字的清单式回复） | 3/5 |
| B. 带示例 | **27 字** | **5/5** |

字数差 2.7 倍，且 A 组出现了一条 246 字的"**放松型** - 找个郊外温泉…"
清单式回复（把聊天写成方案），B 组最长只有 44 字。
说明示例对语气与长度的塑形是**可量化**的，不是心理作用。

## 四、顺带修的一处默认值

编辑器打开时固定停在第 1 个分组（身份背景）。对于**只填了世界观**的模板，
用户看到的是几乎空白的一页，还得挨个点标签才知道内容在哪。
改成默认停在**第一个有内容的分组**。

> 注：四个内置预设其实都填了职业/关系，所以它们仍停在第一页——
> 这个修复针对的是用户自己只填了部分字段的模板。

## 五、浏览器实测

```
从预设建「校园日常」→ 打开编辑 → 切到「说话风格」：
  字段列表含「示例对话」，值是 4 行示例（用户/角色 各两轮），可直接编辑
```

## 六、验证

```bash
pnpm test           # 396 个测试全绿（新增 3 个：示例段渲染/省略/预设成对）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 448.56 KB / gzip 147.80 KB）
node ... work/agnes_examples_probe.mts   # 真机 A/B：72 字 → 27 字
```

---

# 第三十七轮：人设模板的内置预设

## 一、问题

新建提示词模板要手填「身份 / 性格 / 习惯 / 说话风格 / 世界观」五类字段，
新用户很难第一次就写出像样的设定——于是回复质量差，还以为是引擎不行。
空白模板对熟手友好，对第一次用的人是门槛。

## 二、做法

新增 `ui-wechat/src/utils/templatePresets.ts`：四个开箱可用的骨架

- **校园日常**：同校同学的松弛聊天
- **都市恋爱**：成年人的暧昧与试探，节奏克制
- **古风仙侠**：江湖与修行，措辞偏古但别文绉绉
- **悬疑探案**：一起查案，聊天里藏着线索

设计取舍：

- **刻意留白**：预设只写"世界观 + 说话风格"这类最影响回复质量的共性部分，
  姓名/身份/关系留给用户填——那才是每个角色不同的地方（有测试守着这条：
  `identityName` 必须为空）；
- **只收录契约里存在的字段**：有一条测试专门比对 `PROMPT_FIELD_GROUPS`，
  字段名写错等于白写，测试会当场发现；
- 面板里显示为一行虚线小胶囊，点一下就建好模板，重名自动加后缀
  （预设可以反复取用，想基于同一套设定捏两个角色）。

## 三、过程中改掉的一处语义含糊

第一版点预设后会**自动打开编辑器**。实测时发现两个问题：
预设入口被编辑器遮住，想连建两个得先返回；更要紧的是模板此时**已经落库**，
用户点"取消"会以为创建被撤销，其实还在。

改成"只创建、停在列表"——列表项本来就有编辑按钮，行为可预期。

## 四、浏览器实测

```
面板「提示词」Tab：从预设开始：校园日常 / 都市恋爱 / 古风仙侠 / 悬疑探案
点两次「古风仙侠」→ 模板列表 ["古风仙侠", "古风仙侠 2"]（重名自动加后缀）
绑定「古风仙侠」到角色 → 发消息 → 请求 system prompt 中确认包含：
  架空的东方修行世界 ✓   简洁克制 ✓   不要现代网络用语 ✓   有内力与剑法 ✓
```

## 五、验证

```bash
pnpm test           # 393 个测试全绿（新增 7 个：预设合法性/生成/不改常量）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 448.56 KB / gzip 147.80 KB）
```

---

# 第三十六轮：长会话性能实测与渲染窗口

## 一、先量再改：6000 条消息的压力数据

注入 1 个 3000 条的主会话 + 5 个 600 条的副会话（共 6000 条），
用浏览器实测：

| 指标 | 实测 |
|---|---|
| 滚动到中段 / 到底 | 8ms / 28ms，**零长任务** |
| 切进 3000 条会话 | **首帧 856ms，其中 788ms 是一个长任务** |
| 搜索（6000 条全量扫） | 常见词 198ms，罕见词 68ms |

滚动没问题（`content-visibility` 生效），但**打开长会话会卡住将近一秒**，
搜索每次按键也要上百毫秒。

## 二、长会话：限制渲染窗口

根因：`content-visibility` 只省绘制，**React 仍要为每条消息创建真实 DOM**
——实测 22,592 个节点，首帧提交 856ms。

做法：只渲染最近 **300** 条，滚到接近顶部时再往上加载 300 条，
并做视口锚定补偿（前插 DOM 会把视野顶下去）。

**这里踩了一个坑**：补偿一开始写成 `requestAnimationFrame`，
结果它在 React 提交**之前**就跑了，拿到的还是旧的 `scrollHeight`，
`scrollTop` 被算回 0——用户视野直接跳到刚加载出来的那批老消息上
（实测 `scrollTopAfterLoad=0`）。改成 `useLayoutEffect`（DOM 更新后）才对。
顺带加了"加载中不重复触发"，否则滚动事件连发会一次加载两批。

改进后：

| 指标 | 改前 | 改后 |
|---|---|---|
| 切进 3000 条会话首帧 | 856ms | **209ms** |
| DOM 节点数 | 22,592 | **2,416** |
| 最长任务 | 788ms | 209ms |

顺带确认了两条路径没被窗口限制弄坏：

- **往上翻加载更早**：300 → 600 行（只加载一次），`scrollTop` 自动补到 23568
  （≈加载前的 scrollHeight），视野留在原处；
- **搜索跳到很老的消息**：窗口从 300 撑到 3000、目标行高亮居中。

## 三、搜索：缓存小写文本

搜索是"每次按键扫全部消息"，大头在反复 `toLowerCase()`。
消息对象创建后不再变动，于是用 `WeakMap` 缓存小写结果。
配合上面的渲染窗口，实测从 **198ms → 9~19ms**。

## 四、验证

```bash
pnpm test           # 386 个测试全绿
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 445.82 KB / gzip 145.39 KB）
```

> 压力数据只存在于临时浏览器档案里，不影响仓库。

---

# 第三十五轮：记忆的「取代」记录可视化

## 一、问题

第二十一轮做了"记忆冲突更新"：用户改口时，旧记忆会被新记忆取代。
但那之后界面只显示新的那条——**用户看不到它替掉了什么**，
也就无从判断自动整理有没有改错。

这是记忆系统里最需要"可解释性"的一环：AI 悄悄删掉一条记忆，
而用户完全不知情，那比不删更让人不安。

## 二、做法

1. **契约**：`IMemory` 增加可选 `supersedesContent`（被取代的原文）与
   `supersededAt`（取代时间）。只用于界面回显，不参与检索与注入。
2. **合并**：`mergeMemories` 在真正发生取代时，把旧内容写进新记忆
   （合并进已有相似条目时同样写入）。没发生取代就不写这两个字段——
   避免界面上到处是空记录。
3. **界面**：记忆条目里用弱化的样式显示一行
   「取代了：用户喜欢喝咖啡」，左侧一道细竖线，鼠标悬停能看到取代时间。

## 三、浏览器实测

注入两条记忆（一条带取代记录、一条普通）后打开「记忆」Tab：

```
偏好 ★★★★  用户戒了咖啡，改喝茶
            取代了：用户喜欢喝咖啡        ← 弱化显示，title="取代于 2026/9/13 12:27:35"
            咖啡 · 茶   更新于刚刚

事实 ★★★    用户养了一只叫团子的猫        ← 没有取代记录，不显示空行
            猫   更新于刚刚
```

## 四、验证

```bash
pnpm test           # 386 个测试全绿（新增 2 个：写入/不写入取代记录）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 445.13 KB / gzip 145.17 KB）
```

---

# 第三十四轮：让模型自己标情绪（关键词法够不到的那部分）

## 一、动机

上一轮把情绪词表量到 68% 后，剩下的漏判基本都是**结构上抓不到**的：
"去湖边野餐吧"这种建议句整句没有情绪词，再补词就是过拟合。
所以这轮试"让模型自己在回复里标情绪"。

关键约束：标签必须在**开头**。放结尾的话，前面几个气泡早就发出去了，
立绘切换会迟到。

## 二、先测格式，再写实现

两种候选都各跑 10 条真机回复：

| 格式 | 合规 | 正文残留 |
|---|---|---|
| 行内 `[emotion:happy] 内容` | 5/10 | 2/10 |
| **独立首行只写情绪词** | **10/10** | **0/10** |

行内失败得很危险，比如 `[neutral:别闹啦～姐姐就是挺喜欢你这孩子的…]`
——模型把整句话包进了括号，照那个格式剥标签会**误删整条消息**。

而"首行情绪词"的解析天然保守：**首行不是那 8 个词之一就一个字都不动**，
最坏情况只是"没标签"，绝不吞内容。

## 三、实现

- `buildEmotionTagPrompt()`：写进 system prompt，要求第一行只写情绪词；
- `parseEmotionTagHead()`：纯函数，剥标签 + 取情绪；
- 引擎：流开头先攒着（见到换行、或超过 12 字符、或攒到 40 字符上限）再判断，
  标签剥掉、整轮沿用模型标的情绪；没标签就退回关键词推断。

## 四、真机端到端又抓出两个 bug

用**真实引擎 + 真实模型**（`work/engine_live_probe.mts`）跑，第一次结果是：

```
[我今天升职了！] 气泡 4 条，情绪 ["neutral","happy"]  ⚠️ 标签漏进正文
正文：happy 太棒了，恭喜姐姐！为你开心。
```

1. **模型常在标签前先吐一个换行**，而解析器要求标签在"第一行"→ 认不出来；
2. 更隐蔽的是：引擎"见到换行就判定"的规则**被那个前导换行触发了**，
   当场拍板"没标签"，后面真正的 `happy` 就直接透传进正文。

修法：解析器先剥前导空白再找标签；引擎也先剥空白再判断"第一行是否结束"，
判不出来就继续等（另有 40 字符上限防止模型一直吐空白卡住）。

修完复测：

```
[我今天升职了！]  气泡 2 条，情绪 ["happy"]     正文：哇！恭喜呀～替你开心
[周末去哪玩好呢]  情绪 ["thinking"]
```

第二例正是关键词法抓不到的建议句——现在靠模型自标拿到了。

## 五、环境备注

浏览器端的 Agnes 调用这次拿不到结果：页面里 `fetch` 到该域名
一律 `Failed to fetch`（连不带 Authorization 的请求也一样），
而同一时刻 Node 侧 HTTP 200、GitHub 在浏览器里也正常。
判断是当前沙箱里这个 Chromium 到该域名的网络路径问题，**不是应用代码问题**，
所以真机验证改走 Node（`work/engine_live_probe.mts`）。

## 六、验证

```bash
pnpm test           # 384 个测试全绿（新增 11 个：解析容错 + 引擎流开头处理）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 444.79 KB / gzip 145.08 KB）
node ... work/agnes_tag_probe.mts    # 模型合规 10/10、正文残留 0/10
node ... work/engine_live_probe.mts  # 真实引擎 + 真实模型：情绪正确、正文干净
```

---

# 第三十三轮：情绪词表的真机测量（32% → 68%）

## 一、问题：那张词表是拍脑袋写的

上一轮补上情绪推断后，词表是我凭印象列的。这类"看起来合理"的规则
最容易自欺——所以这轮先量再改。

## 二、方法：先固定语料，再谈分数

第一次测量直接拿"每次都不一样的模型输出"打分：两轮跑出 65% 和 55%，
**没法比较**（temperature 0.8，每次措辞都不同）。

于是改成固定语料法：给 Agnes 10 个情绪色彩明确的场景、收集 38 条真实回复、
人工标注期望情绪，之后每次改词表都在这 38 条上打分——结果可复现。

新增 `work/agnes_emotion_probe.mts`（采样）与 `work/emotion_eval.mts`（离线评估）。

## 三、结果

同一份 38 条语料上：

| 词表 | 命中 | 非 neutral |
|---|---|---|
| 最初那版（凭印象写的） | 12/38（**32%**） | 12/38（32%） |
| 现在这版（按真实回复补的） | 26/38（**68%**） | 26/38（68%） |

补进去的都是**通用线索**，不是针对某句话的特例：
`恭喜 / 庆祝 / 厉害 / 真棒`（喜悦）、`抱抱 / 心疼 / 辛苦 / 没关系 / 注意身体`
（关切）、`别熬 / 熬夜`（深夜）、`别乱说 / 别瞎说`（害羞）、`谁呀`（意外）。

## 四、顺带修掉一处规则设计问题

加完"别熬"后一条用例挂了：`"辛苦了。早点休息，别熬太晚。"`
按"最近命中"判成了**困倦**，但角色此刻的表情明明是**关切**。

于是加了一档**强线索**：`对不起 / 抱歉 / 抱抱 / 心疼 / 别难过 / 辛苦 / 陪你 /
注意身体` 这类"对对方处境的反应"优先判定，其余线索才按"最近出现"取。

## 五、天花板在哪（诚实说明）

剩下 12 条漏判，绝大多数是**关键词法本身抓不到**的：

- 4 条是"提议"（"去湖边野餐吧"），整句没有任何情绪词；
- 3 条是提问式好奇（"是你妈吗？还是你朋友？"）；
- 其余是纯靠语气的关切（"你也会好好的"）。

再往下补词就只能针对具体句子过拟合了。真要继续提升，正确做法是让模型
自己在回复里带情绪标签——但那样标签在**流末尾**才到，前面几个气泡已经发出去了，
和"逐个气泡给立绘"冲突，需要另设计。

## 六、验证

```bash
pnpm test           # 373 个测试全绿（新增 8 条真机说法的回归用例）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 443.13 KB / gzip 144.45 KB）
node ... work/emotion_eval.mts   # 语料 38 条：当前 68% / 最初 32%
```

---

# 第三十二轮：情绪接线（立绘终于会换表情了）

## 一、问题：情绪永远停在 neutral

查立绘切换链路时发现：`SemanticBuffer.setEmotion()` 在整个仓库里
**没有任何调用点**（只有测试用过它一次）。而引擎创建 chunk 时用的是
`this.chunker.next(segment, this.currentEmotion, ...)`，
`currentEmotion` 因此永远是初始值 `neutral`。

结果：我上一轮生成的 8 张情绪立绘**永远只会显示"平静"那一张**，
整套按情绪切换的设计是空转的。这跟之前"TypingSimulator 从未被调用"
属于同一类问题——功能写好了，但没人接线。

## 二、做法：情绪从文本推断，不额外调 LLM

新增 `core/src/EmotionInference.ts`（纯函数）：

- 关键词线索表，覆盖 happy / sad / angry / surprised / shy / sleepy / thinking；
- **只看最近 40 个字符**，且取**最后出现**的那条线索——
  当前情绪跟着"眼下这句话"走："唉，你开心就好"是欣慰不是难过；
- 拿不准就返回 neutral：**立绘切错比不切更出戏**；
- 特意不把"！"当惊讶线索（日常回复里到处都是感叹号）。

引擎侧：`handleDelta` 里累积本轮正文并调用 `buffer.setEmotion(...)`，
影响接下来那些气泡；秒回/降级路径也用 `inferEmotion(fullText)`。

## 三、验证

单元测试（6 条）：各情绪典型句、无线索时 neutral、感叹号不算线索、
同句多线索取最后出现、只看最近窗口、窗口长度合理。

引擎集成测试（2 条，用脚本化适配器）：

```
分片 ["哈哈，", "太好了，今天真开心！"] → 至少一个气泡 emotion=happy
分片 ["唉，", "我今天真的有点难过。"]   → 至少一个气泡 emotion=sad
```

浏览器实测：固定立绘初始显示"苏晚晴 · 平静"（直线嘴）；
注入一条 `emotion: "happy"` 的角色消息后，立绘立刻变成
**弯眼微笑的"苏晚晴 · 开心"**，且页面上仍只有 1 个固定立绘。

## 四、验证命令

```bash
pnpm test           # 372 个测试全绿（新增 6 个推断 + 2 个引擎接线）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 443.03 KB / gzip 144.33 KB）
```

---

# 第三十一轮：固定立绘可拖动（顺带修掉重复渲染）

## 一、问题

`AvatarWithSprite` 的文件头注释一直写着"固定立绘常驻，可拖动"，但实际：

- **不能拖**：CSS 是 `pointer-events: none`，根本收不到指针事件；
- **位置写死**：`left:16px; bottom:60px`，挡住哪块只能忍着；
- **状态放错地方**：开关是每个消息气泡各存一份 `useState`，
  从两个气泡各点一次就会冒出两个常驻立绘，刷新后还丢。

## 二、做法

1. **状态进档案**（`ICharacterVisualMetadata` 新增 `spritePinned?` 与
   `spritePosition?`）：全应用只有一份，能随角色卡带走。
   `MessageBubble` 不再本地存开关，只负责"点头像/长按 → 改档案"。
2. **常驻层只渲染一次**：固定立绘是屏幕级图层，改由会话视图渲染
   `<PinnedSprite>`（独立组件），并用 `createPortal` 挂到 `document.body`。
3. **拖动**：`pointerdown` 后用 window 上的 move/up 监听，
   **松手才写库**（拖动过程中每帧写 IndexedDB 太浪费）；
   `clampSpritePosition` 保证拖到哪儿都完整可见（窗口变小也能拉回来）。
4. 位置坐标沿用 CSS 语义：`x` 距左、`y` 距**下**，因此纵向拖动要取反——
   这条单独写了用例，容易写错。

## 三、验证过程中改掉的一次自造回归

第一版把开关挪进档案后，浏览器立刻量出 **`pinnedCount = 2`**：
每个角色消息气泡都渲染了一份常驻立绘。这正是"屏幕级图层不该由气泡拥有"
的体现，于是才有了上面的第 2 步。

## 四、浏览器实测

```
固定立绘初始：pinnedCount=1，rect=(16,340) 200×320（左下角）
拖动 (+300, -120)：rect=(316,220)，档案里存 {x:316, y:180}
                    （180 = 720-220-320，正是"距下边缘"）
重载页面：pinnedCount=1，仍在 (316,220)，档案值不变
层级核对：该点 elementFromPoint 命中的就是立绘自身，wrapper/img opacity 均为 1
```

## 五、验证

```bash
pnpm test           # 364 个测试全绿（新增 7 个：位置钳制与拖拽换算）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 442.35 KB / gzip 143.80 KB）
```

---

# 第三十轮：内置占位素材 + 两个 fixed 定位 bug

## 一、起因

上一轮截图里拍到实锤：演示角色的 6 张立绘缩略图**全是碎图标**——
它们指向的第三方图床已经不可达。新装用户打开应用，第一眼看到的就是破图。

## 二、做法：占位素材自己画

新增 `ui-wechat/src/utils/placeholderSprite.ts`（纯函数）：

- `buildPlaceholderSprite(name, emotion, hue)`：竖构图 5:8 的 SVG——
  柔和渐变底 + 简单人形 + 按情绪变化的眼睛/嘴形 + 底部"名字 · 情绪"标注；
- `buildPlaceholderAvatar(name, hue)`：正方形 SVG，取名字首字。

两者都是 data URL，**不请求网络、体积几百字节、结果确定**。
演示角色改用它们（每个角色 8 种情绪），不再有外链。
占位图是"诚实的占位"：一块底色加简笔人形，不假装是精修立绘；
想要真素材就在角色编辑器里选本地图片（上一轮已支持）。

顺带处理：名字里的 `& < > " '` 会做 XML 转义（否则画出的是坏 SVG）。

## 三、真机发现的两个布局 bug（都因为有了真图才暴露）

换了自带素材之后立绘终于能渲染了，于是量了一下位置：

**1. 立绘查看器被压成一条**

```
.wechat-sprite-viewer  position:fixed，实测 rect = (392,163) 872×58
图被顶到 y=-57（屏幕外），弹层只剩导航条
```

根因：弹层渲染在消息气泡里，而气泡外层是 framer-motion 的 `motion.article`
（带 transform）。**transform 祖先会让 `position: fixed` 退化成相对该祖先定位**，
于是"全屏弹层"变成了 872×58 的小方块。改用 `createPortal(overlay, document.body)`。

**2. 固定立绘跑到屏幕外**

同一个根因：`position:fixed; left:16px; bottom:60px` 本该贴左下角，
实测渲染在 **(408, -158)**，基本看不见。同样 portal 到 body。

修完后实测：

```
查看器 rect = (0,0) 1280×720（整屏），图 293×468 居中可见
固定立绘 rect = (16,340) 200×320（正好是 CSS 期望的左下角）
```

> 这两个 bug 在被发现前一直是坏的——只是以前图都加载不出来、弹层里没东西，
> 看着"像"没打开而已。

## 四、验证

```bash
pnpm test           # 357 个测试全绿（新增 7 个：占位素材转义/确定性/差异）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 440.32 KB / gzip 143.11 KB）
```

---

# 第二十九轮：头像与立绘本地化（角色卡自带素材）

## 一、问题

角色头像与立绘一直是**外链 URL**：演示角色指向第三方图床，
新建角色留空时还会自动拼一个图床链接塞进档案。后果：

- 图床挂掉 / 对方删图 → 只剩破图。**这轮截图里就拍到了实锤**：
  演示角色那 6 张立绘的缩略图全是破图；
- 角色卡不自包含：导出的卡片只带 URL，换台机器打开照样是破图；
- 想用自己的图，只能先找地方把图片传上去、再回来贴链接。

## 二、做法

1. **图片内联**（`ui-wechat/src/utils/imageFile.ts`）：
   `readImageAsDataUrl(file, maxEdge)` 读文件 → canvas 等比缩放 → PNG data URL。
   头像 256、立绘 512；同时缩放的代价换来自包含。
   输出统一 PNG：立绘常带透明通道，转 JPEG 会涂黑背景。
   `fitWithin()` 是纯函数（含"极端长图短边至少 1 像素""异常尺寸不产出 NaN"等边界）。
2. **编辑器**：头像区加「选本地图片 / 清除头像」，并说明"本地图片会缩放后内联保存"；
   新增「立绘（可选）」区——按情绪逐张管理（缩略图 + 情绪下拉 + 删除 + 添加）。
3. **不再兜底到第三方图床**：头像留空就显示首字占位块；新建角色也不会被塞外链。
4. **`supportsPinSprite` 改为按实际立绘推导**（有立绘才谈得上"固定立绘"），
   并保留原有的 `defaultSpriteAnchor`。

## 三、浏览器实测（含真实 PNG）

用 Node 现生成一张 800×400 的 PNG 做素材：

```
上传头像 → 预览立刻变成 data:image/png，naturalSize = 256x128（缩放生效）
添加立绘 → 列表 7 行（原有 6 张保留 + 新增 1 张内联图）
保存     → avatarUrl 是 data URL（1690 字符）、sprites=7、supportsPinSprite=true
重载页面 → 内联图与 7 张立绘全部还在（IndexedDB 持久化正常）
导出角色卡 → 9273 字节，头像与新增立绘都是内联 data URL，卡片自包含
```

顺带验证了上一轮留下的那处修复：**编辑角色不再清空立绘**
（本次编辑前就有 6 张，保存后仍是 6+1）。

## 四、验证

```bash
pnpm test           # 350 个测试全绿（新增 5 个：等比缩放边界）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 437.74 KB / gzip 142.12 KB）
```

---

# 第二十八轮：回复风格控制（文本对话 · 长度与旁白）

## 一、范围变更

用户中途把范围改成"语音不做，只做文本对话相关的（包括立绘等）"。
本轮启动了图片方向的改动后按新范围撤回（删掉 `imageFile.ts` 与其测试），
只保留一处**数据完整性**修复：角色编辑器保存时曾写死 `sprites: []`，
编辑一次角色就把立绘清空——改为沿用原有立绘。

## 二、问题

拟真引擎管的是"分几条气泡、打字多快"（排程），**没人管模型写多少字**。
system prompt 里只有一句泛泛的"回复自然口语化、不要写书面语或长段落"。
于是：想要极简回复的用户没法调；而在微信风格的聊天框里，
模型一旦写"（她抬头看了看窗外）"这类旁白，观感立刻垮掉。

## 三、做法

1. **契约**（`shared/types/src/character.ts`）：新增 `IReplyStyle`
   （`length: terse|short|medium|detailed` + `allowActions: boolean`）
   与默认值 `{ length: "short", allowActions: false }`，
   作为 `ICharacterProfile.replyStyle?`——因此**角色卡会自动带上它**。
2. **PromptBuilder**：新增 `buildReplyStylePrompt()`，把风格渲染成
   `【回复风格】` 段放进开头 system（属于稳定人设，不参与每轮变化的动态注入）。
   默认"禁止旁白"，措辞明确到"不要用 *星号* 或（括号）描述自己"。
3. **角色编辑器**：新增「回复风格」区——4 个长度档位（单选）+ 一个
   "允许动作/神态描写"开关，并注明"这是模型写什么；分几条气泡由拟真引擎控制"。

## 四、真机 A/B（Agnes，每档 3 轮）

同一角色、同一段对话（用户吐槽被领导批、随后问"你現在在干嘛"），
只改开头 system 里的风格段：

| 组 | 旁白 | 平均字数 |
|---|---|---|
| A. 旧通用要求（对照） | 0/3 | 19 |
| B. 简短 + 禁止旁白（新默认） | 0/3 | 15 |
| C. 极简 + 禁止旁白 | 0/3 | **7** |
| D. 适中 + 允许旁白 | **2/3** | 19 |

结论（避免自吹）：

- **长度档位确实生效**：极简 7 字 < 简短 15 字 < 旧默认 19 字；
- **开关两个方向都有效**：允许旁白后模型立刻开始写
  "（拿着毛巾擦了擦头发）"（D 组 2/3）；
- 但 A 组 0/3 说明**这个模型本来就不太写旁白**，所以"默认禁止"更像一道保险，
  真正的收益在长度可控与风格可切。

## 五、浏览器实测

```
编辑器 →「回复风格」区：4 档 + 开关，默认高亮"简短"、开关未勾选
选「极简」+ 勾选允许动作 → 保存 → store 里 replyStyle = {terse, allowActions:true}
发消息 → 请求的 system prompt 首段含：
  【回复风格】
  - 长度：极简：一句话，10 字上下，像随手回的消息
  - 形式：可以少量穿插动作或神态描写，用（括号）标出
```

## 六、验证

```bash
pnpm test           # 345 个测试全绿（新增 4 个：风格段渲染 + 注入位置）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 434.64 KB / gzip 140.91 KB）
```

---

# 第二十七轮：白屏加固（错误边界 + 持久化数据清洗）

## 一、起因

这个项目真出过一次白屏：引入 `useShallow` 时踩到 React 双副本，整页空白。
数据都躺在 IndexedDB 里其实没丢，但界面上完全无从判断，用户也不知道该干什么。
这轮想把"崩了就只能干瞪眼"这条补上。

## 二、第一版方案（错误边界）以及它没接住的那次崩溃

先做了 `ErrorBoundary`：渲染期异常时显示兜底界面，除了"重新加载"，
还提供**导出数据备份**——备份逻辑直接读 store，不依赖已经崩掉的渲染树。
配了 5 个组件测试（仓库里第一个 `.test.tsx`）。

然后在浏览器里**真的把它搞崩**验证：

```
注入 sessions: { bad: null }（模拟损坏的持久化数据）
→ 界面整片空白、兜底界面没出现、React 树再也回不来
```

排查发现：抛错的是 `App` 里那个"背景订阅器" effect——
`session.participantIds.find(...)` 在 `session` 为 null 时炸。
**React 的错误边界只能接住渲染期异常，接不住 effect 里的**，
这类错误会直接把整棵树卸载掉。

## 三、真正的修法：进门先过安检

坏数据不该让应用打不开，只该损失它自己。新增
`core/src/storage/sanitize.ts`：hydration 时清洗持久化快照，
把 id/显示名/participantIds/messages 等**启动路径上会被读到的字段**
不合格的条目丢掉，并在控制台报告丢了哪些。

踩到的第二个坑：清洗一开始挂在 `migrate` 上，**完全没生效**——
zustand 只在**版本号变化时**才调用 `migrate`，而坏数据往往出现在版本没变的情况下。
改挂到 `merge`（每次 hydration 都跑）之后才真正起作用。

## 四、浏览器实测

```
直接往 IndexedDB 写入 { sessions: { 坏会话: null }, characters: { 坏角色: {id:123} } }
→ 重新加载：zenchi-shell 正常渲染（不再白屏）
            坏会话/坏角色被丢弃，char-su-wanqing / char-lin-xiaoxiao 保留
再验证正常路径：建会话 → 重载 → 1 个会话 + 活跃会话 + 2 个角色完好无损
```

## 五、验证

```bash
pnpm test           # 341 个测试全绿（新增 14 个：清洗 9 + 错误边界 5）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 433.04 KB / gzip 139.96 KB）
```

> 说明：错误边界仍然保留（渲染期崩溃仍然需要它），
> 但这轮真正的价值是把"坏数据导致开不了机"从根上修掉了。
>
> 另记一笔：`vitest` 与 dev server 同时跑会争抢 `node_modules/.vite` 缓存，
> 曾导致一次 `pnpm test` 报 ELIFECYCLE 失败（重跑即绿）。跑验证前先关 dev server。

---

# 第二十六轮：编辑自己刚发的消息并重发

## 一、问题

自己打错字、说错话、或者想换个问法，之前只能硬着头皮往下聊。
和上一轮做的「换一条」合起来，才算把"重来"这条路铺完整：
回复不满意 → 换一条；提问不满意 → 改一改再问。

## 二、范围取舍

**只允许编辑最后一条用户消息**。改中间某条会让它之后的所有对话失去前提，
与其留下一段自相矛盾的上下文，不如让用户重新生成最后那一轮。
代码层面也是这么落地的：`findLastEditableUserMessage` 从末尾往前找，
遇到的第一条用户消息就是可编辑对象。

## 三、做法

1. **定位**（`ui-wechat/src/utils/regenerate.ts`，与「换一条」同一个模块）：
   `findLastEditableUserMessage(messages)` → `{ messageId, text }`；
   文本取 text 正文、贴图取内联文案、语音取转写；图片这类没文本的不给入口。
2. **就地编辑**（`MessageBubble`）：`canEdit` 的只有最后一条用户消息，
   气泡左侧浮出一个 ✎（hover 显示）；点开把气泡换成输入框，
   回车发送、Esc 取消、Shift+Enter 换行。
3. **提交**（`App.handleEditSubmit`）：撤回这条消息**及其之后的全部回复**
   （复用「换一条」那套 `removeMessagesFrom`），再按新文本走正常发送流程。
4. **边界**：会话切换时退出编辑态（否则会把编辑框带到另一个会话）；
   回复进行中不显示入口。

## 四、浏览器实测

```
发「晚上吃什么好」→ 8 条回复气泡 → 最后一条用户消息旁出现 ✎（仅此一条）
点 ✎ → 就地编辑框打开，预填「晚上吃什么好」，提示"回车发送 · Esc 取消"
改成「中午吃什么好」回车 →
   编辑框关闭、phase=streaming、消息数 2（新用户消息 + 新回复首个气泡）
   旧的那 8 条回复全部消失
跑完 → 1 条用户消息（"中午吃什么好"）+ 8 条新回复，✎ 入口重新出现
```

## 五、验证

```bash
pnpm test           # 327 个测试全绿（新增 5 个：可编辑消息定位）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 429.31 KB / gzip 138.80 KB）
```

---

# 第二十五轮：角色卡单独导出导入

## 一、问题

之前只有"全量备份"，想复用或分享**单个角色**只能整包搬：
换个角色试试、把角色发给朋友、在另一台机器上重建同一个角色，
都做不到。角色编辑器也只有"保存/取消"，没有出口。

## 二、设计取舍

- **与备份的区别**：备份是为了不丢数据；角色卡是为了**复用与分享**，
  所以卡片自包含「角色档案 + 它绑定的人设模板」。
- **刻意不含记忆与聊天记录**：记忆里存的多半是"关于用户本人的事"
  （过敏、住址、工作），跟着角色卡传播出去并不合适。导出时按字段构造，
  不是"把整个 store 剪一块"。
- **导入永远是新增**：角色 ID 重新生成、重名加后缀、
  模板内容完全一致时复用——不会覆盖或改动任何已有角色。

## 三、做法

| 层 | 文件 | 职责 |
|---|---|---|
| 契约 | `shared/types/src/characterCard.ts` | 卡片结构、版本、失败原因 |
| 纯逻辑 | `core/src/backup/CharacterCard.ts` | 构造/序列化/容错解析/实例化 |
| 组装 | `apps/web/src/characterCardRunner.ts` | 读写 store、触发下载、落库 |
| 界面 | 通讯录「导入角色卡」+ 角色编辑器「导出角色卡」 | 两个入口 |

顺手把下载逻辑抽成 `apps/web/src/download.ts`（备份与角色卡共用，
含文件名非法字符清洗），避免两处各写一遍 blob/anchor 那套。

## 四、浏览器实测（完整闭环）

1. 给苏晚晴绑一个人设模板「临海小城」，打开角色编辑器 → 点「导出角色卡」
2. 落盘文件 `zhichi-card-苏晚晴.json`（2948 字节）：
   `format=zhichi-character`、`version=1`、角色 + 模板都在、
   **不含记忆与会话字段**（按字符串检查确认）
3. 回到通讯录 → 点「导入角色卡」选这个文件 →
   提示"已导入角色「苏晚晴（导入）」（原名已存在，自动加了后缀）"
4. 核对 store：新增角色 `char-1789268621308-giv5io`，
   绑定的仍是原有的 `tpl-card`，**模板总数仍为 1（没有堆副本）**，
   原角色苏晚晴分毫未动
5. 连续导入第二次 → 命名递进为「苏晚晴（导入 2）」
6. 负向用例：导入无关 JSON → 红字提示"这不是「咫尺」导出的角色卡。"，
   角色数量不变

## 五、验证

```bash
pnpm test           # 322 个测试全绿（新增 11 个：构建/解析容错/实例化）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 427.26 KB / gzip 138.23 KB）
```

---

# 第二十四轮：重新生成回复（「换一条」）

## 一、问题

模型给出不满意的回复时，用户没有任何办法——只能顺着说下去，
或者手动把话题拐开。AI 聊天里最基础的那个操作（重新生成）是缺的。

难点在于：一次回复会被引擎拆成多条气泡（实测一条回复 8 个气泡），
所以"重新生成"要撤掉的是**整轮**，而不是最后一条。

## 二、做法

1. **轮次定位**（`ui-wechat/src/utils/regenerate.ts`，纯函数）：
   `findLastReplyTurn(messages, characterId)` 从末尾往前吃掉连续的同角色
   气泡得到这一轮的起点，再向前找**紧邻**的用户消息作为重发内容。
2. **store**：`chatStore.removeMessagesFrom(messageId)` 从指定消息整段截断，
   顺带清掉占位计数与"正在输入"。
3. **界面**：最后一条回复下方显示一枚低调的「↻ 换一条」；
   点它 → 撤掉整轮 → 用同一句用户消息重新发起（用户消息已在屏上，不重复插入）。

## 三、过程中修掉的两个判断错误

1. **角色主动发的消息不该能"重新生成"**：第一版实现是"往前找最近一条用户
   消息"，于是当末尾是角色主动发来的消息时，会拿更早那句不相干的用户消息
   去重发，答非所问。改成"紧邻这一轮的必须是用户消息"（时间分割线之类的
   系统消息可以跳过），并补了用例。
2. **回复还没交付完按钮就出现了**：原本用 `typingActive` 判断，
   但"正在输入"指示器在首个气泡交付后就关掉了，整轮其实还在逐条弹出。
   改用会话 `phase`（streaming/paused）判断，浏览器实测确认：
   点击后 `phase=streaming` 期间按钮消失，交付完成后才重新出现。

## 四、浏览器实测

```
点击前：1 条用户消息 + 8 条 AI 气泡（整轮）
点击后：phase=streaming、按钮消失、消息数 2（用户消息 + 新回复的首个气泡）
完成后：1 条用户消息 + 8 条 AI 气泡（全部是新生成的 id），按钮重新出现
```

## 五、验证

```bash
pnpm test           # 311 个测试全绿（新增 12 个：轮次定位 9 + store 动作 3）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 423.26 KB / gzip 137.11 KB）
```

---

# 第二十三轮：实时 AI 主动消息的话题质量

## 一、问题

「实时 AI」让角色隔一段时间主动发消息，但提示词只有一句通用指令：
"现在是个自然时机，主动发一条消息，可以是日常问候/分享心情/发起话题"
（`PROACTIVE_PROMPT`）。模型不知道现在几点、上次聊到哪、有什么没聊完——
于是主动消息很容易退化成"忙完了吗"这种没有信息量的追问。

顺带发现一个**隐性 bug**：给主动消息做记忆检索时，query 用的是这句指令文本
本身（`syncEngineContext(engine, id, PROACTIVE_PROMPT)`），里面没有任何
关键词，等于检索从未生效。

## 二、做法

新增 `core/src/proactive/ProactivePrompt.ts`（纯函数）：

- `describeTimeOfDay`：按小时切 6 个时段（早上/上午/中午/下午/傍晚/晚上/深夜）
- `describeElapsed`：把"距上次聊天多久"说成人话（刚刚才聊过 / 3 小时 / 一天 / 4 天）
- `buildProactivePrompt`：拼出情境（几点、多久没聊）+ 可参考上下文
  （最近对话、未解线索、剧情概要）+ 三条硬要求（优先接未解线索、
  不许用"在吗/干嘛呢/吃了吗"开场、不许重复最近说过的话）
- `buildProactiveQuery`：用"最近对话 + 未解线索 + 剧情概要"做记忆检索，
  替代原来那句指令文本；修掉上面那个检索失效的 bug

`App.tsx` 的主动消息定时器改为按目标会话组装这两样东西再发起请求。

## 三、真机 A/B（Agnes）

新增 `work/agnes_proactive_probe.mts`，两个场景各跑 3 轮：

| 场景 | 旧通用指令 | 新上下文提示词 |
|---|---|---|
| 线索在**最近对话**里（约好去看海、还没定几点出发） | 接上 **2/3** | 接上 **2/3** |
| 线索只在**剧情卡**里（下周三面试、简历没改完，最近几条只是闲聊） | 接上 **0/3** | 接上 **3/3** |

第一组打平是符合预期的——历史本身就把上下文带进去了。
真正的分水岭是第二组：历史窗口看不到的线索，旧提示词只会反复追问
"忙完了吗""忙完了？记得活动一下肩膀"，新提示词三次都主动接住了：

```
简历改得怎么样了？要不下午一起过一遍？
简历改得怎么样了，周三的面试别太紧张
对了，简历改得怎么样了？周三面试别太紧张
```

## 四、验证

```bash
pnpm test           # 299 个测试全绿（新增 10 个：时段/间隔/提示词拼装/检索 query）
pnpm -r typecheck   # 4 包全绿
pnpm build:web      # web 生产构建成功（JS 421.67 KB / gzip 136.64 KB）
```

---

# 第二十二轮：上下文占用可见（把黑盒打开）

## 一、问题

记忆注入、模板注入、上下文裁剪全发生在引擎内部，界面上**完全看不见**：

- 用户无从确认"记忆到底注入了没有"（二十轮的探针脚本就是这个盲区逼出来的）
- 长会话里上下文被裁剪、旧消息被丢掉，界面也没有任何提示，
  表现为"角色怎么突然忘了前面说过的事"
- 用付费 API 时，token 花在哪、一轮涨多少，同样没有概念

## 二、做法

1. **契约**（`shared/types/src/simulation.ts`）：新增 `IContextUsage`
   与 `IContextUsageEvent`，描述这一次请求的 token 估算、消息条数、
   各部分字符数、是否触发裁剪。
2. **PromptBuilder**：拼 prompt 时就地算出分项统计（角色卡 / 人设模板 /
   记忆 / 剧情 / 历史），随 `IPromptResult.usage` 返回。
   system 项会扣掉模板字数，避免模板被统计两次。
3. **引擎**：构造完请求后推一条 `context-usage` 事件。
4. **store**：chatStore 收到事件后写入 sessionStore 的
   `contextUsageBySession`——按会话隔离、**不持久化**（运行时观测值，
   刷新后重发一次就有，没必要每次都写盘）；删除会话时一并清理。
5. **界面**：标题栏右侧一枚 `1.4k tok` 指示器，点开是明细面板；
   触发裁剪时变成警示色并在面板里写明"丢掉多少字"。

## 三、实测（浏览器 + 真机数据流）

| 场景 | 结果 |
|---|---|
| 首发一条消息 | `139 tok`，角色卡 98 字，其余分项为 0（不显示空行） |
| 加一条置顶记忆 + 绑定人设模板后再发 | `395 tok`：角色卡 99 / **模板 58** / **记忆 35** / 历史 61 |
| 注入 30 条长历史（约 9300 字）后 | `5.6k tok`，`trimmed=true`，面板提示"历史太长已裁剪：丢掉 5340 字"，指示器转警示色 |

过程中发现并修掉一个层级 bug：明细面板被消息列表盖住（标题栏没有 z-index），
给 `.zhichi-shell__main-header` 加 `z-index: 5` 后正常。

## 四、验证

```bash
pnpm test           # 289 个测试全绿（新增 11 个：展示工具 8 + PromptBuilder 2 + 引擎事件 1）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 420.13 KB / gzip 135.87 KB）
```

> 本轮环境禁止 `Remove-Item`，浏览器验证截图（仓库根目录下的 `.pw-profile/`）
> 没能删掉；它已在 `.gitignore` 里，不影响构建与测试，手动删掉即可。

---

# 第二十一轮：记忆的冲突更新（状态变了，旧记忆要被取代）

## 一、问题：记忆只增不减，会自相矛盾

旧流程只会"提炼 + 相似度去重"。但人的状态会变：
用户昨天说"喜欢喝咖啡"，今天说"胃不好戒了改喝茶"。这两句的字符 bigram
相似度只有 0.4（`用户喜欢咖啡` vs `用户戒了咖啡` 只共享"用户/咖啡"），
达不到 0.7 的去重阈值，于是**两条同时留在记忆库里**。后果是角色之后
一会儿劝你喝咖啡、一会儿说你在戒咖啡。

## 二、做法

1. **契约层**（`shared/types/src/memory.ts`）：`IMemoryDraft` 增加可选的
   `supersedes`——"本条取代了哪条旧记忆"，填旧记忆原文。
2. **整理提示词**：JSON 结构里加 `supersedes` 字段，并明确规则
   "新信息如果推翻或取代了【已知的长期记忆】里的某一条（喜好改变、习惯戒断、
   约定完成、状态更新、关系变化），把旧记忆原文一字不差填进 supersedes；
   只标真正矛盾的那一条，不要顺手牵连无关的"。
3. **容错解析**：`supersedes` 只接受字符串、限长，脏数据（对象/数字）直接忽略，
   不影响整次整理。
4. **合并阶段**（`mergeMemories`）：遇到 `supersedes` 先按归一化全文相等匹配，
   找不到再退化到 bigram 相似度（阈值 0.8，比去重的 0.7 更严——
   漏删只是用户手动删一条，错删是丢信息），命中就把它从结果里摘掉，
   然后照常写入新记忆。
5. **置顶记忆不参与自动取代**：置顶是用户显式说的"永远记住这条"，
   让模型一句话删掉它，代价（静默丢失、难以察觉）高于残留一条过时信息。
   这种情况两条并存，交给用户在记忆面板里判断。
6. **单次整理最多取代 3 条**：正常一次整理（约 20 条消息）里真正"状态变了"
   的事通常只有一两条；设上限是兜住模型偶发的乱标，即使它把一堆无关记忆
   都标成 supersedes，也最多误删这么多，而不是一次清空记忆库。

## 三、真机验证（Agnes）

新增 `work/agnes_digest_probe.mts`：预置"用户喜欢喝咖啡 / 用户住在北京 /
用户养了一只叫团子的猫"，再让它整理一段"我最近把咖啡戒了，胃不好，
改喝茶了，迷上白茶；团子又咬坏我耳机"的对话。

跑 3 轮，**3/3 都正确回填 `supersedes: "用户喜欢喝咖啡"`**，
合并后旧记忆全部消失、无关记忆（北京/团子）一条没动，喝茶的新状态都记住了：

```
第 1 次  用户因胃不太好改喝茶并迷上白茶，不再喝咖啡  ← supersedes
         合并后 3 条；旧"喜欢喝咖啡"还在=false；记住喝茶=true
第 2 次  用户改喝白茶，不再喝咖啡                    ← supersedes
         合并后 4 条；旧"喜欢喝咖啡"还在=false；记住喝茶=true
第 3 次  用户戒了咖啡，改喝茶，最近迷上白茶          ← supersedes
         合并后 3 条；旧"喜欢喝咖啡"还在=false；记住喝茶=true
```

## 四、验证

```bash
pnpm test           # 278 个测试全绿（新增 8 个：supersedes 解析/合并/置顶保护/条数上限/提示词）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 417.87 KB / gzip 135.10 KB）
```

> 本轮没有启动 dev server（5174 端口空闲），无 Playwright 产物残留。

---

# 第二十轮：上网调研后的记忆注入优化 + 真机 API 测试

## 一、调研来源

同类项目里最成熟的实现是 SillyTavern（33k star）的 World Info / Lorebook。
读了它的官方文档（docs.sillytavern.app/usage/core-concepts/worldinfo/）与源码
（public/scripts/world-info.js，253KB），提取出可借鉴的几条：

- **常驻条目（blue circle）**：不参与关键词竞争、每次必定注入
- **插入顺序（Insertion Order）**：序号越大越靠近上下文末尾，对输出影响越大
- **@D 深度注入**：可以插在对话中的指定深度（Depth 0 = 最靠后）
- **可选过滤（AND ANY / NOT ANY）、递归触发、sticky/cooldown**：都是关键词层面的
  精细控制，对"按角色归档的长期记忆"属于过度设计，本轮不引入

## 二、据此做的四处改动

1. **动态上下文改到"当前提问之前"**
   记忆与剧情摘要原本拼在开头的人设里。现在拆成独立的末尾 system 消息：
   `[人设][历史…][记忆+剧情][当前提问]`。
   依据是"越靠近末尾影响越大"；附带好处是开头人设每轮不变，
   服务端可以做 prompt 前缀缓存。
2. **置顶记忆 = 常驻条目**：不再靠 0.2 的加分去和别人抢名额，
   而是先无条件占位，其余记忆按相关度竞争剩余预算。
   否则一句无关寒暄就可能把"用户对花生过敏"挤出预算。
3. **记忆段内部按重要度升序排列**：最重要的那条落在离提问最近的位置。
4. **修复 Claude 适配器丢消息**：它用 `find()` 只取第一条 system，
   其余 system 全被静默丢弃——也就是说上下文裁剪后生成的「前情提要」
   摘要**从来没送到过 Claude**。现在按顺序合并全部 system 内容。

## 三、裁剪器的位置语义修复

`ContextTrimmer` 之前把所有 system 消息统一提到最前面。这会让"末尾注入"
在上下文变长（也就是记忆最该起作用的时候）失效。现在按位置分两类：
开头连续的人设留在最前；夹在对话中的动态块按"原本距对话末尾第 N 条"
精确还原到原位。

## 四、真机 API 测试（Agnes）

新增探针脚本 `work/agnes_memory_probe.mts`（key 走环境变量，不落盘）：
同一段对话里让角色"记得用户对花生过敏"，用户却点了含花生的宫保鸡丁。

| 组 | 结构 | 提到花生/过敏 |
|---|---|---|
| A. 无记忆（对照） | 单 system | **0 / 4** |
| B. 记忆注入开头 system（旧位置） | 单 system | 3 / 4 |
| C. 记忆注入末尾 system（新位置） | 双 system | **4 / 4** |

对照组 0/4 说明"提到花生"确实来自记忆注入，不是模型自己猜的。

## 五、真机暴露的两个问题（已修）

1. **空回复静默无响应**：模型有时 `finish_reason=length` 且正文 0 字
   （思考过程吃掉了输出预算，实测思考 300~1400 字）。
   旧代码只是安静收尾，用户发完消息什么都看不到。
   现在：先自动重试（400/800/1600ms 退避），仍为空则报可重试错误，
   UI 出现重试气泡。
2. **中断后追加罐头短句**：流到一半超时时，旧代码会补一句
   "我在的，稍等一下。"——真实回复后面接一句驴唇不对马嘴的罐头话。
   现在：已经交付过正文就直接收尾并给出可重试提示，不再追加。
3. 顺带把 Agnes 预设的推荐超时定为 90s（实测简单回复 3~10s、
   带思考的长回复会超过 30s，被默认超时误判为失败）。

## 六、验证

```bash
pnpm test           # 270 个测试全绿（新增：空回复重试/部分正文收尾/裁剪位置/Claude 合并）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 417.28 KB / gzip 134.68 KB）
```

浏览器 + 真机（Agnes）：

- 设置页「测试连接」→ **连接成功 · 8400ms**
- 请求结构实测为 `ssu`（人设 system + 动态 system + 当前提问），
  末尾 system 含 `【你记得的事】用户对花生严重过敏…`
- 角色回复主动提醒："不过我得提醒你一下，你对花生严重过敏这件事要记得呀，"
  —— 记忆注入在真实链路上确实生效
- 把超时调到 90s 后重发：`phase=completed`、`lastError=null`、
  回复自然（"唔. / 面条？"），不再出现罐头句

> 已释放 dev server（5174 端口空闲）、清空 `.playwright-cli/` 内的产物文件
> （本轮环境的策略禁止 `Remove-Item`，改用补丁工具逐个删除；空目录留存不影响使用）。

---

# 第十九轮：跨会话聊天记录搜索

## 一、问题：聊过的事找不回来

会话一长，"上次那个事是什么时候说的"就完全没法找——只能一个会话一个会话
翻，而消息列表是原生滚动 + `content-visibility`，翻几百条非常费劲。
记忆系统只记"值得长期记住的事"，找原话还是得靠搜索。

## 二、实现

| 层 | 文件 | 职责 |
|---|---|---|
| 纯逻辑 | `ui-wechat/src/utils/messageSearch.ts` | 可搜索文本提取、命中片段截取、跨会话检索与排序 |
| 界面 | `ui-wechat/src/components/MessageSearchPanel.tsx` | 搜索框 + 结果列表（关键词高亮、时间、发送者） |
| 容器 | `WeChatShell` | 关键词非空时用结果替换会话列表 |
| 定位 | `ChatSessionView` + `MessageList` | 跳到目标消息并高亮闪烁 |

搜索范围：文本正文、语音的 ASR 转写、贴图的内联文案。
图片（无文本）、系统消息（时间分割线等噪声）、撤回提示不参与。
数据源直接读 `sessionRuntimes`——它已经是全量会话消息的单一来源，
不需要额外建索引。

## 三、定位跳转踩到的三个坑（都已处理）

1. **贴底逻辑抢滚动**：消息数变化时"贴底黏性"会把视图拉回底部，
   跳转前必须先把黏性置为 false，否则刚滚过去就被拽回最新消息。
2. **DOM 还没准备好**：切换会话时列表会重建、消息随后才灌进 store，
   找不到目标行要重试（120ms × 4 次）。
3. **`content-visibility` 的高度估算**：屏外消息行的高度是估算值
   （`contain-intrinsic-size: auto 80px`），滚过去之后真实高度才算出来，
   会把目标顶偏。改成"滚动 → 重新测量 → 再修"的收敛循环，
   偏到 4px 以内或最多 6 轮为止。

## 四、实测

1. 建两个会话（林笑笑 9 条 / 苏晚晴 3 条），关键词「看海」只出现在林笑笑那边
2. 在聊天页搜索框输入「看海」→ 会话列表被结果替换，命中 2 条，
   片段里 `<mark>` 精确框住"看海"三字，会话名与时间都正确
3. 点结果 → 会话切到林笑笑、目标行拿到高亮、搜索框自动清空回到会话列表
4. 短会话跳转：目标是第 0 条且列表总高度 = 可视高度（无滚动空间），
   偏移 -226px 属"列表贴顶"的正常结果
5. 长会话跳转（注入 40 条调试消息撑到 49 条）：
   目标第 20 条，`scrollTop=914`、内容高 2971、可视 664，
   **居中偏移 = 0px**，收敛循环生效
6. 全程控制台 0 报错 0 警告

## 验证命令与结论

```bash
pnpm test           # 260 个测试全绿（新增 13 个检索用例）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 415.97 KB / gzip 134.21 KB）
```

> 已释放 dev server（5174 端口空闲）并删除 `.playwright-cli/`。

---

# 第十八轮：数据备份（导出 / 导入）

## 一、问题：数据只活在浏览器里，丢了就真没了

第十七轮那次"刷新清空会话列表"的故障暴露了一个结构性风险：角色、会话、
消息、记忆、剧情全部存在 IndexedDB，**清缓存 / 换浏览器 / 换电脑 = 全丢**，
而应用此前没有任何导出入口。这不是体验问题，是数据安全问题。

## 二、实现：三层分工，零新依赖

| 层 | 文件 | 职责 |
|---|---|---|
| 契约 | `shared/types/src/backup.ts` | 备份文件结构、版本号、失败原因枚举 |
| 纯逻辑 | `core/src/backup/BackupFile.ts` | 构造 / 序列化 / 容错解析 / 非破坏性合并 |
| 组装 | `apps/web/src/backupRunner.ts` | 读写 store、触发下载、把合并结果落库 |
| 界面 | `SettingsPanel`「数据备份」区 | 导出按钮 + 导入入口 + 结果提示 |

顺手做了一处归位：`ISessionRuntimeSnapshot` 原本定义在 ui-wechat 的 store 里，
但它是纯数据契约（备份文件也需要它），已上移到 `shared/types/src/session.ts`，
store 处保留同名再导出，既有引用不受影响。

## 三、合并语义（导入不删任何本地数据）

- 记录型（会话 / 角色 / 通讯录 / 提示词模板）：同 ID 以备份文件为准，新 ID 追加
- 记忆：按 ID 求并集，两边都不丢
- 剧情：事件按 ID 求并集；状态卡取 `updatedAt` 更新的那一份（旧备份不会覆盖新剧情）
- 消息：按消息 ID 去重后按时间排序（备份里读不到 ID 的消息一律保留）
- 输入草稿：本地非空的保留（不覆盖用户正在打的内容）
- 活跃会话：本地有且仍存在就用本地的，否则回退到备份指定的，再否则取第一个会话
- 配置（API 配置 / 实时 AI / 整理配置）：以备份文件为准

导入进来的消息统一"冻结"为已完成态（`revealed: true` / `pending: false`），
避免备份里上次中断留下的半成品气泡永远停在半个、或重放一次假的打字动画。

## 四、端到端实测（真实浏览器 + 真实文件）

1. 造数据：建会话（苏晚晴）+ 发消息 + 写一条记忆 → 1 会话 / 9 消息 / 2 角色 / 1 记忆
2. 点「导出全部数据」→ 状态栏回执"已导出：1 个会话 / 9 条消息 / 2 个角色 / 1 条记忆"，
   磁盘上真的落了一个 `zhichi-backup-20260912-230137.json`（14040 字节）
3. 检查文件内容：`format=zhichi-backup`、`version=1`、13KB、
   **不含 apiKey 字段**、记忆与首条消息文本都在
4. 清空 IndexedDB（模拟清缓存）→ 刷新 → 确认 0 会话 / 0 消息 / 0 记忆
5. 点「导入备份」选中该文件 → 回执"导入完成：1 个会话 / 9 条消息 / 1 条记忆
   （已与本地数据合并）"，聊天界面重新出现原来那两条气泡，
   store 与 IndexedDB 都恢复为 1 会话 / 9 消息 / 1 记忆
6. 负向用例：导入一个无关 JSON → 提示"这不是「咫尺」导出的备份文件。"，
   本地数据仍是 1 会话 / 9 消息 / 1 记忆，未被破坏
7. 全程控制台零报错

## 验证命令与结论

```bash
pnpm test           # 247 个测试全绿（新增 19 个：备份构造/解析/合并）
pnpm test:coverage  # 语句覆盖率 91.95%（BackupFile.ts 96.37%）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 411.06 KB / gzip 132.50 KB）
```

> 已释放 dev server（5177 端口空闲）并删除 `.playwright-cli/` 与临时测试文件。

---

# 第十七轮：数据安全与运行时整洁（浏览器实测发现）

> 起因：用 Playwright 走查消息列表渲染时，控制台报错牵出三个更深的问题。

## 一、修复：刷新可能把会话列表清空（严重 · 数据安全）

- **现象**：点击角色建立会话并聊了几句，刷新后会话列表变成"还没有聊天"，
  但 `chatStore` 的消息还在（9 条），成了找不到会话的孤儿数据。
  查 IndexedDB：`wechat-rp-sessions` 里 `state.sessions` 是 `{}`，
  而 `contacts` / `characters` 恰好是"全新安装 + 种子角色"的样子。
- **根因**：`App.tsx` 的"首次启动注入种子角色"effect **没有等待 persist
  hydration**。hydration 走 IndexedDB，是异步的；`useEffect` 在首帧后就执行。
  于是它读到的是**初始空状态**，判定"没有角色"→ 写入种子 → 触发 persist
  把当时的内存快照（`sessions: {}`）写回磁盘，覆盖掉真实数据。
  仓库里其它 effect 都检查了 `hydrated`，只有这一处漏了。
- **修复**：种子注入加 `if (!sessionHydrated) return;`，依赖数组改为
  `[sessionHydrated]`。
- **验证**：故意把数据库改成"`characters: {}` + `sessions: 1 条`"（模拟角色
  数据缺失但会话还在），连续刷新 4 次，每次读回都是
  `dbSessions: 1 / dbCharacters: 2（种子被重新注入）/ activeSessionId 不变`
  ——会话不再被覆盖，同时种子角色仍能自愈恢复。

## 二、修复：React 存在两份副本（运行时隐患）

- **现象**：把 `ChatSessionView` 的 `shallow` 换成官方推荐的 `useShallow`
  之后，页面直接白屏：`Invalid hook call` +
  `Cannot read properties of null (reading 'useRef')`。
- **根因**：报错栈里出现两个不同的 React chunk。`packages/ui-wechat`
  的 `node_modules` 里有自己的一份 `react`，vite 预构建
  `zustand/react/shallow` 时把它接到了另一份 React 上。
  之前没暴露，是因为旧写法用的 `zustand/shallow` 是纯函数、不调用 hooks。
- **修复**：`apps/web/vite.config.ts` 增加 `resolve.dedupe: ["react", "react-dom"]`，
  强制全应用共用一份 React（这是 vite 官方对 duplicate React 的标准解法）。
- **顺带**：`ChatSessionView` 改用 `useShallow`，消除 zustand v4.5 在控制台
  打印的 `[DEPRECATED] Use createWithEqualityFn ...` 警告。

## 三、修复：favicon 404

- **现象**：每次加载页面，控制台必有一条
  `Failed to load resource: 404 @ /favicon.ico`。
- **修复**：`apps/web/index.html` 内联一个 SVG 图标（微信绿底 + "咫"字），
  既消除 404，又省掉一次网络请求。

## 四、清理：17 个僵尸 dev server 占满端口

- **现象**：启动 `pnpm dev` 时端口从 5174 一路试到 **5193** 才成功。
  `netstat -ano` 显示 5177–5193 每个端口都有监听进程——历轮迭代启动的
  dev server 从未被真正关闭（Ctrl+C 会被 vite 的终端 UI 吞掉）。
- **处理**：先用 HTTP 探测每个端口的页面标题区分归属——
  5174–5176 是**另一个项目**（中译英命名词典），5177–5193 全部是本项目。
  只结束本项目那 17 个进程（逐个校验 PID 与端口无重叠），
  另一个项目的 6 个进程保持不动（清理后复查：5174–5176 仍在监听）。
- **验证**：5177–5193 全部释放，`pnpm dev` 回到从 5174 起分配。

## 验证命令与结论

```bash
pnpm test           # 228 个测试全绿
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 402.77 KB / gzip 129.64 KB）
```

浏览器实测：消息列表改名后渲染正常（3 条气泡、贴底 `gap=0`、内边距 12px），
刷新后会话与消息完整保留，控制台零报错、零警告。

---

# 第十六轮：文档重写 + 记忆/剧情计划核对

## 一、修复：`pnpm lint` 必然失败的死脚本

- **问题**：根 `package.json` 声明了 `"lint": "pnpm -r lint"`，但 4 个子包
  （shared/types、core、ui-wechat、web）都没有对应的 `lint` 脚本，
  任何一次 `pnpm lint` 都会以 `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` 退出。
- **修复**：删除该脚本，同时在 README 的已知限制里写明"未接入 lint 工具链"，
  避免后续再有人误以为仓库带了 ESLint。

## 二、README 从骨架说明改写为真实项目文档

原标题下只有目录罗列，与实际能力严重脱节。重写后包含：

- 功能一览（对话体验 / 角色与会话 / 记忆与剧情 / 工程）
- 快速开始：`pnpm install` → `pnpm dev` → 接真实模型的三步流程
- 23 个供应商预设说明与环境变量兜底配置，标注
  `VITE_LLM_TEMPERATURE` 是**千分数**（800 = 0.8）这个易踩的坑
- 常用脚本表、目录结构、架构图与三条不变量（引擎零 UI 依赖 / 状态单向流动 / 内容在 core 计算）
- DEV 调试入口：`window.__lastLLMRequest`、`__llmRequests`、`__chatStore`、`__sessionStore`
- 已知限制：群聊未实现、图片/语音为占位、无 lint

## 四、清理上一轮遗留：死依赖与误导性命名

- **问题**：消息列表在上一轮已改为「原生滚动 + `content-visibility`」，
  但 `react-virtuoso` 仍留在 `packages/ui-wechat/package.json` 依赖里；
  组件名叫 `VirtualizedMessageList`、CSS 类名是 `wechat-chat-session__virtuoso`，
  代码里还有"react-virtuoso 虚拟滚动，500+ 消息不帧率退化"的过时注释——
  三处都在误导后来者"这里用了虚拟滚动库"。
- **修复**：
  - `VirtualizedMessageList.tsx` → `MessageList.tsx`，组件与接口
    `IVirtualizedMessageListProps` → `IMessageListProps`，文件头注释改写为
    "为什么不用虚拟滚动库"的说明（保留踩坑结论，不再提具体库名当现状）；
  - CSS 类 `.wechat-chat-session__virtuoso` → `.wechat-chat-session__scroller`；
  - `ChatSessionView` / `components/index.ts` 同步改名，`ChatSessionView`
    顶部过时注释改为"原生滚动 + `content-visibility: auto`"；
  - 从 `package.json` 移除 `react-virtuoso`，`pnpm install --lockfile-only --offline`
    更新锁文件（`pnpm-lock.yaml` 中该包相关条目由 3 条降为 0 条）。
- **说明**：本机 `pnpm install`（完整安装）会因 Windows 权限报
  `ERR_PNPM_PACKAGE_MANAGER_REMOVE_MODULES_DIR`（拒绝访问），
  故只更新了锁文件；`node_modules` 里的残留副本不参与构建，下次完整安装会自动清理。

## 三、逐项核对「记忆与剧情」实施计划（全部落地）

| 计划项 | 落地位置 | 状态 |
|---|---|---|
| 历史注入 | `core/src/llm/toLLMHistory.ts` + `App.tsx#syncEngineContext` | ✅ |
| 模板注入 | `RealismEngine.setPromptTemplate` → `buildPrompt` 第 5 参数 `extras` | ✅ |
| 模板绑定语义 | `PromptPanel` 写 `character.promptTemplateId`，已绑定显示「使用中」 | ✅ |
| 记忆类型/存储 | `shared/types/src/memory.ts`、`sessionStore.memories`、schema v3 | ✅ |
| 提炼/去重 | `MemoryDigest.ts`（`buildDigestPrompt` / `parseDigest` / `runDigest` / `mergeMemories`） | ✅ |
| 轻量检索 | `MemoryRetriever.ts`（0.6 命中 + 0.25 重要度 + 0.15 衰减 + 0.2 置顶，Top 10 / 800 字） | ✅ |
| 触发编排 | `apps/web/src/digestRunner.ts`（单飞 + 失败冷却 + 独立 AbortController） | ✅ |
| 剧情状态卡/事件/回滚 | `shared/types/src/plot.ts`、`PlotKeeper.ts`、`PlotTab.tsx` | ✅ |
| 推进剧情 | `InputBar#onAdvancePlot` → `buildPlotAdvancePrompt` → `startStreamWithAdapter` | ✅ |
| 三 Tab 面板 | `PromptPanel` + `MemoryTab` + `PlotTab` + 2 个新图标 | ✅ |
| 设置项 | `SettingsPanel`「记忆与剧情」区（开关 / 阈值 / 注入上限 / 窗口 / 事件数 / 快照数） | ✅ |

## 验证命令与结论

```bash
pnpm test           # 228 个测试全绿（23 个测试文件）
pnpm test:coverage  # 语句覆盖率 91.45%（RealismEngine 89.91% / MemoryDigest 94.33% / PlotKeeper 99.39%）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功（JS 402.68 KB / gzip 129.60 KB）
```

数字自检：README 声明的「228 个测试」「23 个供应商预设」「约 91% 覆盖率」
均与实测一致；`pnpm lint` 已随脚本删除而不再出现在文档中。

> 无残留 dev server（5173-5182 端口全部空闲），`.playwright-cli/` 目录已清理。

---

# 记忆与剧情系统 · 验证清单（schema v3）

> 目标：让角色真正"记得住"并支持剧情推进。
> 启动：`cd d:\咫尺 && pnpm dev`，输入栏左侧「提示词」按钮打开面板 →
> 三个 Tab：提示词 / 记忆 / 剧情。

## 一、地基修复（prompt 注入链路）

- [x] 会话历史真正进入 Prompt（`syncEngineContext` → `engine.setMessageHistory`）。
  > 修复前 `setMessageHistory()` 全仓无调用点，角色连上一句都不知道。
- [x] 人设模板注入 system prompt（修复 `buildPrompt` 漏传模板）。世界观 / 当前剧情字段由此生效。
- [x] 提示词面板「绑定」按钮把模板绑定到当前会话角色（写 `character.promptTemplateId`），
      已绑定项显示「使用中」，并保留「填入输入框」次要操作。
- [x] `buildPrompt` 第 5 参数改为 `extras` 对象：`{ promptTemplate?, memories?, plotSummary? }`，
      注入顺序为 角色卡 → 人设模板 → 记忆 → 剧情摘要 → 睡眠状态。

## 二、长期记忆

- [x] 后台整理：累计 ≥ 20 条未整理消息触发（可在设置中调整），复用当前 API 配置与模型。
- [x] 整理产出 JSON 容错解析（剥离 markdown 围栏、坏 JSON 静默放弃、kind/importance 白名单收敛）。
- [x] 记忆合并：字符 bigram 相似度 ≥ 0.7 视为同一条，保留高重要度并合并关键词。
- [x] 轻量本地检索：关键词子串 + bigram 覆盖度命中，`0.6×命中率 + 0.25×重要度 + 0.15×时间衰减(14天) + 0.2×置顶`，
      取 Top 10 且总量 ≤ 800 字。
- [x] 记忆 Tab：新增 / 编辑 / 删除 / 置顶 / 搜索 / 「立即整理」 / 「待整理 N 条」提示。
- [x] 失败冷却 3 分钟，整理游标不推进、下次重试；独立 AbortController，不与引擎流互相干扰。

## 三、剧情

- [x] 状态卡：章节 / 场景 / 故事时间 / 地点 / 剧情概要 / 未解线索 / 关系。
- [x] 事件时间线：后台整理自动追加（按摘要去重，保留最近 100 条），支持手动添加与删除。
- [x] 回滚：状态卡每次变化前压入快照（保留 5 版），一键回滚；事件不参与回滚。
- [x] 「推进剧情」按钮：基于状态卡主动推进一步；无状态卡时退化为通用主动消息指令。
- [x] 剧情摘要（章节 + 线索 + 概要）随每条回复注入 system prompt。

## 四、存储与配置

- [x] `schema v3`：sessionStore 新增 `memories` / `plotStates` / `digestCursors` / `digestConfig` 并持久化；
      `apiKey` 仍不落盘；v1 旧快照重置、v2 原样保留（缺省字段由浅合并补齐）。
- [x] 删除会话清理剧情与整理游标；删除角色清理其记忆与关联会话剧情。
- [x] 设置面板新增「记忆与剧情」区：自动整理开关、触发阈值、每次注入条数上限。
- [x] Mock 适配器下自动整理静默跳过（不产出可解析 JSON）。

## 五、验证命令

```bash
pnpm test           # 162 个测试全绿（新增 54 个：记忆/剧情/转换/注入）
pnpm test:coverage  # core 语句覆盖 90.98%（MemoryDigest 93.6% / PlotKeeper 99.4%）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第十五轮：消息复制 + 会话列表时间社交化

## 一、新能力：复制消息文本（右键 / 长按）

- 桌面端**右键**气泡、触屏**长按 500ms** → 复制该条消息文本；
- 复制成功后气泡上方浮出 1.2 秒的「已复制」提示；
- 新增 `utils/clipboard.ts`：优先 `navigator.clipboard`（仅安全上下文可用），
  并保留 `execCommand("copy")` 兜底，避免局域网 http 访问时复制失效；
- 定时器在组件卸载时清理（避免泄漏）。
- **实测**：剪贴板 stub 收到正确文本 `["这是一条用于复制测试的消息"]`；
  右键与长按各触发一次；「已复制」提示经 MutationObserver 确认出现过。

## 二、会话列表时间社交化

- 抽 `utils/sessionTime.ts`（原为组件内私有函数），并补齐微信式规则：
  今天 `HH:mm` / 昨天 `昨天` / 一周内 `星期X` / 更早 `M/D` / 跨年 `YYYY/M/D`；
- 6 个单测覆盖各档边界。
- **实测**：5 分钟前的会话显示 `22:26`，26 小时前的会话显示 `昨天`。

## 验证命令与结论

```bash
pnpm test           # 228 个测试全绿（新增 6 个：会话时间格式）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第十四轮：按会话的输入草稿（微信式体验）

## 问题

输入框内容是全局状态：在会话 A 打了一半切到 B，那半句话会**跟着跑到 B**
（比"草稿丢失"更糟——容易把给 A 的话误发给 B）。

## 改进

- `sessionStore` 新增 `drafts: Record<sessionId, text>` 与 `setDraft()`：
  空字符串等于删除该键；随会话一起持久化；删除会话/角色时同步清理。
- `App` 在会话切换时：把旧会话的输入内容存为草稿、并恢复新会话的草稿
  （没有则清空输入框）；发送成功后清空该会话草稿。
- 用 `inputValueRef` 读取最新输入，避免把 `inputValue` 加进切换 effect 的依赖。

## 实测（五项）

1. 在苏晚晴会话输入"草稿A" → 切到林笑笑：输入框**为空**（不串会话），
   且 store 中 `drafts[苏晚晴]` 已保存；
2. 在林笑笑输入"草稿B" → 切回苏晚晴：输入框**恢复"草稿A"**，
   两个会话的草稿同时存在互不影响；
3. 在苏晚晴发送 → 该会话草稿被清除、输入框清空，林笑笑的草稿保留；
4. 刷新页面 → `drafts` 仍在（IndexedDB 持久化）；
5. 切到林笑笑 → 输入框恢复"草稿B"。

## 验证命令与结论

```bash
pnpm test           # 222 个测试全绿（新增 2 个：草稿存取/清除、删除会话清理草稿）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第十三轮：代码整洁度 + 角色编辑器细节走查

## 一、代码整洁度：标出两处死代码

- `ContactList`：无任何内部使用方（会话列表 = `SessionList`，通讯录 = `ContactsPanel`）。
- `useSmartScroll`：无任何内部使用方（贴底判定已改为 `ChatSessionView` +
  `utils/scrollFollow.ts`，后者有单测）。
- **处理方式**：由于当前项目**没有版本控制**（无法回滚删除），选择在文件头
  与导出处加显著标注说明现状与替代实现，而非直接删除。

## 二、角色编辑器细节走查（4 项全部正常）

1. **原型联动**：点「夜猫」→ 作息自动变为 `10:00 / 03:00`、简介填为
   "夜猫子，深夜最活跃"；且联动是**保护性**的——简介已填时不覆盖、
   编辑已有角色时不改写作息。
2. **自定义作息保存**：改为 `08:15 / 01:45` 保存后，
   store 中 `wakeTime=08:15`、`sleepTime=01:45`、`archetype=night-owl`。
3. **编辑模式回填**：打开编辑后标题为"编辑角色"、名字/作息
   （`08:15`、`01:45`）回填正确、原型按钮高亮为「🦉夜猫作息」。
4. **空名字保护**：清空名字后「保存角色」按钮为 disabled，
   强制点击也不会保存或关闭编辑器。

## 验证命令与结论

```bash
pnpm test           # 220 个测试全绿
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第十二轮：侧栏拖拽 / 删除会话验证 + 残留清理

## 一、验证侧边栏拖拽

- 拖拽分隔条：宽度 300px → 420px（inline style 同步更新）；
- 边界钳制：拖到最左停在 **220px**、拖到最右停在 **520px**，符合设计范围。

## 二、验证删除会话（确认后）

覆盖 `window.confirm` 为同意后从会话菜单删除：
- 会话从列表移除（2 → 1 → 0），`activeSessionId` 归空、主区回到"选择一个聊天开始对话"；
- 关联数据清理正确：`plotStates` 与 `digestCursors` 均被删除；
- 角色记忆不受影响（符合"记忆属于角色"的设计）。

## 三、修复：删除活跃会话后消息残留在内存

- **现象**：删除当前会话后，`chatStore.messages` 仍保留该会话的消息
  （UI 不显示、也不会串到其他会话，但会持久化进 IndexedDB 占空间）。
- **修复**：会话切换逻辑在"无活跃会话"时清空运行时
  （messages / 待发队列 / pendingCount / lastError / typingIndicator）。
- **验证**：删除唯一会话后 `chatMessages: 0`、`chatPhase: "idle"`。

## 验证命令与结论

```bash
pnpm test           # 220 个测试全绿
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第十一轮：核心交互验证 + 记忆面板可视化

## 一、验证「推进剧情」（此前从未端到端走过）

- 预置剧情卡（章节"第一章"、未解线索"未送出的礼物"）后点击输入栏的「推进剧情」；
- 抓取请求确认：最后一条 user 消息是推进指令
  （"请以苏晚晴的身份推进一步剧情——优先处理「当前剧情」中你最在意的一条未解线索…"），
  system 同时含 `【当前剧情】`、`章节：第一章`、`未解线索：1) 未送出的礼物`；
- 结果：角色主动发消息、`phase` 收尾为 `completed`，
  且**没有插入用户消息**（符合"角色主动开口"的设计）。

## 二、验证长按头像切换固定立绘

- 指针按下 600ms → 固定立绘出现（`[data-pin=true]`）；
- 抬起后紧接的 click **不会**误打开立绘查看器（`viewerOpenedByClick: false`）；
- 再次长按 → 固定立绘消失（数量回到 0）。

## 三、改进：记忆面板显示来源与更新时间

- 新增 `utils/relativeTime.ts`：`刚刚 / N 分钟前 / N 小时前 / 昨天 / N 天前 / YYYY-MM-DD`
  （含未来时间容错），7 个边界测试。
- 记忆条目底部显示「更新于 X」，有来源消息时追加「· 来源 N 条消息」，
  悬停可看到完整时间戳。
- 实测三个样本：`更新于 刚刚`、`更新于 5 分钟前 · 来源 2 条消息`、`更新于 2026-08-03`。

## 验证命令与结论

```bash
pnpm test           # 220 个测试全绿（新增 7 个：相对时间边界）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第十轮：回复节奏可调（拟真参数一键预设）

## 一、问题：拟真参数彼此耦合，逐项调很难调到顺手

- 上轮实测：一条 8 段回复（Mock）约需 30-50 秒，取决于打字速度档位、
  犹豫概率与气泡间隔上限的组合；其中「气泡间隔上限」与「碎片化阈值」
  此前**完全没有暴露**在设置里。

## 二、改进

1. **一键节奏预设**（`utils/pacingPresets.ts`，纯函数 + 4 个单测）：
   - 轻快闲聊：fast / 犹豫 0.05 / 间隔上限 1500ms
   - 标准（默认）：normal / 0.15 / 3500ms
   - 慢热：slow / 0.25 / 6000ms
2. **补充两个滑块**：气泡间隔上限（800–6000ms）、碎片化阈值（8–60 字）。
3. **默认气泡间隔上限 6000 → 3500ms**：超过 ~3.5 秒的停顿会明显像"卡住"。

## 三、实测（页面内计时器，Mock 一条 8 段回复）

| 档位 | 完整回复耗时 |
|---|---|
| 标准（默认） | **30.5s** |
| 轻快闲聊 | **20.0s**（−34%） |

切换方式：设置 → 拟真度控制 → 回复节奏（三档按钮），点击即刻生效。

## 验证命令与结论

```bash
pnpm test           # 213 个测试全绿（新增 4 个：节奏预设映射与单调性）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第九轮：微信式"边回复边说话"+ 提示词面板全流程验证

## 一、新能力：对方回复期间可以继续发消息（自动排队）

- **改进前**：流式回复期间输入框被禁用，必须等对方说完才能打字——
  与微信习惯不符（现实里对方正在输入时你照样可以连发几条）。
- **改进后**：
  - 流式期间输入框可正常输入与发送；
  - 消息**立即上屏**（用户能看到自己发了什么），并进入待发队列；
  - 当前回复结束后，队列按 FIFO 自动发出下一条，不打断正在生成的内容；
  - 占位符提示改为"对方正在回复…（仍可继续发消息）"。
- **实测**：发送 A（进入回复）→ 紧接着发送 B → B 立即上屏且 `queue=["B-第二条"]`
  → A 的回复自然结束后队列自动消费 → 角色回复
  `我听到你说"B-第二条"了。`，`phase` 收尾为 `completed`、队列清空。

### 过程中修复的两个自身缺陷

1. **Enter 发送被拦**：`handleKeyDown` 里仍保留 `!disabled` 判断，
   流式期间按 Enter 会被丢弃（消息消失）。已移除该判断。
2. **队列永不消费**：`enqueueUserMessage` 复用了 `addUserMessage`，
   而后者会把 `phase` 置为 `streaming`——队列消费者据此误判"引擎忙"，
   消息永久卡在队列里。现已拆分为"只上屏"与"上屏 + 进入等待"两条路径。

## 二、提示词面板全流程验证（补完上一轮中断的验证）

- 面板 UI 新建结构化模板（世界观 = "冰雪小镇，常年下雪"、当前剧情 = "两人在店里初次见面"）；
- 点「绑定」→ 角色 `promptTemplateId` 更新、列表项出现 `--bound` 类与「使用中」按钮；
- 发送消息 → 捕获的请求 system 中出现
  `【世界观/背景】- 世界设定: 冰雪小镇，常年下雪 - 当前剧情: 两人在店里初次见面`；
- 点「使用中」解绑 → 新请求不再包含模板内容；
- 「填入输入框」次要操作正常（面板关闭、输入框填入 42 字模板文本）。

## 三、其他已验证路径

- 会话菜单「标为已读」：未读徽标 1 → 0，红点同步消失。

## 验证命令与结论

```bash
pnpm test           # 209 个测试全绿（新增 4 个：入队上屏 / FIFO / 清空 / 不改 phase）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第八轮：记忆/剧情自动整理的真实链路验证与修复

## 一、真实模型下的自动整理（首次端到端跑通）

- 把整理阈值临时调为 2 条，发送"我叫小明，最喜欢喝冰美式。"
- 结果：真实模型完成整理并落库——
  - 记忆：`[事实] 用户名叫小明`（重要度 5）、`[偏好] 用户最喜欢喝冰美式咖啡`（4）
  - 剧情：章节「初识」、概要"小明向苏晚晴介绍名字和饮品偏好"、
    事件"小明与苏晚晴初次相识并建立联系"
  - 整理游标推进到消息总数（10 → 10）
- 说明 `runDigest` 的真实 JSON 输出能被容错解析（剥离围栏/形状校验），
  `mergeMemories`、`applyDigest` 均按预期工作。

## 二、记忆确实被注入并影响回复

- 抓取聊天请求的 system：含 `【你记得的事】- [事实] 用户名叫小明
  - [偏好] 用户最喜欢喝冰美式咖啡` 与 `【当前剧情】…章节：初识`。
- 角色真实回复："当然记得呀~是你刚才告诉我的嘛。"——记忆链路闭环。

## 三、修复：真实模型输出里的纯标点空气泡

- **现象**：真实回复中出现 `冰美式.`、`.`、`.` 这类气泡，其中单独的 `.` 是空气泡。
- **根因**：上一轮只丢弃"纯空白"片段，而真实模型常输出行尾句点、列表符号等格式噪声。
- **修复**：新增"结构性标点"判定——仅由 `. , ; : 、，。；：·-—` 与空白组成的片段不产生气泡；
  而 `…`、`？`、`！` 等有表达力的标点单独成气泡时保留（拟真表达不受损）。

## 四、修复：记忆语义重复累积

- **现象**：第二轮整理后出现第 3 条记忆"用户名叫小明，最喜欢喝冰美式。"，
  与已有的两条语义重复——因为整理请求没有告诉模型"已经记住了什么"，
  且单纯 bigram 相似度识别不出"多个事实合并成的长句"。
- **修复**（根因 + 兜底）：
  - 根因：整理请求下发「【已知的长期记忆】」清单，并要求模型不要重复输出；
  - 兜底：`mergeMemories` 增加"包含式"判定（新记忆包含旧记忆或反之 → 视为同一条合并）。
- **验证**：整理请求中确认带有 `已知的长期记忆` 段落；新增 2 个单测覆盖提示词与合并逻辑。

## 五、调试钩子增强

- `window.__lastLLMRequest` 会被后台整理请求覆盖，不便于排查；
  现同时维护 `window.__llmRequests`（最近 5 次请求的数组），聊天与整理请求可分别识别。

## 六、外部限制说明

- 本轮后半段 Agnes API 开始持续返回 `401`（疑似额度/风控限制），
  因此"第二轮整理的完整落库"未能复测；项目侧处理是稳健的：
  401 → 引擎降级收尾（不卡死），整理请求失败 → 静默记录冷却、下次重试。
- 如需继续真实链路验证，建议确认该 key 的额度/状态，或更换 key。

## 验证命令与结论

```bash
pnpm test           # 205 个测试全绿（新增：结构性标点过滤 ×2、已知记忆提示 ×1、包含式去重 ×1）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第七轮：接入真实 LLM（Agnes）后的故障排查与修复

> 本轮用真实 API（Agnes `agnes-2.5-flash`）跑通链路，暴露了 Mock 覆盖不到的问题。

## 一、超时/底层中止会让对话永久卡死（严重）

- **现象**：真实请求超时后，界面停在"对方正在回复…"，没有回复也没有错误，
  必须点"中止"才能继续——与最初报告的 bug 同源。
- **根因**：`OpenAIAdapter` 把所有 abort 都归类为 `kind: "aborted"`，
  而引擎 `streamWithRetry` 认为 `aborted` 只可能来自"用户主动中止"，
  于是直接 `return`：不重试、不降级、不发错误、不收尾。
  但 `AbortSignal.timeout()` 超时同样会产生 abort → 状态永久停在 `streaming`。
- **修复**：
  - 适配器区分两种中止：超时 → `kind: "timeout"`（可重试/可降级），
    用户取消 → `kind: "aborted"`；
  - 引擎新增 `userAborted` 标志：只有用户主动中止才静默返回，
    其余中止一律走降级路径收尾，任何情况下都不会悬挂；
  - `reader.cancel()` 统一吞掉 Promise rejection（此前会产生
    `TypeError: reader has been released` 的未处理拒绝）。
- **验证**：把超时设为 3 秒发送消息 → `completed` + 降级短句
  "嗯…我刚才走神了。" + `llm-fallback-triggered` 错误事件（修复前此处永久卡死）。

## 二、真实模型的换行会产生空气泡

- **现象**：真实回复里出现 2 条内容为 `"\n"` 的空白气泡（8 条消息中占 2 条）。
- **根因**：`SemanticBuffer` 把 `\n` 也列为切分点，且不检查片段是否为纯空白。
- **修复**：切分与结束 flush 时丢弃纯空白片段（不分配 chunk 序号）；
  由于引擎已有"无终止块则补收尾"的逻辑，丢弃空白不会影响完成事件。
- **验证**：修复后真实回复只有 "等会做饭，" 与 "你呢？" 两条实质气泡。

## 三、设置面板补齐超时与重试

- 真实服务商（尤其免费额度）响应可能超过默认 30s，此前无处调整。
- 新增「请求超时（毫秒）」与「最大重试次数」两个配置项，保存后即时同步到引擎。

## 四、真实链路验证结论

- 设置面板「保存 → 测试连接」：`连接成功 · 1227ms`。
- 真实对话：`agnes-2.5-flash` 返回中文口语回复，被引擎按标点切成多条气泡，
  流式结束后自动收尾（`completed`），无需手动中止。
- 请求组装：`window.__lastLLMRequest` 可见人设模板 / 记忆 / 剧情 / 历史均已注入。
- 备注：排查过程中出现过两次 `401`（疑似并发或免费额度限制），
  后续请求恢复正常；如需长期使用建议关注 Agnes 的额度与并发限制。

## 验证命令与结论

```bash
pnpm test           # 201 个测试全绿（新增：空白片段丢弃 ×3、非用户中止收尾 ×1）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第六轮审计：实时 AI 主动消息 + 未读红点

## 一、未读红点没有任何触发源（功能空转）

- **证据**：开启实时 AI 后切到另一个会话，原会话 12 秒内零消息、
  `unreadCount` 保持 0——背景订阅器虽已接好，却永远收不到消息。
- **根因**：定时器只对 `activeSessionId` 触发（`App` 的实时 AI effect），
  非活跃会话的引擎永远不会产生消息，红点与未读数因此是死的。
- **修复**：改为跨会话轮转——每次心跳由新增的
  `pickNextProactiveSession()` 挑一个"最久没有消息"的会话主动发消息：
  活跃会话直接显示，非活跃会话由背景订阅器累计未读。
- **实测**：活跃的苏晚晴 `unread=0`；非活跃的林笑笑 `unread` 增长到 13，
  会话列表对应行同时出现未读徽标与红点（`hasBadge / hasDot = true`）。

## 二、睡眠中的角色会被主动打扰

- **问题**：主动消息没有作息检查，角色睡着时仍可能主动发"嗯…"（drowsy-burst）。
- **修复**：`RealismEngine` 新增 `isSleeping()`（复用 PresenceManager 判定），
  定时器在挑选候选时跳过睡眠中的角色。
- **实测**（先关定时器等流结束，取干净基线）：把林笑笑设为睡眠后重新开启定时器，
  18 秒内其 `unread` 保持 24 不变，而清醒的苏晚晴继续收到主动消息。

## 三、其他

- 新增 `utils/proactiveSession.ts` 纯函数 + 4 个单测（最早消息优先 / 稳定排序 / 不改入参）。
- `packages/ui-wechat/src/index.ts` 现在统一导出 `utils/*`（messageOrder / scrollFollow /
  timeDivider / proactiveSession / characterTemplate），组装层可直接复用这些可测纯函数。

## 验证命令与结论

```bash
pnpm test           # 197 个测试全绿（新增 4 个：主动消息会话轮转）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第四轮审计：会话复用 / 菜单交互 / 角色提示词落库

## 一、通讯录点同一联系人会重复建会话（数据重复）

- **证据**：连续两次 `createSession('char-su-wanqing')` → `sameId: false`，
  会话数 0 → 2；UI 上通讯录点两次同一联系人 → 会话列表出现两条同名会话。
- **修复**：`createSession` 先查找该角色已存在的单聊，命中则切换并返回它
  （微信语义：一个联系人只有一个会话；同时清零该会话未读）。
- **回归**：新增 3 个 store 测试（复用 / 不同角色独立 / 复用时清零未读）；
  实测两次调用 `sameId: true`、会话数只 +1。

## 二、会话菜单按钮无法关闭菜单（交互 bug）

- **证据**：打开菜单后再次点击"更多"按钮 → 菜单仍处于打开状态。
- **根因**：`mousedown` 的 click-outside 先把菜单关掉，紧接着 `click` 的
  `setMenuOpen(v => !v)` 又把它打开（一次点击被处理两次）。
- **修复**：click-outside 判定排除"更多"按钮自身，开关完全交给按钮的 onClick。
- **实测**：第一次点击打开（true），第二次点击关闭（false）。

## 三、角色编辑器的提示词被静默丢弃 + 编辑模式无入口（数据丢失）

- **证据**：
  - `CharacterEditor.handleSave()` 从未使用 `promptContent`，
    并硬编码 `promptTemplateId: "tpl-<archetype>"`（如 `tpl-gentle`）——
    该模板并不存在，用户写的人设既没落库、也不会注入对话；
  - `CharacterEditor` 仅在 `WeChatShell` 中以新建模式挂载，
    `editingCharacterId` 永远为空 → 创建角色后无法修改。
- **修复**：
  - 新增 `utils/characterTemplate.ts`：为每个角色派生稳定且唯一的专属模板 ID
    （`tpl-char-<characterId>`），内容为空时不创建模板并解绑 `promptTemplateId`；
  - `handleSave` 落库模板（已存在则更新，不重复创建）并绑定到角色；
  - 编辑模式回填已绑定模板内容（含 hydration 晚到的补填）；
  - 通讯录每个联系人新增 hover 显示的「编辑」入口，打开编辑器并载入该角色。
- **回归**：新增 4 个纯函数测试；实测创建角色后
  `promptTemplateId = tpl-char-…`、模板内容等于输入；
  再次编辑改为新内容后 `templateCount` 仍为 1、绑定 ID 不变、内容已更新。

## 验证命令与结论

```bash
pnpm test           # 191 个测试全绿（新增 7 个：会话复用 3、角色模板 4）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

浏览器实测：通讯录新建角色（含提示词）→ 通讯录出现角色 → 点开可正常对话
（1 条用户消息 + 8 条分片回复、输入框自动恢复）→ 点「编辑」可回填并修改提示词。

---

# 第五轮审计：角色编辑立即生效 + prompt 组装端到端验证

## 一、角色编辑后不生效（Bug）

- **证据**：把角色改为"当前时间处于睡眠窗口 + silent 策略"后发送消息，
  仍然 `presenceText = "在线"`、照常发出 LLM 请求并回复。
- **根因**：`EngineManager.getOrCreate()` 的注释写着"profile 变化（角色编辑后）重建引擎"，
  但实现是 `if (existing) return existing;` —— 从不比对/推送档案。
  引擎持有构造时的 profile 副本，PresenceManager 与 TypingSimulator 每次都用它，
  于是作息 / 性格 / 人设的改动要刷新页面（或切走再切回触发重建）才生效。
- **修复**：`RealismEngine` 新增 `updateProfile()`；`EngineManager.getOrCreate()`
  在复用实例时把最新档案推送给引擎——作息/性格改动在下一条消息立即生效。
- **回归**：新增 `EngineManager.test.ts`（实例复用 + 档案更新后 silent 睡眠立即不再回复）。
- **实测**：改为 silent 后发送消息 → `已就寝`、`requestSent: false`、`completed`、无回复；
  改为 drowsy-burst 后立即生效 → 注入"半梦半醒"、回复极短（`嗯…` / `怎么了…`）。

## 二、prompt 组装端到端验证（此前只有单测覆盖）

- **新增调试钩子**：`llmRuntime` 在 DEV 下把最近一次 LLM 请求挂到
  `window.__lastLLMRequest`（生产构建不启用），用于核对 prompt 组装。
- **验证结果**（抓取真实请求的 system 内容）：
  - 人设模板：`【补充】这是E2E人设标记：喜欢深夜书店。` ✓
  - 长期记忆：`【你记得的事】- [偏好] E2E记忆标记：用户喜欢冰美式` ✓
  - 剧情摘要：`【当前剧情】章节：E2E章节 / 剧情概要：E2E剧情标记` ✓
  - 历史注入：第二轮请求为 11 条消息（system + 上一轮 user/assistant 序列 + 当前 user）✓
  - 睡眠注入：silent 不发请求；drowsy-burst 追加"你刚被消息吵醒，半梦半醒"✓

## 验证命令与结论

```bash
pnpm test           # 193 个测试全绿（新增 2 个：EngineManager 实例复用 / 档案更新生效）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

---

# 第三轮审计：输入体验 + 时间线 + 长历史验证

## 一、长历史滚动定位（此前担心的隐患，实测无问题）

- 注入 200 条长短不一的消息（内容高 15609px）后刷新：
  恢复后 `gap = 0` 且 4 秒内无漂移——`content-visibility` 的尺寸估算未造成定位偏差。

## 二、输入框不随内容增高（体验缺陷）

- **现象**：`textarea` 固定单行高（35px），写长消息时只能看到一行，无法回看。
- **修复**：`InputBar` 按 `scrollHeight` 自适应高度，上限 100px（约 5 行）后内部滚动。
- **实测**：6 行输入时高度 35px → 98px，`scrollHeight` 142px，超出部分滚动。

## 三、删除会话会写回"孤儿快照"（正确性小 bug）

- **根因**：切换会话的 effect 无条件保存旧会话运行时；删除当前会话后
  `activeSessionId` 变为 null，effect 仍把已删除会话的运行时写回 store 并持久化。
- **修复**：保存前检查该会话是否仍存在（`sessions[prevId]`）。

## 四、新增：微信式时间分隔线

- 类型层早有 `time-divider`，但从未自动插入。
- 新增 `utils/timeDivider.ts`：间隔 ≥ 5 分钟或跨天时插入分隔线，
  文案按「今天 HH:mm / 昨天 HH:mm / M月D日 HH:mm / YYYY年M月D日 HH:mm」分级。
- **实测**：首条 19:47、跨 17 分钟处 20:05 各出现一次，30 秒间隔处不插入。

## 五、待办（本轮评估后暂缓）

- `react-virtuoso` 已无任何引用，可从 package.json 移除（需联网跑 `pnpm install`，
  当前环境网络受限，留待本地执行）。
- `useSmartScroll` 已无组件使用（保留供外部复用，属预期内 API）。
- `chunk-scheduled` 不插入占位气泡，故 `MessageStatus` 的 loading 态不会出现；
  已有"对方正在输入…"提供同类反馈，判定为不需要重复实现。

## 验证命令与结论

```bash
pnpm test           # 184 个测试全绿（新增 8 个：时间分隔线判定与格式化）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

浏览器冒烟（Playwright 实测通过）：会话创建 → 三 Tab 渲染 → 手动新增记忆 →
保存状态卡（首次保存自动创建）→ 手动加事件 → 刷新后记忆/剧情保留 → 发送消息触发拟真回复。

---

# 聊天链路 Bug 修复记录（滚动 / 生命周期 / 持久化）

> 用户反馈：① 每次回复结束后必须点"中止"才能继续对话；② 新消息会"往上挤"；
> ③ 回复过程中列表不跟随滚动。以下为逐项根因与修复。

## 一、回复结束不自动收尾（必须手动中止）

- **根因**：`SemanticBuffer.complete()` 在残余 buffer 为空时（回复以标点结尾——
  Mock 回复正好以"？"结尾）不产出 `isTerminal` chunk，而 `lifecycle completed`
  只在交付 terminal chunk 时发出 → store 的 phase 永远停在 `streaming`，
  输入框持续禁用。
- **修复**：`RealismEngine` 统一 run 终止出口 `finishRun()`；`handleComplete()`
  在本次 flush 未产出 terminal chunk（含空回复）时，于交付队列末尾排程一次收尾，
  保证每轮 run 恰好发出一次 `completed`。
- **回归**：新增 3 条引擎测试（以标点结尾 / 无标点结尾 / 空回复且可立即开始下一轮）。

## 二、消息"往上挤"（顺序错乱）

- **根因**：`stableSortByChunkSequence` 把 `chunkSequence` 当全局排序主键，
  但该序号每轮 `Chunker.reset()` 后从 0 重新开始 → 新一轮消息（seq=0）
  被排到上一轮长回复（seq≥1）之前。
- **修复**：新增 `utils/messageOrder.ts`，按 `timestamp` 主序、`chunkSequence` 兜底；
  乱序数据（hydration）也能纠正。
- **回归**：`messageOrder.test.ts`（跨轮顺序 / 同刻稳定 / 时间戳兜底 / 不修改入参）。

## 三、刷新后消息丢失

- **根因**：`sessionRuntimes` 只在"切换会话时"写入，刷新时该快照不存在，
  恢复逻辑走 else 分支把 chatStore 已恢复的消息**主动清空**。
- **修复**：恢复逻辑三分支——有快照用快照；无快照但持久化消息属于本会话则保留并回写快照；
  真正的空会话才清空。同时在消息变化时持续保存运行时快照。
- 附带修复：恢复出的 `streaming/paused` 阶段统一归一化为 `interrupted`，
  避免刷新后输入框再次被锁死。

## 四、新消息不自动跟随滚动（"往上挤"的另一半）

- **根因**：`react-virtuoso` 的滚动模型与"流式逐条追加 + 高度不定的气泡"不兼容：
  新气泡到达时不跟随（`followOutput` 实测无效），外部主动滚动还会被其内部
  高度估算逻辑撤销（逐帧 `atBottomStateChange(false)`）。
  尝试过 followOutput 常量/函数、`alignToBottom`、`atBottomThreshold`、
  `defaultItemHeight`、`scrollToIndex`、原生 scrollTop 六种修法均不可靠。
- **修复**：移除虚拟滚动，改为普通 DOM 列表 + 原生滚动，用 CSS
  `content-visibility: auto` + `contain-intrinsic-size` 跳过屏外渲染保留性能；
  滚动跟随由 `ChatSessionView` 的"贴底黏性"逻辑统一控制：
  - 贴底（距底 ≤120px）/ 自己发消息 / 首次填充 → 自动滚到底
  - 用户翻阅历史 → 不打扰，累计"N 条新消息"提示，点击回到列表底部
- **实测**：贴底发送 8 条分片回复全程 `gap=0`；翻阅历史时新消息不打断阅读。

## 五、其他连带修复

- **"对方正在输入..."从未显示**：`startStreamWithAdapter()` 未推送
  `typing-indicator active=true`（只有旧的 `startStream()` 推过），已补齐。
- **未读计数在渲染期 setState**：StrictMode 下会被双调用导致计数翻倍，
  已改为 `useEffect`。
- **中止后 `pendingCount` 残留**：`abortStream` / lifecycle `completed`/`aborted`
  分支统一清零。
- **切换会话滚动位置错乱**：列表按 `sessionId` 重建 DOM，切换即复位到最新消息。
- **引擎启动失败会锁死输入框**：`handleSubmit` 增加 try/catch 兜底，
  失败时回退为 `idle` 并给出提示。

## 验证命令与结论

```bash
pnpm test           # 174 个测试全绿（本轮新增 12 个：排序 / 滚动跟随 / 引擎终止边界）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```

> 移除 react-virtuoso 后，web 产物 JS 由 446 KB 降至 393 KB（gzip 145 KB → 126 KB）。

浏览器实测：发送消息 → 8 条分片气泡按序到达 → 全程贴底 → 回复结束输入框自动恢复
→ 刷新后 125 条消息完整保留且仍在底部 → 翻阅历史时新消息不打断阅读。

---

# 第二轮审计：拟真能力回接 + 失败重试（Bug 修复记录）

## 一、逐字打字机 / 错别字纠错整体失效（功能退化）

- **根因**：`TypingSimulator` 是被完整实现的（`plan()` 计算逐字时间轴、气泡间隔、
  犹豫停顿、错别字与纠错时间点；`applyTypo()` 生成错字文本），
  但 `RealismEngine.scheduleChunkDelivery()` 从未调用它——
  硬编码 `revealDelays: []`（= 立即显示全文）、`containsTypo: false`。
  适配器重构时把拟真时序换成了简化公式，导致 TODO 二.1/2.2/2.3 声称已验证的能力全部失效。
- **修复**：
  - `scheduleChunkDelivery()` 调用 `this.typer.plan(chunk, isFirst)`；
    气泡间隔改用 `preDeliveryDelayMs`（含犹豫停顿），逐字时间轴改用 `revealDelays`，
    排队顺延 `nextDeliveryTime = 交付时刻 + 本条打字时长`；
  - 错别字在交付前用 `applyTypo()` 替换为错字版本，`char-reveal-plan` 携带
    `correctedText`（`ICharRevealPlanEvent` 新增可选字段）；
  - `IMessageRuntime` 增加 `correctedText`，`MessageBubble` 在纠错时间点后
    用正确文本替换错字版本。
- **实测**：气泡文本分最多 15 步逐字增长；错字版本
  `我听到你说"我现再` → `我现再在` → 自动纠正为 `我现在在这里呢`。

## 二、失败重试入口未接线

- **根因**：`ErrorRetryBubble` 组件与样式齐全，但从未被渲染——
  LLM 失败后用户只能重新手打一遍。
- **修复**：`ChatSessionView` 在 `lastError && !typingActive` 时显示重试气泡；
  App 侧 `handleRetryLast()` 先移除失败的那条用户消息再走正常发送流程
  （避免历史里出现两条相同消息）；`lifecycle started` 时清除上一轮错误。
- **实测**：注入失败态 → 气泡显示错误文案与"重试" → 点击后重新发起请求 →
  收到完整回复（+8 条）、错误气泡消失、用户消息不重复、输入框恢复可用。

## 三、本轮排查但确认无问题的路径

- 立绘查看器 / 固定立绘在滚动、`content-visibility` 下仍正常渲染（实测未消失）。
- 多会话切换：消息不串（林笑笑会话 9 条 ↔ 苏晚晴会话 30/66 条互不污染），
  切换后自动贴底、未读复位。
- 中止 → 立即重发：中止后输入框恢复、消息保留、可立刻再次发送并正常完成。

## 验证命令与结论

```bash
pnpm test           # 176 个测试全绿（新增：逐字时间轴、错别字链路、correctedText 传递）
pnpm -r typecheck   # 4 包全绿
pnpm build          # core + ui-wechat 构建成功
pnpm build:web      # web 生产构建成功
```
